# OSLO R1 — Build-Spec & Pre-Freeze Consolidation (living manifest)

**Status:** ⚠️ **R1 is in ACTIVE DEVELOPMENT — NOT frozen.** The consolidation machinery below is assembled and kept current; the **freeze is not yet declared.** The owner continues updates (target freeze: **before dev handoff**, per DL-120). This manifest becomes the frozen dev-handoff spec at that point.
**Type:** product-grill consolidation manifest (non-canonical; recommends — the owner ratifies, canon lands via `dl-land`).
> **❄️ FROZEN 2026-07-20 (owner-directed).** Enhancements are frozen — this build is the locked deliverable. **Open R1 items: none** (the full two-beat journey scope, DL-155/156, is frozen complete; the DL-156 "follow-ons" were assessed at freeze as a seeded-demo note, an out-of-scope enhancement, and an already-satisfied item, respectively — not R1 gaps). **Next phase, out of R1 scope:** execution monitoring (Execute → In execution → Outcome), opened only on owner direction. Any post-freeze change needs an explicit un-freeze.

**Current build (snapshot):** `vertical-slices/slice-10-tiering-limits/prototype.html` — **md5 `a327d702` · boot self-check 157/157 · 0 pageerrors** (frozen 2026-07-20). *(Guards: 152→153 at DL-148 `_assertCriticalPathComputed` (tightened DL-150); 153→154 at DL-151 `_assertExportSendsPlanNotAnalysis`; 154→156 at DL-153 `_assertHeroArcIsHonest` + `_assertUnderstandDetailIsNested`; DL-154 repointed the arc guard to Optimize/Execute, no count change; 156→157 at DL-156 `_assertStartHereFollowsTheBeat` (arc guard → 4 nodes/Validate·Improve; nesting guard re-scoped to the persistent read).)*
**Relation to `final-package-summary.md`** (2026-07-12, D001–D142): that remains accurate for the tiering/canon findings (DL-102/103/104) and the owner-open list, but pre-dates the enhancement layer (DL-113→) and the Overview/read rework. Read this manifest first; it is the current entry point.

> **This is a LIVING MANIFEST, not the freeze itself.** The prototype IS the spec — always demo the highest-numbered prototype. This document tracks what R1 will contain, resolves the canon layering (errata folded into parents), records the sign-off state, and lists what the owner still owes — and it is **refreshed as development continues** until the owner declares the freeze.

---

## 1. Consolidation status (freeze pending)

R1 is **not yet frozen.** Per **DL-120**, R1 feature-freezes **before dev handoff**; until then the owner keeps developing. The consolidation this manifest runs (fold errata into parents, keep the slice sign-offs current, resolve the canon layering, track the open list) is **prepared and maintained now** so that declaring the freeze — when the owner is ready — is a clean, one-step cut rather than a scramble.

**What R1 currently contains:** the ten cumulative vertical slices (the original engagement, D001–D142 / DL-102·103·104) **plus** the enhancement layer landed on the intermediate track (**DL-113 → DL-144**, growing), all folded forward into the one deliverable prototype. New enhancements continue to author directly into R1 until freeze. **⛔ DL-145 (execution-ready planning, Phase-1 identity) is a SCOPE/IDENTITY decision only — it folds NO code into R1 (md5 unchanged); its Phase-2/3 build DLs are a new direction, tracked separately below.**

---

## 2. Canon fold-list — errata folded into parents (read this as the R1 canon set)

To hand dev **one** ordered canon set (not a scavenger hunt across errata), each parent decision is listed with its errata/amendments folded in, and the supersession chains resolved.

### Original engagement (unchanged, still R1 canon)
- **D001–D142** — the ten-slice product experience. `decision-log.md` authoritative.
- **DL-102** (Controlled Release & Tiering-in-Alpha). *(DL-101 = a voided hand-authored duplicate; excluded.)*
- **DL-103** (Analysis cost basis) **← DL-104 folded in** (errata: priority/latency residue struck; UP-1/2/5 retired; UP-APPLY/UP-REPORT numbered).

### Enhancement layer (DL-113 → DL-129) — folded set
- **DL-113** — Progress statement unit (the "statement" as the counted unit). *Note: the Progress grounding surface it originally sat on is retired (see DL-123/124); the statement unit survives on the read.*
- **DL-114** (CAF dimension reconciliation — multi-dimension impact + widened Alignment + by-dimension Inference map) **← DL-115 folded in** (wording erratum).
- **DL-116** — CAF drill-down (`ftype` finding-type; quantify the drivers).
- **DL-117** — Confidence↔CAF coupling (episodic trend relocated to the card footer). *Partially superseded by DL-123:* the trend relocation stands; the "qualifier retained" clause is superseded (the global qualifier is retired).
- **DL-118** (Overview four-surface role model + Start-here charter + Progress echo-cleanup) **← DL-119 folded in** (D253 decomposition-disclosure density pass). *Superseded in part by DL-123/124:* the role model + Start-here stand; DL-118's "Progress owns the grounding ledger" and DL-119's statement-type decomposition are retired (grounding left Progress).
- **DL-120** — R1 fold-in / freeze governance (this manifest executes it).
- **DL-121** — Differentiated Confirm (the lead issue's Confirm acts inline; standalone confirms are map pointers).
- **DL-122** — Notification router (awareness record + global toast, interrupt-tier, probe-fenced). **R1-fold: see §3 (owner decision pending).**
- **DL-123** (Option C — CAF with reliability per dimension) **← DL-124 folded in** (grounding leaves Progress entirely → the read). This pair is the current **read architecture**, and it **supersedes/amends**: **D002/D051** (reliability qualifier → per-dimension), **D180/D180a** (Progress grounding → work-state; anti-burndown now guard-enforced), **D183/D183b/D183c** (no composite; grounding language per-dimension), **D194/D253/DL-111** (grounding-ledger unit + provenance states → CAF rows), **DL-118** (Progress owns ledger → the read owns grounding), **DL-117** (qualifier retained → retired). Retired the "blend / 29-hero" foundation-bar owner-lock.

**Net R1 read architecture (what dev builds):** CAF leads as one construct; each dimension carries a neutral level + a per-dimension grounded/inferred evidence cue; the whole-read grounding rollup sits at the foot of the CAF rows; the limiter carries "the read never stands bare"; **Progress is pure work-state** (Open/Closed); load-bearing lives on the Inference map; the global reliability qualifier is retired (overall grounding word survives on-demand in the popover + Why box).

### Recognition, notification, maturity & read-signal layer (DL-126 → DL-134) — landed 2026-07-18
- **DL-126** — **Earned-maturation recognition.** Rewards only monotonic-with-work, ungameable signals (grounding · confirmations · stage), **never the confidence band** (D187/D173c). A milestone tier (earned toast + persistent `✓ largely grounded` marker) and a lighter **first-time tier** (early-engagement curve; self-decaying); pre-activation is value-framed, not earned (`_firstValue` gate). Fire-once, probe-fenced, boot-seeded silently.
- **DL-127** — **Second-tier upsell dormant** (amends **D123**). While no paid tier is purchasable (T-4, no billing rail), the `SECOND_TIER_LIVE` switch is off: proactive UP-7 retired; the limit-prompt paid CTA becomes an honest willingness-to-upgrade **intent probe** (records demand, no fake purchase); Plans shows Basic on the forward ladder. Keeps D123's gating; amends its "purchasable" clause. Honest disclosure + free resolution still lead (D170).
- **DL-128** — **Notification display** (realizes **DL-122 / D179a / D179b**). The payoff card is a **read-delta only**; the event is a notification (toast + record), never pinned in the card; the deep pass routes its notification like every path. **One moment shows one toast** — a recognition outranks the coincident routine notification (record kept); toasts queue one-at-a-time.
- **DL-129** — **The maturity ladder** (amends **D053**). "Stage" is redefined from analysis depth to **earned evidence-maturity**: the 5-rung ladder **Oriented → Corroborated → Grounded → Anchored → Validated**, computed from the user's evidence signals, never from running an analysis. Fast/Deep becomes a run-type + notification, not a stage. The rung-crossings ARE the recognition milestones (DL-126 folds in: stage-Validated → rung Validated, adds Corroborated; grounded/anchored are rungs 3–4). Popover shows the current rung + position; full climb on the ⓘ.

- **DL-130** — **Read-signal role-clarity** (amends **D196b**; refines DL-123/124/129, D051). Three roles instead of four overlapping scales: **the read** (band + CAF), **your progress** (the ladder), **the trust check** (reliability). **Part A:** the top-bar chip carries the ladder **rung**, not the redundant grounding word (grounding survives as the fine measure — limiter, CAF cues, `#ov-grounding` rollup, popover). **Part B:** reliability becomes an adaptive **trust-check** — calm "✓ Sound basis" when solid, loud D052 flag on a thin leg / false-confidence; `--cool` not `--earned`, never celebrated, always available, can fall. A single blended value / co-location were rejected (opposite dynamics; D183b composite). Guard `relIsATrustCheck`. **The composite band (Enh #6) stays deferred to a tripwire.**

- **DL-131** — **Per-dimension evidence cue declutter** (refines DL-123 presentation). The CAF row cue keeps the evidence **word** (level ≠ trust — Option C honesty) but drops the redundant **bar**; when a row is expanded the cue hides and its word folds into the drill's "Rests on." One `_evWord()` home for the thresholds. Full removal was considered + rejected (re-opens level=trust; not redundant with the aggregate grounding rollup).

### First-time read, onboarding & footer layer (DL-132 → DL-144) — landed 2026-07-18
- **DL-132** — **The Overview lead-line** (amends **D179a**; refines DL-123/124). A plain-language sentence leads the read *inside* the confidence card (first-panel intact): the band in plain maturity words + the computed limiter + a clickable pointer into Start here; **no number/forecast** (D183b), guard `leadLineIsSynthesisNotScore`. It **sunsets after first engagement** (opens/confirms the top issue or follows the pointer → retires permanently, never cycles; `leadLineDone`) — a first-time nudge, not a standing echo. Shipped with two safe dedupes (Start-here overflow Attention pointer; Feasibility CAF-tip echo). The required limiter line kept.
- **DL-133** — **The feature tour is offered after orientation** (refines **D044/D093**). Right after the strategic chain, OSLO *offers* the tour (`#tourOffer`; never auto-runs it): accept → tour; decline → the left-rail chip appears as a standing reminder. First-run only (`tourOffered`); chip retires once taken (`tourSeen`); `_syncRailTour` owns visibility; the palette keeps a durable re-entry. Corrects a prior declutter that dropped the reveal-time prompt.
- **DL-134** — **Confidence-panel footer declutter** (refines **D062/DL-096/D056**). Footer reduced to **Why ▾** (the reliability-basis home, D051). Attention map → cut (nav carries it; the map now has one Overview home). Timeline → folded into the **trend chip** (now clickable → History). **"Steady" hidden** — the trend shows only when the read moved; direction guard amended (`shown ⇔ computable && direction≠0`); trend chip registered interactive for its focus ring. The limiter line kept (carries the verb + "never bare", D002/D051/D186c).
- **DL-135** — **"How this is calculated" pill retires** (amends **D054**; refines DL-132). With the lead-line leading the read, the standing method pill was a second competing first-time explainer; it is **retired**, its method essence moved to the concept's own ⓘ ("Outcome Confidence" info tip) — *not* buried in "Why" (the guard-locked reliability-basis home, D051). The now-empty `.num` wrapper (former index home) is removed; no index returns (D183b); orphaned handlers null-guarded. One clean explainer path remains.
- **DL-136** — **Issues + Attention map consolidated** (amends **D062 / D038**). One "Issues" destination with a **Map ⇄ List** toggle (they already shared one `_filt` model); the separate "Attention map" nav row retired. Map is the default view; the last-seen view **persists** on re-entry (`_iaView`); "Attention" is not used as the label (map view reads "Where your plan needs attention"). The map keeps its coverage/zeros job. The **Inference map** stays separate (provenance).
- **DL-137** — **Project summary → hovercard** (refines **DL-096/D046**, **D055**). The self-described-optional standing panel is retired from the Overview; the full summary lives in a hovercard on the project name (`#tbProj`, hover/focus). Nothing trimmed; `#proj-summary` stays a graded voice surface.
- **DL-138** — **"Extended Analysis" → "Outcome Analysis"** (amends the **D040** term). All 44 occurrences renamed (UI + config); terminology only — the two-pass model + token budget unchanged; "Deep Pass" retained. Coheres with **Outcome Confidence** (the read) — Outcome Analysis is the run that firms it.
- **DL-139** — **"Initial Analysis" → "Preliminary Outcome Analysis"** (amends the **D031/D036/D040** term; pairs with DL-138). All 12 occurrences renamed; terminology only. Completes the pair: **Preliminary Outcome Analysis** (fast first read) → **Outcome Analysis** (deeper pass) → both produce/firm **Outcome Confidence**. "Fast Pass"/"Deep Pass" engineering names retained.
- **DL-140** — **Readout document → "Executive Briefing"** (amends **D172d** document name; workspace stays "Reports"). Display-only rename in one place (`REPORT_TYPES.doc` / `_readoutDocName()`); internal key/type-name/ids/guards unchanged (`nm:'Readout'` kept to avoid the D143 dead-type guard false-matching "executive report"). The D172d name guard was made robust (toolbar title must equal the registry doc name). No behaviour change.
- **DL-141** — **Reports workspace holds multiple reports + Outcome Readiness (generated #1)** (amends **D143**, extends **D172d**; realizes part of the reporting-roadmap packet). A slim tab strip switches the **authored Executive Briefing** (editable, untouched) and the first **generated** read-only report, **Outcome Readiness**, computed entirely from live state. The D143 "exactly one type" guard is amended to "**real types only**" (every entry needs a `render`; the six dead types stay dead) — protection preserved, real additions admitted. No forecast/composite (D183b); maturity-not-health (D003).
- **DL-142** — **Assumptions & Evidence (generated #2)** (extends **DL-141**; realizes the second of the three reports). OSLO's due-diligence snapshot — *what the plan rests on* — computed entirely from live state, read-only, distinct from the authored Briefing. Renders the **evidence split** (Confirmed vs still-inferred, `_progressRows()`), the **load-bearing assumptions still unconfirmed** (`_ciLoadBearingStatements().filter(_ciInferred)`, severity-ranked by supporting issues — the "confirm these first" list), and **open questions** + **per-dimension inference** (`_openClarIds()` · `_ciDimInferenceStats()`) with the Option-C honesty line. Nothing invented; no forecast/composite (D183b); inference named honestly, never as a warning (DL-109). Dynamic severity classes built outside the `class="…"` literal (`_assertEveryClassNameResolves`). Summary view ships; Full depth is a later pass.
- **DL-143** — **Decision Record (generated #3) — the trio complete** (extends **DL-141**; realizes the last of the three reports). The **human-judgement half of the strategic chain**: the owner's own decisions, computed live from the `_decision` register, each paired with what it firmed. Per decision: **what was decided** (Applied OSLO's fix / Answered OSLO's question / Chose a path), **what it firmed** (document → Confirmed by you, with the reliability move; a selection named honestly as *addressed, not confirmed*), and **whether the read has taken it up** (**Live in the read** vs **Awaiting the next analysis update**). **D088 is structural** — no decision is ever credited with moving the Outcome Confidence read; that moves only at the next analysis update, and the report computes "taken up" as *a run newer than the decision's own event*. Neutral marker (not severity); no composite/forecast (D183b); computed never invented (D173). Taken-up transition verified end-to-end. Dynamic status class (`gr-dstat-live`/`gr-dstat-await`) built outside the `class="…"` literal. Summary view ships; Full depth is a later pass.
- **DL-144** — **Generated reports get Summary ⇄ Full depth + Export — the reporting roadmap complete** (extends DL-141/142/143; realizes the packet's cross-report pass). **(1) Depth** — a persisted **Summary ⇄ Full** toggle (`repDepth`, per report) on **Assumptions & Evidence** and **Decision Record**; **Outcome Readiness is single-depth**. Full is genuinely additive, all computed: Assumptions·Full names each load-bearing item's supporting issues and prints the **complete inferred register** by dimension (`_ciStatements().filter(_ciInferred)`) + the confirmed-side count; Decision·Full adds per-decision issue provenance and the **append-only withdrawn trail** (`HISTORY type='withdrawn'` — a withdrawal is a new event, never an erasure). **(2) Export** — every generated report carries an Export control that reuses the **one ratified export surface** (`openExportSeam()` — currency marker, disclaimer, tier gating, append-only export record); **no parallel machinery**, and "send" = that modal's view-only share link, kept distinct from a frozen export per **D107**. **D088 unbroken** — depth changes how much is shown, never what a decision claims about the read. Class-resolve clean (D195a). *This completes the reporting roadmap: three generated reports (Outcome Readiness · Assumptions & Evidence · Decision Record), each exportable, two with depth, alongside the authored Executive Briefing.*

**Scaffold-only (no canon):** the demo bar was reorganized (Sim states → a `Simulate ▾` menu; hide/restore; always-visible Restart) and the simulated fast pass was reduced ~30s → ~10s for testing (the product ~60s target is unchanged). Testing conveniences — nothing lands.

### Execution-ready planning — a NEW direction (DL-145, Phase-1 identity — landed 2026-07-19)
- **DL-145** — **Execution-ready planning — OSLO produces the detailed, exportable plan (Phase-1 identity)** (**Class A**; packet `DECISION-PACKET-execution-ready-planning.md`). ⛔ **SCOPE/IDENTITY ONLY — no product code; md5 unchanged 4474bc48.** Ratifies OSLO's deliverable as an **outcome-optimized plan detailed & exact enough to export into an execution tool and run** — assessment is the mechanism, the plan is the end. Seven ratified components: **(1)** boundary = the **export handoff** (OSLO produces/certifies; the execution tool runs — *reinforces* progress≠completion); **(2)** OSLO **both authors & certifies** — infer-to-complete + use-and-infer, **every inference marked for validation** (the From OSLO / Confirmed by you engine at task altitude, reconciling D173 with authoring); **(3)** depth = **infer to completeness, honesty via grounding**, outcome-driven depth, **graded inference**; **(4)** readiness = **coverage of the execution-critical set → a named validation-progress state** (what you've validated, *never* a will-succeed verdict), derived from coverage, non-blocking; **(5)** **sequencing dependencies in — edges + critical path** (feasibility analysis; durations inferred-marked; resource-leveling deferred) — **retires the "no dependency register" implementation stance**, kept distinct from **D114** understanding-dependencies; **(6)** **one converged task model** underneath, the **seven documents stay as focused views** + an **eighth consolidated pre-export view** (home to readiness + critical path); **(7)** execution-export = **deep connector, Asana first** (general/cross-functional PM ICP; Jira fallback), **provenance as a native field** (non-negotiable — the validation loop must survive the hand-off). Spine reinforced, not weakened: **D003** maturity-not-health · progress=understanding · **D183b** no composite/forecast · **D173/DL-109** computed-then-marked · OSLO advises. **Follow-ons (own DLs): Phase-2** — the model (task decomposition · task-altitude CAF + issues · sequencing/critical-path · the 8th view); **Phase-3** — the structured Asana export + the Work breakdown mock deepened (the mock *follows* the decision — deliberately the last thing built).
- **DL-146** — **Phase-2 · 2A — the Work breakdown becomes an authored, graded task tree** (Class B; realizes DL-145 §2/§3; **first code manifestation** — md5 **cc81c52a**). The flat 5-row table becomes an **authored task tree**: five workstreams decomposed to task/subtask level (outline-numbered `1·1.1·1.3.1`, `data-lvl` indentation). **Every row is From OSLO** (`_seedTableProvenance` marks all cells derived) until the user confirms it **by editing the cell** — the ratified attestation engine at task altitude, **no new validation path** (DL-145 §3.1). **Graded inference**: thinnest inferences carry a neutral **`.conf-low`** grade (epistemic, **never severity** — D003). Built on the **unchanged `<table>` format** so all table machinery + the 152 guards verify on the deeper tree; CI-32–38 + ISS-05 preserved; class-resolve clean. *Next: 2B (task-altitude CAF + issues) · 2C (dependencies + critical path) · 2D (8th consolidated view).*
- **DL-147** — **Phase-2 · 2B — assessment scaled to task altitude** (Class B; realizes DL-145 §5.2; extends DL-146; md5 **81e97288**). OSLO now **finds task-level gaps** invisible at deliverable altitude. Two findings surface on the **deeper read** (Extended pass) through the **same issue engine**, anchored to tasks: **ISS-10** *The freeze rests on undated tasks* (Feasibility, task 2.1 — the Sep 1 freeze depends on undated CFP-close/program-selection; visible only once decomposed) and **ISS-11** *Part of the breakdown is inferred* (Clarity, task 1.3.1 — OSLO's read on its own low-confidence decomposition; honest inference per DL-109, never a warning). Real issues (raise WBS 1→3; light CAF/Attention/panel; `_submitClarification` path). Invents nothing (D177); CI-71–73 (`hz:'deep'`). Observation-type (no clar) so the **D167 opening-turn budget** holds. *Next: 2C (dependencies + critical path — ISS-10 is the anchor) · 2D (8th consolidated view).*
- **DL-148** — **Phase-2 · 2C — sequencing dependencies + computed critical path** (Class B; realizes DL-145 §5; extends DL-146/147; md5 **8d10fb1f**; **guard count 152 → 153**). A `WBS_TASKS` model carries dependency edges + inferred durations (low-confidence). `_criticalPath()` runs earliest-finish over the DAG and **computes** the longest chain to the Sep 1 milestone (**Close the CFP → Select the program → Lock the run-of-show, ~5 wk** — longer than the venue chain). Rendered as a **From OSLO** “Sequencing & critical path” panel in the WBS view, durations low-confidence, ISS-10 link when live; feasibility analysis on OSLO's side of the handoff, **outside `#artdoc`** (never editable plan content). **Computed, not authored** (D173): `_assertCriticalPathComputed()` proves the chain follows the edges and is the longest. Resource leveling deferred. *Next: 2D (8th consolidated pre-export view) — final Phase-2 slice.*
- **DL-149** — **Phase-2 · 2D — the eighth consolidated view (Full plan); PHASE-2 MODEL COMPLETE** (Class B; realizes DL-145 §6; ties together DL-146/147/148; md5 **6d3aca1e**). The seven documents stay focused; the **eighth “Full plan” view** (peer pane `showView('fullplan')`, nav ⊞) is the whole sequenced plan, the pre-export surface. Computed: **(1)** execution readiness — coverage of the execution-critical set (`_execReadiness()`) → a **validation-progress state** (*Mostly OSLO's draft → Load-bearing confirmed → Fully validated*), what-you've-validated not a verdict, **non-blocking**, coverage bar not health (D003); **(2)** the computed critical path (2C, reused); **(3)** confirm-before-hand-off list (open execution issues incl. 2B findings, routing to the existing confirm surfaces, D088); **(4)** *Export to your tool* → ratified `openExportSeam()`. Seven documents + guards untouched; `.conf-low` made global; class-resolve clean. **✅ Phase-2 model complete (2A–2D).** *Only Phase-3 remains: the structured, provenance-preserving Asana export (the Export button is its entry).*
- **DL-150** — **Phase-2 · 2D completion — the consolidated view displays the plan** (Class B; completes DL-149; realizes DL-145 §6 fully; md5 **fa29a13a**). Owner-reported gap fixed: the Full plan view showed the analysis but not the plan itself. Now renders **the whole task model** — 14 tasks × 5 workstreams (owner · inferred duration · dependency · critical-path mark · low-confidence), from `WBS_TASKS` **promoted** to the full decomposition (one source of structured data for the consolidated view AND the critical path, not re-parsed HTML). Correctness fix: chains to different deadlines → critical path is longest **to the milestone**; `_assertCriticalPathComputed()` tightened (reported path unchanged). `fpt-*` class-resolve clean. *(Deeper convergence — the WBS document off the same model — is a later refinement.)*
- **DL-151** — **Phase-3 — the structured Asana export; DIRECTION COMPLETE** (Class B; realizes DL-145 §7; consumes DL-146–150; md5 **8811516c**; guard **153 → 154**). *Export to Asana* (Full plan) → mapping preview → simulated hand-off + History record. **(1) Only the plan crosses** (tasks/subtasks→tasks · owners→assignees · dates→due · deps→deps); **OSLO's analysis stays in OSLO** (source of truth; user returns during execution; monitoring reads Asana back rather than a stale copy). `_assertExportSendsPlanNotAnalysis()` enforces the execution allowlist — any analysis field fails the build. **(2) Provenance = OSLO-owned read-only custom field** (3 values, 2A grade carried) as an honesty signal (validation stays in OSLO); **+ OSLO Task ID custom field** (the monitoring-phase reconciliation anchor). Tag fallback (free-tier = degraded for monitoring). Non-blocking (DL-145 §4); export≠share (D107); simulated. `#asanaExportScrim` registered in `_DIALOG_PANELS`. **✅ Execution-ready direction complete: identity (DL-145) → model (DL-146–150) → Asana export (DL-151).** *Runway laid for the next direction: execution monitoring (read Asana state back via the OSLO Task ID anchor, OSLO stays the live intelligence layer).*

### Overview hero repositioned to the plan's journey — the front door speaks the north star (DL-152 identity + DL-153 build + DL-154 naming amendment — landed 2026-07-19/20)
- **DL-152** — **The Overview hero is the plan's journey to a hand-off (Direction C-1)** (**Class A**; packet `DECISION-PACKET-overview-hero-repositioning.md`; **reframes D199 / D174**, preserves D003/D183b · D179e · D196a). Given the DL-145 export north star, the Overview leads with **where the plan is on the way** to its destination, not Outcome Confidence alone: a 3-node arc (originally *Understand → Confirm → Hand off*; renamed by DL-154 below). **Node 1** = Outcome Confidence (the maturity read, unchanged as concept *and* metric — ramp/CAF/limiter/reliability all first-class); **node 2** = execution-readiness coverage (`_execReadiness`, now surfaced on the Overview, not only Full plan); **node 3** = the **destination** (export non-blocking, DL-145 §4), a dashed ↗ marker, **never a "ready" verdict**. *"How ready is my plan?"* = **position on the arc**, computed. **C-1 is the structural fix**: since export was always non-blocking, the third node was never a threshold, so the "ready to *succeed*" forecast trap is removed **by construction, not by disclaimer** (D003/D183b hold). Composition ratified: **additive + V1-nested** (below). Identity/scope call — the build follows in DL-153.
- **DL-153** — **The Overview journey hero, built — arc + nested Understand read + guards** (Class B; realizes DL-152; consumes DL-149 `_execReadiness` + the DL-146–150 model; md5 **c313570c**; guard **154 → 156**). Three things ship, each guarded: **(1) the arc** (`renderHeroArc`/`_planStage`) — active node computed (node 1 below High, then node 2), the third node a destination never active; `_assertHeroArcIsHonest()` enforces one computed active node, an always-reachable destination, and **no forecast/health vocabulary**. **(2) the V1 nesting** — the Outcome Confidence panel is wrapped in an inset `.ch-nest` with the cool **maturity** left-edge + an *Understand* tab, the active node dropping a maturity spine into it, so the panel reads as **the Understand node OPENED**, not a disjoint section. The panel markup is **byte-for-byte the ratified hero**, so its guards keep verifying; `_assertUnderstandDetailIsNested()` makes the nesting a build fact (heading + ramp are descendants of the inset, the tab exists, the arc precedes the detail, the spine drops). **(3) §5B resolved — two scoped counts, one substrate**: the arc's node-2 "**7 of 23 execution-critical**" (`_execReadiness`, WBS·Schedule·Resources) vs the panel's "**20 of 48 statements**" (whole-read grounding, D179e's one home) — not the same tally twice; each names its scope so they can't read as a contradiction. Maturity accent only (hue ≈214°, cool — no RAG, no `--primary` on state elements); class-resolve clean (`ovj-drop`·`ch-nest`·`dtab`).
- **DL-155 / DL-156** — **The journey gains its two beats — Understand → ⟮Optimize: Validate · Improve⟯ → Execute** (**DL-155 Class A** identity, amends DL-152/154 + supersedes DL-153's read-under-Understand clause; **DL-156 Class B** build, amends DL-153; md5 **a327d702** · guard **156 → 157**; packet `DECISION-PACKET-journey-validate-improve`). The middle *Optimize* splits into its **two beats on the axis** under an Optimize bracket: **Validate** (confirm load-bearing inferences → the read becomes *trustworthy*; metric = execution coverage) then **Improve** (raise the trustworthy read; metric = the maturity band). **Each node owns one metric** — coverage and the read stop competing — and they are **never merged** (level ≠ trust: confirm bad news → coverage↑, feasibility↓; 23/23 can still be Moderate; merging = the false-confidence forecast D183b/D180 forbids). **Understand → first-time milestone** (behavioural trigger `_planStage`, computed D173, replaces the backwards band≥High rule; ✓ done after the first optimization action). **§6A the read is a PERSISTENT hero panel** (D179a; "The read" tab; supersedes DL-153's read-as-Understand's-detail — the nesting guard is re-scoped). **§6F Start here is beat-aware** (`_beatOrder`/`renderFocus`): Validate leads with what grounds the read, Improve with the limiter dimension; computed, advisory, non-blocking, no tally — guard `_assertStartHereFollowsTheBeat`. Cool `--maturity` accent only (done ✓ never green); class-resolve clean (`ovj-brk`·`ovj-stage.done`·`focus-beat`). Demo conveniences (Simulate ▾): *Sim first-run (Understand)* + *Replay onboarding*. **Logged follow-ons:** demo Start-here doesn't visibly reshuffle (seeded lead leads in every beat); per-beat inline detail deferred; §6C coverage-bar treatment open.
- **DL-154** — **The journey hero speaks the north star — stages become Understand → Optimize → Execute** (**Class A**; **amends DL-152's naming**; build applied, md5 **3d7ab6cb**; guard count unchanged). The framing DL-152 ratified stands; the *stage names* sharpen so the front door narrates the **outcome-optimization journey**, not generic workflow. **Confirm → Optimize** (names the value — firming the plan for the outcome — not the mechanic), **Hand off → Execute** (concrete destination), and the frame line becomes *"on the way to the outcome."* **"Confirm" is not retired — it drops a level**: *Optimize* is the **stage** name; *Confirm* stays the **per-item verb** (D196a) and the *Confirmed by you* provenance class is untouched. **"Optimize" is an activity, not a verdict** — it mirrors the DL-145 north-star language ("outcome-*optimized* plan") while staying clear of the forecast reading (D003/D183b). The arc-honesty guard was repointed (destination keys on "Execute", active stage on "Optimize"); everything else — C-1 structure, the V1 nesting and all its guards, the two-scope §5B counts, non-blocking export — is unchanged. **Forward note (owner-raised):** the arc is the lifecycle spine — it extends past *Execute* into execution nodes (*In execution → Outcome*) when the monitoring direction arrives; each stage opens into its own detail the V1 way. The anti-health spine (D003/D183b) carries across the boundary.

---

## 3. Notification router — R1-fold (RATIFIED — DL-125)

**Resolved 2026-07-17: the notification router folds into R1**, ratified as **DL-125** (PR #174, `oslo-canon-bot`, Doc Integrity green, squash-merged), which amends DL-120's out-of-scope line for the router. It is built + verified (boot-clean), its routing doctrine landed as DL-122, and it ships inside the deliverable prototype — so R1 spec and R1 build now agree. Still out of R1 (unchanged): the confidence-architecture tripwire revisit; the R1-test-driven fixes bucket.

---

## 4. Slice sign-off status (all ten — clean for handoff)

Per `slice-signoff.md`, all ten slices are signed off; the surfaces reworked by the enhancement layer are re-signed on the current build:

| Slice | Sign-off state |
|---|---|
| 1 Access & Onboarding · 2 Intake & Fast-Pass · 4 Attention Map · 5 Artifact Workspace · 6 Issues · 7 History · 8 Workspace · 9 Collaboration | Signed off (original engagement; untouched by the enhancement layer). |
| **3 Overview & Understanding Console** | **Re-signed DL-123 → DL-124**, then **DL-126 → DL-144** (recognition · notification display · maturity ladder · read-signal role-clarity · CAF cue declutter · lead-line · tour offer · footer declutter · how-calc pill retire · Issues+Attention consolidation · project-summary hovercard · Outcome/Preliminary-Outcome Analysis renames · Executive Briefing rename · multi-report workspace + all 3 generated reports: Outcome Readiness · Assumptions & Evidence · Decision Record · Summary⇄Full depth + Export). |
| **10 Tiering & Limits (THE DELIVERABLE)** | Signed off (D134–D142; DL-103 fold-in); **re-signed DL-123 → DL-124**, then **DL-126 → DL-144** (all of the above enhancement layer) — **md5 4474bc48 · 152/152**. Tiering (incl. the Reports workspace's document rename) never structurally reopened. |

No slice carries an open P1/P2. WI-R1…R6 closed.

---

## 5. Doc-integrity debt — categorized (canon-zone hygiene, NOT product-grill debt)

Doc Integrity on the canon repo reports **870 docs · 0 errors · 61 warnings** (green — warnings are advisory, non-blocking). Pulled 2026-07-17; the 61 sort into three buckets, **none of them in the product-grill deliverable** — they are pre-existing terminology/status drift in the engineering, scope, and handoff zones (`00_owner`, `10_product`, `20_handoff`, `30_engineering`), which this grill does not own and which are not in its connected access:

- **54 × `[banned-term]`** — an architecture term the register discourages appears in a spec: **"Context Plane" (~45)**, **"Judgment Layer" (~7)**, **"Outcome Management" (2)**. Spread across `OSLO_RELEASE_1_CANONICAL_SCOPE_V1`, the analysis-engine + runtime-model + cognitive-responsibility specs, the API/event handoff contracts, `CURRENT_TRUTH`, the product vision, etc. **Fix:** replace each with its canonical term from the `CANONICAL_GLOSSARY` / banned-terms register (the mapping lives in the doc-integrity config, a zone not connected here — do not guess the replacements).
- **6 × `[active→superseded]`** — an active doc references a superseded one: the **FINDING_PANEL** / **RECOMMENDATION_PANEL** specs are superseded by the **…_WORKSPACE** specs (update their status headers; repoint the two refs in `RELEASE_1_UX_HANDOFF_PACKAGE_SPECIFICATION_V1`); and `RELEASE_1_DATA_MODEL_SPECIFICATION_V1.2 → V1.1` (×2). **Fix:** update status metadata / repoint the cross-references.
- **1 × `[renamed-term]`** — `code/docs/deep-tasks/…/deep-task-0007.md` uses retired `canonical_key` → use `dedup_key` (DL-053). **Fix:** rename the key.

**This is a canon-repo terminology pass, separate from the product freeze — it does not block R1** (0 errors; warnings advisory). **Path to clear:** either the owner runs the pass in their repo (they hold the register + full-repo access), or grants this grill the `10_product` / `20_handoff` / `30_engineering` / `00_owner` folders + the banned-terms→replacement mapping, and the grill produces the exact edits. **Recommendation:** treat as a scheduled canon-hygiene pass, not a freeze blocker.

---

## 6. Owner-open items (carried forward — still open, none blocking the freeze)

The tiering/reporting/economics open list from `final-package-summary.md §5` and `tier-definitions-census.md` is **unchanged** by the enhancement layer (the enhancements were all read-architecture, not tiering). Still owed, in priority order: the **local-model judgment eval** (DL-069) → **E1–E3 engine economics** → the **Reporting specification (M4)**; then the census values (monthly analyses, Basic-price basis, UP-APPLY threshold, collaborator seats, OD-10 window, Free CRR ceiling, MON-04 cap, report names, billing rail, CR-2-vs-budget-gate, reverse-trial duration). Each ships **visibly unset**. See `final-package-summary.md §4–5`.

**Deferred by design (not R1):** the R1-test-driven fixes bucket (post-handoff); the outcome-confidence composite "revisit at a tripwire" (Enh #6 — decision was *keep the composite band now*; the packet should be placed on the device for the record). *(The two-hero-explainers question is now resolved — DL-135 retired the "How this is calculated" pill; no longer open.)*

---

## 7. Deliverable & package

- **Build against:** `vertical-slices/slice-10-tiering-limits/prototype.html` (md5 `62bcb47e`) + its reconciled house docs (frontend-ui · user-experience · success-criteria · e2e-test-scenarios · edge-cases · open-items — all carry a `⛔ RECONCILED 2026-07-17` note for the DL-123/124 surface).
- **Canon:** `decision-log.md` (D001–D142) + the DL records DL-102…DL-153 in the `oslo-knowledge-base` repo (DL-145 = execution-ready planning identity, scope-only; DL-146 = 2A · DL-147 = 2B · DL-148 = 2C · DL-149/150 = 2D + consolidated plan · DL-151 = Phase-3 Asana export, **execution-ready direction complete**; DL-152 = Overview hero repositioned to the plan's journey (identity, C-1) · DL-153 = the journey hero built (arc + V1-nested Understand read + §5B) · DL-154 = stages renamed to Understand → Optimize → Execute (identity amendment of DL-152) · DL-155/156 = the two-beat journey — Understand → ⟮Optimize: Validate · Improve⟯ → Execute, per-node metrics, persistent read, beat-aware Start here (identity + build, amend DL-152/153/154) — md5 a327d702 · 157 guards).
- **This manifest** is the entry point; `final-package-summary.md` remains the tiering/canon-findings reference.

---

## 8. Governance

This manifest **recommends**; only the owner **ratifies**. The two ratifications it asks for: **(a)** the notification-router R1-fold (§3), and **(b)** whichever owner-open items (§6) the owner wants to close before dev handoff. Canonical changes route through **Framework 001** and land via **`dl-land`** (one PR in flight, merged linearly).

**Only the owner ratifies. This manifest recommends.**
