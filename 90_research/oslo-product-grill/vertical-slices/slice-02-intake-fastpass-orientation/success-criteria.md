# Slice 2 — Intake & Fast-Pass Orientation · Success Criteria

Cumulative: all Slice-1 criteria continue to hold (no regression). New Slice-2 criteria below.

## Regression — Slice 1 preserved
- [ ] Activation flow works: invite → activate → welcome → intake (D022).
- [ ] Four start methods present; Sample loads brief into composer, user-initiated (D023/D030).
- [ ] GA preview toggle exposes the anonymous + save-to-keep layer; hidden in Alpha default (D032).
- [ ] One-time strategic-chain orientation fires on first project; account-menu replay works (D027).
- [ ] Account menu logout + stay-signed-in persist via localStorage (D028).
- [ ] Advisory footer persists; dark default + focus rings + reduced-motion (D001/D015).

## D035 — seven plan artifacts
- [ ] All 7 sections (Intent·Context·Scope·Requirements·Work breakdown·Schedule·Resources) are constructed and listed.
- [ ] Inferred content is labelled **From OSLO** and reliability-qualified (High/Moderate/Low).
- [ ] Thin evidence produces Clarification Requests, not fabricated certainty.

## D036 — measured Fast Pass time
- [ ] Arrival notice shows a **measured** elapsed time framed "under the 60-second target"; no hardcoded canonical number. Pacing ≈30s (D031).

## D037 — six Fast Pass outputs
- [ ] Overview surfaces confidence, initial attention (reachable + badge), top issues, clarifications, suggested-fixes pointer, and analysis status.

## D038 — Overview landing + co-primary Attention
- [ ] Post-Fast-Pass landing is the confidence-led Overview (focal score, CAF bars, no ring — D019).
- [ ] Attention Map is reachable via the top-center view switch; heatmap = 7 sections × Clarity·Alignment·Feasibility; severity color only on issue cells (D003).

## D039 — first-run orientation + fresh arrival
- [ ] Strategic-chain orientation fires first project only.
- [ ] Arrival notice shows only on a fresh analysis (not on a returning-user re-open).

## D040 — Extended Analysis supersedes
- [ ] Extended Analysis auto-runs after Fast Pass, non-blocking.
- [ ] On completion: chip Provisional→Current, "superseded the provisional orientation" banner, refined read (58→62 / Feasibility Very Low→Low).

## D041 — failure → last-good + retry
- [ ] Armed failure keeps the last-good read and shows "couldn't complete — showing your last-good understanding · Retry."
- [ ] Retry recovers provisional→current.

## D042 — clarification loop
- [ ] The same clarification appears at orientation (light prompt) and inside the tied issue.
- [ ] Answering marks the section Confirmed by you, re-runs analysis, and closes the issue; Overview/Attention counts update.
- [ ] Issues are never resolved by hand — only reanalysis moves them (D006).

## Cross-cutting
- [ ] Confidence number never bare (band + reliability + cause present) (D002/D019); 5-band scale (D020).
- [ ] No copy implies OSLO plans/decides/runs the project (D001).
- [ ] `node --check` on the extracted `<script>` passes with no error (D016).
