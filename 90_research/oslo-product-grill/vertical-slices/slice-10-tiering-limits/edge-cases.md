# Slice 10 — Tiering & Limits · Edge Cases

# ⚠️⚠️ RECONCILED 2026-07-20 — DL-143 → DL-156 LANDED AFTER THIS DOC. READ THE DELTA FIRST.

**This doc was last reconciled 2026-07-17 (through DL-124).** DL-143 → DL-156 landed afterward — the **reports trio + Summary/Full depth + export** (DL-143/144), the **execution-ready planning direction** (DL-145 identity; DL-146–150 the authored task tree · task-altitude issues · computed critical path · the eighth "Full plan" consolidated view; DL-151 the structured Asana export), and the **Overview two-beat journey** (DL-152–156: Understand → ⟮Optimize: Validate · Improve⟯ → Execute, persistent read, beat-aware Start here). These are **built into the frozen prototype** (`prototype.html`, md5 **a327d702** · boot 157/157) but **not folded into this doc**. On those surfaces, where this doc disagrees with the **frozen prototype / the DLs**, they win. See `RECONCILIATION-2026-07-20-DL143-156.md` (same folder) for the full delta and `../../RELEASE_1_BUILD_SPEC.md` for the R1 freeze marker (zero open R1 items).


---

# ⚠️⚠️ AMENDED 2026-07-12 — **D162 IS RATIFIED. READ THIS FIRST; EVERYTHING BELOW IT IS SUBORDINATE.**

**The owner rejected the Issue Panel as cognitively overwhelming.** The D159 sweep hit the modals and the big surfaces and **missed the panels — which is where the doctrine copy had accumulated worst**, because every doctrinal rule has a touchpoint on an issue (evidence · provenance · comments · share · append-only · CR-2) and each one had grown a paragraph explaining itself. **The panel had become a museum placard about the product.**

This is a **presentation / IA pass** — *no logic changes, no behavioural regressions, every runtime guard still passes.* Where anything below conflicts, **D162 wins** (and D162 extends D159, which still governs everywhere else).

## D162a — the copy rule (extends D159)

> **Say the honest thing ONCE, in the fewest words, at the moment it matters. Never twice. Never with its rationale.**

- Honest **labels** stay — *"Comments never change the assessment"* (D111), *"Evidence, not a verdict"*, *"previous analysis"*, *"Analysis is behind your edits"*, the reliability qualifier. **Once, and short.**
- **Contracts, rationale and reassurance move to an info affordance (ⓘ) — never resident.**
- **Deleted outright, and they stay deleted:** the *"◆ this is a validation recommendation — the kind a second pair of eyes settles fastest / prime candidate"* hint (**design rationale, said out loud**) · the *"0 review requests sent · free and unlimited — on every plan"* counter (**reassurance addressed to the OWNER, not the user**) · the duplicated *"never change the assessment"* sentence and *"a conversation about the read, recorded next to it"* · the append-only lecture · the *"Sends the issue + its context + the recommendation + the artifact reference. It never changes the issue."* contract (**→ ⓘ tooltip on the button**).

## D162b — progressive disclosure, driven by INTENT

The user opens an issue to learn **"what's wrong, and what do I do about it?"** Everything else is an action they *may* want.

**Always visible (the default state):** title · severity · dimension · where it lives (artifact + jump link) · lifecycle track · the plain-language read (*Why this matters* + the dimension impact) · **ONE primary action** — *Apply this fix* where there is a recommendation, *Answer* where OSLO is the one asking, otherwise none.

**Everything else is ONE scannable row** — label · count · chevron · **real hover state** — expanding in place, independently, keyboard-accessible (`<button>`, so Enter/Space work natively), with correct `aria-expanded`:

```
▸ Evidence · 2
▸ Clarification · Has the venue confirmed Wi-Fi for 500+ concurrent…
▸ Comments · 0
▸ Reviews · 1 · 1 awaiting        (only when reviews exist)
  [⤴ Share for review] ⓘ   [✦ Discuss with OSLO]
```
> ⛔ **D184 / D190c — THE RECOMMENDATIONS ROW IS GONE.** The recommendation is **resident above its button**
> (D184.1), and its **alternatives expand in place, directly beneath it** (`Other options (2)` → `#ipAlts`).
> **The disclosure row under Evidence was DELETED** — *the alternatives to a recommendation are part of the
> DECISION; Evidence is the RECORD.* Nothing about the fix is behind a chevron any more.

Row state persists **while the panel is open** (a re-render from `selectPath` / `addComment` must not collapse what the user opened) and **resets on close** — a fresh open is a fresh, minimal read.

## D162c — the three affordance defects, fixed

| Defect (owner-reported) | Fix |
|---|---|
| **Evidence looked FLAT** — it had a `▸` but no hover state, no cursor change, no affordance. The user could not tell it expanded. | The `.ip-rowh` component: **pointer cursor · hover background · chevron that rotates on expand · visible count · focus ring**. Every secondary section now uses it — one component, not five bespoke headers. |
| **Clarification defaulted EXPANDED**, with a large empty textarea dominating the panel. **A big empty textarea shouts "do work now"** at a user who came to *read*. | **Default MINIMIZED.** The row names what OSLO needs (question preview, truncated to 54 chars); the input appears on expand. *"Answer in chat →"* stays inside the expanded state. The **primary "Answer" button** (when OSLO has no recommendation) opens the row and drops the caret in it. |
| **Share for review carried three lines of explanation.** | **Just the button.** The contract lives on an **ⓘ**. The prime-candidate hint and the CR-2 counter are **deleted**. |

## D162d — cascaded

- **Recommendation panel** (the `.ip-rec` block): the *"Applying drafts the change into your plan. Discussing changes nothing."* note and the *"Recommendations live only inside the issue"* rationale → **ⓘ / button tooltips**. *"Possible resolution paths"* → **"Other options"** (D190b — *"path" is jargon dressed as plain English*). *"— recorded as your chosen approach"* → dropped (the **Confirmed by you** tag already says it).
- **Reviews block:** the responses stay **in full, forever** (that is the record) — the lecture around them does not. *"This is evidence, not a verdict … it went into an analysis run … OSLO did not accept it on your behalf"* + the D133 alignment essay collapse to **one line + an ⓘ**: *"**Evidence, not a verdict.** ISS-03 is still **Open**. · Attested by \<name\> · Folded into **Alignment**."*
- **Artifact flyout** (`.anno-pop`): the read (severity · dimension · what's wrong, truncated to 150 chars) + **Open issue →** and the span-specific **Ask about this →**. **"Share for review →" is gone from the flyout** — it fired a full modal out of a hover, and it lives one click away in the panel where its contract can sit on an ⓘ. **CR-2 is untouched: nothing disabled, nothing metered.**
- **Share dialog:** prime-candidate hint and CR-2 counter deleted there too.

## Word count — user-visible copy in the Issue Panel

| | Before | After | Δ |
|---|---|---|---|
| **Default state (what the user actually sees on open)** | **~206 words/panel** (1,236 across the 6 issues) | **71 words/panel** (427) | **−65%** |
| Copy present in the panel DOM at all (incl. collapsed rows) | 1,322 | 828 | **−37%** |

*The first row is the one that matters: the second measures what we **moved**; the first measures what the user is **made to read**.*

## What did NOT change — behaviour, guards, honesty

- **All 39 runtime guards (`window._S10`) still pass**, on **Free × Basic × notes-OFF × notes-ON**, **0 console errors**. *(28 → 37 → 39: D167 adds `chatOpeningCarriesAsk` and `chatPullShort`.)*
- **CR-2 holds absolutely** — *Share for review* is **never disabled, never metered**, on any tier, in any phase, to anyone (new or not). We stopped **narrating** it, not honouring it.
- Advisory-only; issues are **never resolved by hand**; the three epistemic classes (**From OSLO** / **Confirmed by you** / **Attested by \<name\>**) are untouched.
- **Clarification → History entries are byte-identical from the panel and from chat** (one path: `_submitClarification`).
- Manual editing stays free; *"Write my own fix in \<artifact\> →"* stays.
- All D159–D161 rules stand — prototype-notes toggle **OFF by default**, no meta in product copy, explanations on demand.

## Guard change — one, and it CLOSES a loophole (it does not relax anything)

**`_assertRecommendationNeverHidden()` (DL-103 §7d) would have passed VACUOUSLY** under the new disclosure model. It checked `getComputedStyle('.ip-rec').display !== 'none'` — but under D162 the recommendation sits inside a collapsed row, and it is the **ancestor** row body that is `display:none`, not the element. The element's own computed display is still `block`, so the guard would have reported "visible" about something nobody could see. **Same failure class as the D160 export-drawer loophole.**

**Fixed two ways, both required:**
1. The guard now **also fails if `.ip-rec` sits inside a `.ip-row:not(.open)`**. *(Negative control: force the row shut at the cap → the guard fails. It is not vacuous.)*
2. **The product opens the row itself the moment the cap can bite** — `_ipRows()` defaults the Recommendations row **open** when `_capHit('fixes')`, and `applyFix()`'s cap branch forces it open **before** raising the prompt. **The binding line is intact: the recommendation is always visible; only the assisted apply is metered; manual editing is always free.**

> ⛔ **Progressive disclosure is a disclosure, not a hiding place.** Do not "simplify" either half of this back out.

---


# ⚠️⚠️ AMENDED 2026-07-12 — **D159 · D160 · D161 ARE RATIFIED. READ THIS FIRST; EVERYTHING BELOW IT IS SUBORDINATE.**

**The owner has rejected the product's over-explanation of itself.** This is a **presentation / IA pass**, not a logic pass — *no behavioural regressions, every runtime guard still passes.* Where anything below this section conflicts, **D159–D161 win.**

## The rule (D159)

> **The doctrine governs what the product may CLAIM and DO. It must NEVER govern how much the product TALKS.**
> **Obey it everywhere. Speak it almost nowhere.**

The prototypes were built under instructions to *"carry the note in-product"* and *"say it out loud."* That was an error, and the owner has named it: **the say-it-out-loud test was a constraint on BEHAVIOUR** — *don't do things you'd be embarrassed to explain* — **and it was turned into a CONTENT REQUIREMENT** — *explain everything*. They are not the same thing. Conflating them turned the app into **a museum placard about itself**.

## What changed

| # | Decision | What changed in the build |
|---|---|---|
| **D159** | **No meta in product copy.** No canon references (`DL-###`, `D###`, `CR-2`, `CHG-061`, `UP-#`, `MON-04`, `§4c`), no rationale paragraphs, no governance vocabulary, no design commentary, no owner-TBD scaffolding. **Progressive disclosure:** explanations exist **on demand**, never resident. **Bias to simplicity everywhere — modals included.** | **Every** surface swept: Reports · Plans · Limits ("Your plan") · Settings · Share · Export · Access & invites · every UP prompt · chat · the demo scaffold bar · the reviewer-view ribbon · static chrome and tooltips. **User-visible copy across the measured surfaces: 17,485 → 6,465 words (−63%).** With the toggle **off**, a grep of the rendered DOM for `DL-` · `D###` · `CR-#` · `CHG-` · `UP-` · `MON-` · `§` · "owner-TBD" · "not ratified" · "naming pending" returns **ZERO hits**. |
| **D160** | **Reports: the reading surface is SACRED.** Default view = **the document, and only the document** — full-width, centred, comfortable measure, generous whitespace. Controls move **off** the surface into a slim toolbar/drawers, **closed by default**. | The 360px composer is **gone**. `#rptDoc` is now a centred page (~74ch, 56–60px padding). A slim `#rptBar` carries **Recipient · Sections · Format · Schedule · Export**; each opens a drawer in `#rptDrawer`, **all closed by default**, `Esc` returns you to the document. **Chrome around the document: 1,533 → 124 words (−92%).** |
| **D160** | The **package wrapper** (`#rptPkg`) is **export metadata**, not reading material. | Moved into `#rptPkgHost`, shown **only on the export preview**. It is still **always rendered and always carries the disclaimer** — canon requires it, and `_assertDisclaimerOnPackageNotInBody()` still proves it on every render. |
| **D160** | **Mandatory items keep their homes.** | **Currency marker** — stays in the memo body as plain attribution (D153): *"DevNorth 2026 · plan as of 12 July · ‹PM›"*. **Disclaimer** — on the package wrapper, seen at export. **Forecast note (D155)** — appears **only when triggered**, inline, subtle, dismissible. **Never resident** (verified: 0 resident notes on the default view). |
| **D161** | **ONE global "Prototype notes" toggle — OFF by default, persisted.** Off ⇒ it looks and reads like a product. On ⇒ every owner-TBD, canon citation, retired lever, guard name and escalation is revealed for governance review. | Lives in the demo scaffold bar (`#pnToggle`, `LS: protoNotes`). **The governance content is RELOCATED, NOT DELETED.** `pn(html, kind)` returns an **empty string** when off — the strings **never reach the DOM**. This is deliberately *not* `display:none`: "hidden but present" is exactly how meta copy creeps back. Styled as a hatched, monospaced annotation layer so it **can never be mistaken for product copy**. |

## What did NOT change — and this is the point

**The behaviour.** Every guard, every limit, every honest label. **28/28 boot assertions pass on Free AND Basic, with the toggle OFF and ON, with zero console errors.**

**Honest plain-English product copy stays**, because a product would actually say it: *"Analysis is behind your edits"* · *"previous analysis"* · *"Comments never change the assessment"* · *"From OSLO" / "Confirmed by you" / "Attested by ‹name›"* · the limit disclosures · the reliability qualifier · *"Nobody is ever removed"* · *"Asking anyone for their read is always free"* · *"Every plan gets the same read."*

**The test applied to every string:** *"Would a product actually say this to a user, or is this the team explaining itself?"* If the latter → it went behind the toggle.

## Guard changes (guards fixed, doctrine never)

- **`SELLING_SURFACES` re-pointed.** `reportsBody` **was** the composer; it is now the notes rail. Left alone, every copy prohibition would have started scanning an **empty div and passing for free**. The list now covers `rptBar` · `rptDrawer` · `rptDoc` · `plansBody` · `limitsBody` · `upBody`. **Coverage did not drop.** A new `REPORT_SURFACES` list scopes the report-specific scans.
- **`_assertNoDisabledLimitAffordances()` (D138) strengthened.** With the export drawer closed, `.rpt-act .btn` is not in the DOM — a selector-only check would have passed **vacuously**. The guard now **also demands a permanently-present, never-disabled entry point** (`#rptExportBtn`) on the toolbar. **Progressive disclosure is legitimate; an unreachable limit-bearing affordance is not.**
- **`renderAccess()` bug fixed:** a local `const pn` shadowed the global `pn()` note-builder for the whole function body (TDZ `ReferenceError`). Renamed to `phEl`.

## Verification

`node --check` PASS · jsdom without `runScripts` → 31 body children · **28/28 boot assertions on Free × Basic × notes-off × notes-on, 0 console errors** · rendered-DOM meta grep: **0 hits off / 672 hits on** · **negative controls fail correctly** (remove the toolbar Export → `d138` fails; gate editing on Free → `rptEditFree` fails; strip the package disclaimer → `rptDiscOnPackage` fails).

---

---

# ⚠️⚠️ AMENDED 2026-07-12 — **DL-103 IS RATIFIED. READ THIS FIRST; EVERYTHING BELOW IT IS SUBORDINATE.**

**`00_owner/decisions/records/DL-103-analysis-cost-basis-and-tier-rederivation.md` (Ratified, Class A)** supersedes the tier model this slice was built on. **Nothing prior to this section is deleted** — per the house rule, the correction must stay legible — but where it conflicts with what follows, **DL-103 wins.**

## The ten changes

| # | DL-103 | What changed in the build |
|---|---|---|
| **§1** | **NEVER TIER JUDGMENT QUALITY. (Doctrinal.)** One judgment bar for every tier. Routing is chosen **by step** (cheap models to extract; the best available to judge) — **never by tier**. **Supersedes "Basic sells capacity; *Pro adds model quality*"** (§4c T3 · DL-074 §4 · the monetization backlog). | The `ROUTING` per-tier ladder is **struck** → `MODEL_ROUTING_BY_STEP`. Every "Pro is where the models get better" / "the quality upsell" line is gone. Pro = **execution & programme support**. Guard: `_assertNoTierKeyedModelQuality()`. |
| **§2/§4** | **The cost basis is stale; the §4c numeric basis is SUSPENDED.** §4c/DL-074 derive every governor and price from **rented frontier pricing** — which **DL-069 had already abandoned** (local Gemma/Llama runtime) *one day earlier*. Tier numbers are **re-derived, not tuned**. | Every former "RATIFIED" number sourced from §4c's cost model now renders **PENDING RE-DERIVATION**. `BASIC_PRICE = 12` is kept **with a pending-basis marker** — the value is owner-confirmed; its **basis** is not. |
| **§3** | **E1–E3 commissioned** — prompt caching · incremental/scoped recompute · evidence coalescing. **~6.4× cheaper. No model change. No quality trade.** *(E3 is load-bearing for CR-2: without it, collaborators silently consume the user's budget, users learn that asking for evidence costs them their own read — **and stop asking**.)* | Surfaced honestly in UP-6, the budget meter and chat: *"the limit you are looking at is an artifact of an engine we are fixing, not an economic fact."* Illustrative arithmetic (~12 today → ~74 with E1+E2) is shown **labelled as illustrative**. |
| **§5** | **The re-derived ladder.** Free = the full core read on **one** plan + viral primitives; analyses are an **abuse ceiling, not a product limit**. Basic = **capacity/scope** (3 projects · bigger plans · connected sources · reporting · export/sync). Pro = **execution & programme support**. Team = **collaboration, per seat**. Enterprise = **portfolio + governance**. | `renderPlans()` rebuilt end-to-end on this ladder. |
| **§6** | **ONE honest limit, in the user's currency: ANALYSES per month — never tokens.** **UP-1 (daily fix cap) and UP-2 (daily chat cap) are RETIRED.** Daily caps are demoted to **invisible rate-limits (burst-smoothers)**. **UP-6 becomes the primary prompt.** | The token governor is gone. `ANALYSIS_BUDGET = {free:null, basic:null}` → **pending; nothing is enforced.** **Chat is uncapped, on every tier** (the gate in `sendChat` is deleted). UP-5 also goes as a product limit; UP-6 carries its honest *"keep the last analysis"* resolution. Guards: `_assertBudgetInAnalyses()`, `_assertChatNeverCapped()`. |
| **§7d** | **The daily ASSISTED-APPLY cap is RETAINED** — it is *labour*, not comprehension. **BINDING: the recommendation is ALWAYS visible · only the assisted apply is metered · MANUAL EDITING IS ALWAYS FREE.** Placement: **above activation, below power use.** **Number from Alpha instrumentation, NEVER a cost model → until the data exists, NO CAP.** | New prompt **UP-APPLY** (⚠️ no canon UP-number — escalated). `ASSISTED_APPLY_CAP.free = null` ⇒ **inactive**. First resolution is a real one-click action: **"Make the edit yourself — free, right now."** Basic gets **"Apply all recommended fixes."** Guard: `_assertRecommendationNeverHidden()`. |
| **§7d-bis** | **Refresh cadence.** The coalescing window keys off **the user's journey (new ▸ established), NEVER the tier.** The tier lever is **auto vs manual refresh**. **BINDING: "Update now" is FREE ON EVERY TIER.** | `updateNow()` — **no tier check exists, and none may be added.** Free: slow auto + Update now. Basic: continuous. Refresh still draws on the monthly budget. Guard: `_assertUpdateNowFreeOnEveryTier()`. |
| **§7c** | **LABOUR, NOT LATENCY. The priority-queue / speed lever is STRUCK.** An async product cannot sell speed. Fast Pass never queues — **a product guarantee (DL-046), not a lever**. **Artificial delay PROHIBITED.** | **Not built.** Said out loud on the Plans page (*"the upgrade we deliberately did not build"*). Guard: `_assertNoPriorityQueueLever()`. |
| **§7e** | **Reverse trial** (~14 days on Basic, then Free) — **a GA-phase mechanic**. **BINDING: TAKE BACK THE PIPES, NEVER THE READ.** | Gated behind `PHASE==='ga'` (DL-102: in Alpha everyone is invited). Downgrade takes **integrations · reports · continuous auto-refresh · extra live plans**; it **never** takes **History · issues · artifacts · chat · the read**. Guard: `_assertDowngradeNeverRemovesUnderstanding()`. |
| **§7h** | **OUTCOME-BASED PRICING IS PROHIBITED.** OSLO deliberately refuses to predict delivery outcomes. | Nothing prices or markets on outcomes. Said out loud on Plans. Guard: `_assertNoOutcomePricingCopy()`. |
| **§7j** → **D148–D154** | **REPORTING (M4) is the #1 lever — and it is a STATUS lever.** ⚠️ **REBUILT 2026-07-12 (owner-directed): D148–D154 REVISE D144/D146/D147.** Reports is a **WORKSPACE, not a modal**. The report is an **executive summary written for its reader, in their language** — **ZERO OSLO vocabulary in the body**. **Editing is FREE on every tier; the gate is REUSE** (Basic remembers your wording). | **Reports = a peer view** (`#pane-reports`, `showView('reports')`) — left the **live composer** (`#reportsBody`), right the **package wrapper** (`#rptPkg`) and the **document** (`#rptDoc`). **Seven sections, fixed order** (summary · what changed · **risks BEFORE assumptions** · assumptions · plan of action · decisions · appendix). **Two altitudes on every risk** — *for the plan* (deliverable) and *for the goal* (**does the plan, AS WRITTEN, still reach its intent — a structural claim, never a forecast**). **The plan of action is the PM's, in the first person** — OSLO seeds, the PM owns. **The disclaimer is on the PACKAGE, not in the prose**; the **currency marker stays in the body** as plain attribution. **Tailor the ask, never the read** (D145 stands): only §6 changes with the recipient. Guards: `_assertNoOsloVocabularyInReport()` · `_assertNoForecastLanguageInReport()` · `_assertReportStructure()` · `_assertPlanOfActionIsPMVoiced()` · `_assertDisclaimerOnPackageNotInBody()` · `_assertEditFreeOnEveryTier()` · `_assertAskTailoredNeverTheRead()` · `_assertReportPackagesNeverProduces()` · `_assertScheduledReportRechecksCurrency()` · `_assertReportsNoHealthFraming()`. |

## §7f — the in-plan conversion moments, surfaced honestly (not sprung)

connected sources (day 1, forever) · reporting (weekly) · export/sync → execution tool (DL-083, Basic) · bulk actions · **one live understanding** (archived = read-only, the read freezes; **archive stays reversible**, DL-058) · the **envelope** (UP-4 partial read, honestly disclosed) · **plan 2** (a pull, not a wall).

## §7g — upgrade mechanics

**One click. Self-serve. Price on the button. No sales call, ever.** Triggers on **behaviour and realized value, never a calendar** — the value-moment class (UP-7/UP-8) is **strengthened**, not an afterthought.

## The guardrails that did NOT move

Never meter the epistemic record (artifacts uncapped · History never expires) · never sell safety · **CR-2** (evidence-seeking never bounded) · **D124** (never present a PHASE limit as a TIER upsell) · no eviction on downgrade · no fabricated scarcity · **MON-04** (no upgrade wallpaper) · every prompt names the specific limit **and** the specific tier · **no invented numbers — unset renders unset** · advisory-only (chat never mutates).


> **⚠️ AMENDED 2026-07-11 — the tier values were ratified all along, in the engineering zone**
> (`30_engineering/environment/RELEASE_1_CALIBRATION_DEFAULTS_V1 §4c`). Rows **6**, **7**, **17** and **20** below
> described behaviour built on the belief that the price, the Basic allowances, the envelope and the monthly gate
> were undecided. **They were not.** The superseded text is struck through and kept — the history is the lesson.
> See `tier-definitions-census.md` for the full correction. New rows **21–25** cover the corrected behaviour.

## The ones that decide whether this product is honest

| # | Case | Behaviour | Why |
|---|---|---|---|
| 1 | **A cap is hit before the first MRI** | **No prompt fires at all.** The gate still holds (the attempt is refused), but no upsell appears. | MON-04 global guard: never before first value. Selling before you have delivered anything is the worst version of this product. |
| 2 | **A cap is hit while an analysis is running** | No prompt. It fires (if still applicable) once the pass completes. | Never interrupt a Fast/Deep pass. |
| 3 | **A user is at the deep-run cap and a reviewer responds** | The Extended Analysis **runs immediately**, free, counted in the **evidence lane**. | **CR-2 / D126.** Metering this would degrade the user's understanding *on purpose* to sell an upgrade. |
| 4 | **A user has 0 invites AND is at the seat cap** | Both are named, **separately**, in the same dialog — the **phase** message carries **no upgrade CTA**; the **tier** message does. | **D124.** Presenting a supply constraint as an upsell is a dark pattern. |
| 5 | **A user downgrades Basic→Free with 10 collaborators** | **Nobody is removed.** They simply cannot *add* another Collaborator. | **D132.** Evicting humans to enforce a billing change is disqualifying on a trust product. |
| 6 | **The monthly budget gate fires** | ~~The prompt appears, names Basic, and **enforces nothing** — the threshold is unset and it says so.~~ **AMENDED:** the gate is **real and enforced**. The threshold is the **monthly token governor** — Free **4M** · Basic **10M** (§4c: *"the binding governor"*). UP-6 shows the real number and the real reset date. | The threshold **was** decided — in `30_engineering`. Rendering a ratified limit as "unset" is the same failure as inventing one, pointed the other way. |
| 7 | **A project exceeds the envelope** | ~~A **partial** analysis + an honest disclosure, on **one** surface, with the size **unset**.~~ **AMENDED:** the disclosure now names the **ratified** envelope — Free **~20 docs / ~50k words**, Basic **~40 / ~100k** (§4b CHG-056 · §4c). Still **one** surface. The project is **never rejected**; it degrades gracefully. | **D139** — epistemic honesty first. The disclosure would ship even if Basic did not exist. A **precise** honest limit is a better disclosure than a vague one. |
| 8 | **The user is at the fix cap and applies a fix anyway** | The button works; the **attempt** is gated; **the plan is untouched**; UP-1 explains. | **D138** — the control is never the thing that says no. |
| 9 | **The user is at the deep-run cap and applies a fix** | The **fix is applied and saved**; only the **re-read** defers; History records the deferral; the read stays at **last-good** and is labelled as such. | **D128 P1** — a plan edit is the epistemic record and is never metered. Only the *run* costs money. |
| 10 | **The user hits the chat cap mid-sentence** | The typed question **stays in the composer**. | A limit may never destroy something the user wrote. |
| 11 | **A Viewer is added when every seat is full** | It **works**. Viewers are unlimited on every tier. | **X-1.** Read-only spread is pure upside. |
| 12 | **Several caps are hit on the same day** | The **global per-day prompt cap** silences the extras. | MON-04. The guard is canon; the number is unset and the build errs toward **silence**. |
| 13 | **A prompt is dismissed** | It does not return today (per-trigger cooldown). UP-3 alone has **no cooldown** — it is high-intent and canon says immediate. | Ratified cadence. |
| 14 | **The clock crosses midnight** | Daily meters reset to 0 on the next read; the reset time was always shown as a real time. | Calendar-day reset (D136). |
| 15 | **A user asks chat to upgrade them** | Chat **refuses, explains, and hands back** to the plan screen. | **D001 advisory-only.** An assistant that can spend your money is not an advisor. |
| 16 | **An artifact count or History length grows large** | Nothing happens. No cap, no trim, no expiry, no tier check. | **D128 P1.** |
| 17 | **A user on Basic exceeds the Basic envelope (~40 docs / ~100k words)** | The partial disclosure still fires — **the honesty is tier-independent**. It names Pro's and Team's envelopes as the forward ladder and **offers no Buy button** (neither is purchasable in R1). | D139. Pro/Team are **shown and priced** (DL-074 §4), and **not for sale** in R1. |
| 18 | **Someone adds a tier check to an evidence path** | `_assertEvidenceNeverGated()` **fails loudly in the console**, naming CR-2. | The prohibition is enforced by code, not by a comment. |
| 19 | **Someone re-adds a `disabled` on a limit affordance** | `_assertNoDisabledLimitAffordances()` **fails loudly**, naming D138. | Same. |
| 20 | **Someone fills in an unset number "just for now"** | `_assertNoFabricatedNumbers()` **fails loudly**, naming the Anti-Assumption protocol and the "Basic = 10 projects" incident by name. **AMENDED:** the same assertion now also fails on the **mirror error** — a **ratified** value rendering as *unset*. | This is the failure mode this slice exists to prevent — **in both directions.** Crying "unset" over a number the owner decided months ago is the same lie, told backwards, and it teaches every future reader that the number does not exist. |

## AMENDED 2026-07-11 — the corrected cases

| # | Case | Behaviour | Why |
|---|---|---|---|
| 21 | **⚠️ THE BIG ONE — a reviewer's evidence arrives *after* the monthly governor has gated** | **The evidence is RECORDED — immediately, unconditionally, never refused.** The Attested response lands in History before any budget check is reached. **What defers is the *run*:** OSLO says, in the user's own words about their own reviewer — *"Priya's answer is recorded. Your read will update when your monthly analysis budget resets, or on upgrade."* The Overview keeps showing the **last completed analysis**, labelled as exactly that. | **CANON HAS NOT DECIDED THIS.** CR-2 (evidence never metered) · CRR-04 (every response triggers a run) · §4c (the rollup gates AI spend) collide. The build implements **record → defer → disclose** and **labels it a recommendation, in-product**. It is the only resolution in which all three ratified constraints survive. **Escalated, not invented.** |
| 22 | **A Free user reaches the monthly governor and wants more analysis** | **There is nothing to buy.** OSLO discloses the limit honestly and offers **one** path: upgrade. **No top-up, no credit pack, no per-analysis purchase.** | **DL-074 §3 (ratified):** overage is *paid tiers only*; *"Free converts via upgrade… no Free purchase path."* `_assertNoFreePurchasePath()` fails loudly if anyone ever wires one. |
| 23 | **A *paid* user reaches the monthly governor** | They may allow **metered overage** — **per Extended Analysis**, against a **spend cap they set**, with **threshold alerts** before they reach it. Never automatic, never silent. | **DL-074 §5:** visible meter · user-set spend cap · alerts — *"no silent overspend, no bill shock."* Overage is an **explicit, priced relaxation** of the gate, not a bypass of it. |
| 24 | **A user asks what their seat limit is** | OSLO gives the number **and says it is not settled** — *"recommendation — not ratified"*, on the meter, on the plans card, in Settings, and in chat. It is enforced provisionally, and it is **never** the reason OSLO asks anyone to buy anything. | **§4c has no seat row for Free/Basic/Pro.** It is the **one** undefined dimension in the ladder. A product that enforces a number it invented while calling it a rule is lying — even when the number is reasonable. |
| 25 | **Someone marks the seat caps RATIFIED in code, or invents a replacement number** | `_assertSeatCapsFlagged()` **fails loudly**, naming §4c, the Team per-seat price, and the cannibalisation risk. | Basic = 10 seats at $12/mo **guts a ~$99–149/seat Team**. The number is the owner's; **the constraint is structural.** |
| 26 | **A Basic user expects a better answer than Free** | OSLO says, on the plans card, that **Basic runs the same models as Free** — *"Basic sells capacity, not a better answer"* (§4c). Model quality is the **Pro** step, and OSLO names it rather than blurring it. | §4c: Basic *"differentiates on capacity, not model quality"*. Implying the paid tier **thinks better** when it runs identical routing is how software lies about itself. |
| 27 | **A user hits the daily deep cap but is nowhere near the monthly governor** | UP-5 fires, names the **daily** cap — and says plainly that the daily number is a **burst-smoother**, and the **monthly budget is the real ceiling**. Both are shown, honestly, in Usage & limits. | §4c: Basic's 6/day is a *"burst ceiling (**not the governor**)"*. Naming the wrong limit as *the* limit is a small dishonesty that compounds. |


---

# ⬛ EDGE CASES ADDED / CHANGED BY DL-103 (2026-07-12)

## The ones that decide whether this product is honest — *round two*

| # | Edge case | The wrong behaviour | What the build does |
|---|---|---|---|
| **E-DL103-1** | **The user hits the assisted-apply cap while an issue is open.** | The recommendation greys out / collapses / is replaced by an upsell. **This is the single most dangerous failure in the slice** — it converts a *labour* meter into an *understanding* meter, and it is **PROHIBITED (D126/D128).** | The issue stays open. **The recommendation stays fully visible.** "Apply this fix" **stays enabled** (D138). The attempt fires **UP-APPLY**, whose **first** resolution is a real one-click action: *"Make the edit yourself — free, right now."* The analysis still runs on the manual edit. `_assertRecommendationNeverHidden()` fails loudly if this ever regresses. |
| **E-DL103-2** | **The user asks OSLO a 200th question in a day.** | A chat cap fires; the composer disables; an upsell appears. | **Nothing happens. Chat is not capped, on any tier.** A message does not even count against the monthly budget. *"Never meter who gets an answer"* (D126). |
| **E-DL103-3** | **The monthly analyses run out on Free.** | *"You've used 4,000,000 of 4,000,000 tokens."* | The limit is **in analyses**, and its number is **pending re-derivation** — **so nothing is enforced at all**, and the product **says why**: *"the limit you are looking at is an artifact of an engine we are fixing, not an economic fact."* No token figure is ever rendered as a limit. |
| **E-DL103-4** | **A Free user's read is stale and they need it now.** | *"Upgrade to refresh."* — **this would gate the currency of understanding.** | **"Update now" is free, on every tier, always.** No tier check exists in `updateNow()`. What Basic buys is **not having to ask** (continuous auto-refresh). The run still draws on the monthly budget — the legitimate cost control. |
| **E-DL103-5** | **A Free user opens the Readout.** *(⚠️ REWRITTEN AGAIN under D148–D154 — no modal, no spine.)* | Everything is locked; the surface is an ad. | **The whole memo — all seven sections — is theirs, free, forever, fully editable, as a PDF** (CHG-061: the seed is never gated; it must be able to travel into an exec's inbox). The **extras, branding and scheduling** are visible and their controls **still work** — the *attempt* fires `UP-REPORT`, which sells **persistence** and says out loud that **the editing itself is free**. **The seed is not gated; the depth is — and the words are never gated at all.** |
| **E-DL103-6** | **A report lands in front of the user's CEO.** | A line reads *"78% likely to deliver on time"* / a RAG chip appears / a completeness figure is rounded up. | **A report that could be mistaken for a health rating is a DEFECT.** Confidence = **understanding maturity**. Every claim carries its basis; *derived* is never dressed as *attested*; the currency marker and the disclaimer are always on; **if OSLO only read part of the plan, the report says so.** `_assertReportsNoHealthFraming()` walks the rendered DOM. **The user is staking their career on this — overclaiming detonates in *their* face.** |
| **E-DL103-7** | **Generating a report while the read is stale.** | The product quietly runs an analysis first, so the report "looks current". | **A report packages; it never produces.** `genReport()` spends **no meter**, touches **no governor**, and moves the read by **zero degrees**. The snapshot is **dated to the last completed analysis** and labelled *"previous analysis"* on the wrapper and the snapshot card; **in the body it appears as plain English** — *"the plan has moved on since this was put together, so read it as where things stood then."* `_assertReportPackagesNeverProduces()` reads the source of **every** report path (including the memo builders and the edit path) and fails if a run is ever wired in. |
| **E-DL103-8** | **A trial user downgrades to Free (GA).** | History truncates · issues vanish · chat locks · the read is held hostage. | **Take back the pipes, never the read.** Downgrade takes **integrations · reports · continuous auto-refresh · extra live plans**. It **never** takes **History · issues · artifacts · chat · the read**. `_assertDowngradeNeverRemovesUnderstanding()`. And **nobody is ever evicted** (D132). |
| **E-DL103-9** | **A reviewer answers after the monthly budget is reached.** | The evidence is refused, or silently dropped. | **Unchanged and still absolute (CR-2):** the evidence is **recorded immediately and unconditionally**; the **run** defers, **disclosed honestly**. ⚠️ And DL-103 §3 names the real fix: **E3 (evidence coalescing)** — without it, collaborators silently consume the user's budget, **users learn that asking for evidence costs them their own read, and stop asking.** |
| **E-DL103-10** | **Someone "helpfully" reinstates a daily fix or chat cap.** | It looks like a tuning change. | It is a **supersession of DL-103**, not a tuning change. `_assertNoFabricatedNumbers()` now asserts the RETIRED rows **stay retired** and the PENDING rows **stay pending**, and fails loudly by name. |
| **E-DL103-11** | **Someone adds "Basic runs it now" / a priority queue.** | It is the most obvious lever in an AI product. | **It is struck.** Latency only bites when the user is blocked — and OSLO is built so they never are. The one moment it *would* bite is the one §7b forbids monetizing. `_assertNoPriorityQueueLever()`. **Artificial delay is prohibited.** |
| **E-DL103-12** | **Someone writes "Basic gives you a more accurate read."** | Plausible marketing copy. | **You would never say it out loud — that is the tell (§7b).** `_assertNoTierKeyedModelQuality()` walks the plans/reports/limits/prompt DOM and fails on any *affirmative* tier-quality claim (the scanner is denial-aware, so the doctrine may state the prohibition by naming it). |

---

## REPORTING (M4) — the edge cases that decide whether this feature is honest

| # | Edge case | The wrong behaviour | What the build does |
|---|---|---|---|
| **E-M4-1** | **The PM sends the same read to their sponsor and to their eng lead.** | The product "helpfully" **re-frames the assessment for each audience** — softer for the sponsor, harder for engineering. | ⛔ **THIS IS SPIN, AND IT IS THE ONE WAY THIS FEATURE COULD TURN ON THE PRODUCT.** It is the *"make me look good by shading the truth"* failure, and it would **destroy the PM's credibility in front of the exact people they are trying to impress.** **§4 (the ask) is theirs. §1–§3 (the read, the limiter, the unknowns) are IDENTICAL for every audience.** `_spineRead()` **cannot see who is receiving the report** — no argument, no composer state — and `_assertAskTailoredNeverTheRead()` renders the read for **every** recipient at boot and **byte-compares** it. **One honest read. Many asks.** |
| **E-M4-2** | **Someone drops §5 to make the memo "tighter for the exec."** | It is the most tempting edit on the surface: §5 is the hedging, and hedging reads as weakness. | **A report without §5 is NOT shippable.** `_assertReadoutSpineComplete()` walks the *rendered* readout and fails loudly if any of §1–§5 is missing or out of order. §5 is what makes every claim above it **defensible when someone pushes back in the room** — which is the entire reason the PM can put this in front of leadership **under their own name**. Dropping it does not make the memo tighter; it makes it **indefensible**. |
| **E-M4-3** | **A reader mistakes the readout for a health rating.** | *"Confidence 58/100"* is read as *"58% likely to succeed."* A RAG chip appears. A readiness score is inferred. | **A report a reader could mistake for a health rating is a P1 DEFECT (DL-104 §5)** — not a copy nit. §1 states, in the same breath as the number, that it is **understanding maturity** and explicitly **not** health, **not** readiness, **not** a probability of success. `_assertReportsNoHealthFraming()` walks the rendered DOM for the banned vocabulary; `REPORT_BANNED_NAMES` fails the render if the artifact is ever *named* for health, readiness — or called a **"status report."** |
| **E-M4-4** | **A weekly schedule keeps mailing a confident-looking read to the sponsor after the plan has moved on.** | The automation ships a stale read **as current**. Nobody notices for a month. | ⛔ **This is the precise, career-grade failure scheduling introduces.** The read is not *wrong* — it is **out of date**, and presented as current it is **a lie the PM did not know they told.** `runScheduledReport()` **re-checks currency at send time**, and a stale package **goes out labelled "previous analysis" and says so on its face.** It does **not** run an analysis to freshen itself (that would make a report *produce* understanding). `_assertScheduledReportRechecksCurrency()`. |
| **E-M4-5** | **OSLO only read part of an oversized plan, and the PM exports a readout.** | The memo reads as a complete assessment. The exec assumes the whole plan was reviewed. | **No fabricated completeness. Ever.** With the envelope exceeded, **§1 says so first**: *"OSLO read only part of this plan… there may be issues in the unread part, and OSLO cannot tell you what they are."* The disclosure travels **inside the artifact**, not just on the Overview the exec never sees. |
| **E-M4-6** | **The readout dresses a derived claim as an attested one.** | *"Confirmed: the venue supports 500 concurrent users."* — when nobody confirmed anything. | **Derived is never dressed as attested.** §3 names, by artifact, exactly what is still **From OSLO** and what is **Confirmed by you**; a stakeholder's input is **Attested by them**, and it is evidence *that they hold that view* — never OSLO's verdict (D115/D133). **The three classes are never blurred**, and the readout says so at its foot. |
| **E-M4-7** | **A PM wants "just the issues" — a data dump.** | The product obliges: 12 issues, no framing. The PM looks like a **clerk**. | **The spine is not optional.** §2 gives the limiting dimension **and the specific reason** — prioritisation *with a reason*, never a list. **The difference between strategic and clerical is selection and framing, not volume.** *Artifact detail* exists as an **optional** section for a working audience — but it is a section of the memo, never the memo itself. |

---

# ⬛ EDGE CASES — REPORTING (M4), **D148–D154 · REBUILT** (2026-07-12)

| # | Edge case | The wrong behaviour | What the build does |
|---|---|---|---|
| **E-M4-1** | **The sponsor reads the memo and asks "what's a CAF?"** | The report has taught them OSLO's internals instead of telling them about their event. **This is exactly why the last build was rejected.** | **There is not one word of OSLO's vocabulary in the body.** `_assertNoOsloVocabularyInReport()` sweeps the rendered document against `REPORT_OSLO_VOCAB` on every render, word-boundary matched, **with no denial exemption** — the memo may not even *deny* doctrine, because naming it is already speaking it. **The doctrine governs what it may CLAIM. It never governs how it SOUNDS.** |
| **E-M4-2** | **A number in the memo is not as solid as it looks.** | The report states it flat, or hedges it into meaninglessness, or explains OSLO's reliability model. | **It is qualified in the sentence.** *"We are planning for 450+ people on it at once against a 500-device figure that **came from our own plan, not from The Grid**."* The reader now knows exactly how far to trust it, **and no doctrine was spoken.** That is derived-vs-attested, rendered as English. |
| **E-M4-3** | **The PM wants to elevate a risk by what it means for the goal.** | The report says *"this project is unlikely to hit its targets"* — **a prediction, which doctrine forbids and which is the same P1 class as a health rating.** | **Frame BY outcome; never FORECAST the outcome.** *"As written, the plan rests that on a network nobody has confirmed… what the plan delivers is a day of talks, which is a different event from the one we sold."* **A structural claim about the plan**, not a prediction about the world. `_assertNoForecastLanguageInReport()` fails the build on *likely · probability · forecast · on track · will succeed…* |
| **E-M4-4** | **The plan-of-action section reads like OSLO wrote it.** | *"OSLO recommends confirming the venue's capacity."* → **the PM becomes a passenger in their own report**, and the sponsor thinks *"the tool wrote this."* | **OSLO seeds; the PM owns.** First person, always: *"I'm getting the network commitment out of The Grid in writing this week… I'm driving all of these. Back to you Friday."* `_assertPlanOfActionIsPMVoiced()` requires the first person, **forbids the word OSLO in that section**, and requires an edit affordance — **a seed the PM cannot rewrite is not a seed; it is the tool writing the report.** |
| **E-M4-5** | **A Free user wants to fix a sentence OSLO wrote badly.** | *"Editing the readout comes with Basic."* → **the PM is being asked to sign words they cannot correct**, in the one artifact where their name is on the line. | ⛔ **PROHIBITED (D154).** **Editing is free — every section, every week, on every tier, from scratch.** `_reportEditAllowed()` contains **no tier check**, and `_assertEditFreeOnEveryTier()` reads the edit path's own source and fails if a tier check or an upgrade prompt is ever wired in. **The gate is REUSE:** Basic is what makes the wording come *back*. |
| **E-M4-6** | **A Free user edits the report, then comes back next week.** | The edits are silently kept (the gate quietly rots), **or** the loss is sprung on them without warning. | **Nothing was ever persisted, and the surface said so before they typed** — *"On Free, nothing is kept. Next week the report comes back seeded and you write it again."* The at-limit prompt then sells **persistence**, never the right to their own words, and its **first resolutions are free**: rewrite it now, export it now. |
| **E-M4-7** | **A Basic user's saved wording meets a plan that has moved on.** | The persisted memo is re-sent as though the read behind it were still current. | **Basic remembers your WORDS. It never remembers a stale read.** `simNextWeek()` re-applies `RPT_EDITS` and **re-derives every OSLO-authored section from the CURRENT plan.** A stale read is labelled **"previous analysis"** on the wrapper, whatever the wording says. |
| **E-M4-8** | **The PM types OSLO vocabulary into a section they rewrote.** | The build fails, and OSLO polices the user's own prose. | **The PM's words are the PM's.** An edited section carries `data-pm="1"` and is **exempt** from the vocabulary and forecast guards. OSLO grades **its own** writing, never the user's — anything else would be the tool writing the report again (**advisory-only**). ✅ **CLOSED by D155 — see E-M4-13.** |
| **E-M4-9** | **Canon requires every package to carry an explicit disclaimer.** | A *"this is not a forecast"* paragraph is pasted into the memo — **which invites the reader to wonder whether it was trying to be one.** | **The disclaimer is a property of the PACKAGE, not the prose** (D153). It rides on the **PDF cover / share-link metadata** (`#rptPkg`, `snap.cover.disclaimer`) — **canon satisfied, writing saved.** The real protection is that **the memo never makes a forecast claim**. `_assertDisclaimerOnPackageNotInBody()` checks **both halves**: on the wrapper, absent from the body. |
| **E-M4-10** | **The recipient changes.** | The summary is softened for the board and sharpened for the ops lead. **That is spin — and spin in front of the exact people you are trying to impress is what would end the PM's credibility.** | **Only section 6 changes.** §1–§5 and the appendix are **byte-identical for every recipient**, proven behaviourally *and* structurally (the read-builders cannot so much as **see** the recipient). **One honest read. Many asks.** |
| **E-M4-11** | **Nothing has changed in the plan since last week.** | Section 2 invents movement so the report looks busy. | *"**Nothing in the plan has moved since 5 July.** This is the first of these notes, so there is no previous week to hold it against."* **No fabricated completeness, and no fabricated activity.** |
| **E-M4-12** | **Someone reinstates the modal, or the "How to read this" section.** | It looks like a UI preference. | It is a **supersession of D148/D149**, not a preference. The modal (`#reportsScrim`), the spine and `_assertReadoutSpineComplete()` are **deleted**, and `_assertReportStructure()` fails on any section list other than the seven. |

---

# ⬛ EDGE CASES — **D155 · D156 · D157 · D158** (owner: accepted, 2026-07-12)

| # | Edge case | The wrong behaviour | What the build does |
|---|---|---|---|
| **E-M4-13** | **The PM types *"we're 80% likely to hit 450"* into their own summary — and it ships under OSLO's mark, on OSLO's cover, carrying OSLO's disclaimer.** | **Two wrong answers, and both are prohibited.** (a) **BLOCK the send** — the tool overrules the human, which violates advisory-only (**D001**) and makes OSLO the editor of words that go out under the PM's name. (b) **SAY NOTHING** — OSLO lends its name to a claim it forbids itself. **A forecast wearing OSLO's credibility.** | **The gentle note (D155) — the only honest position left.** When PM-authored prose (`data-pm="1"`) contains the **same** forecast/probability/RAG/health vocabulary the guards apply to OSLO's own writing (`REPORT_ADVISORY_WORDS = REPORT_FORECAST_WORDS + REPORT_BANNED_FRAMINGS`), OSLO surfaces an **unobtrusive advisory beside that section**: *"Heads up — this reads as a forecast. OSLO doesn't predict outcomes, and this goes out under OSLO's mark."* **Same vocabulary, opposite treatment: a FAILURE in OSLO's words, an ADVISORY in the PM's.** |
| **E-M4-14** | **Somebody turns the note into a gate** — "just a confirmation dialog before send". | The advisory becomes a block. **The tool has decided.** | ⛔ **IT NEVER BLOCKS.** `_assertForecastNoteNeverBlocks()` proves it **three ways**: (a) **the send path cannot SEE the note** — `genReport()` / `runScheduledReport()` may not reference `_pmForecastHits` / `RPT_NOTE_DISMISSED` / `_rptForecastNote` at all (a path that cannot see it cannot be gated on it); (b) the note's renderer cannot disable a control, fire an upgrade prompt or interpose a `confirm()`; (c) **behaviourally** — whenever a note is on screen, every send/export control is still enabled. |
| **E-M4-15** | **OSLO "helpfully" softens the PM's sentence.** | The prose is hedged, rephrased or auto-corrected. **The PM becomes a passenger in their own report** (D152) and the sponsor thinks *"the tool wrote this."* | ⛔ **OSLO NEVER REWRITES.** `_assertOsloNeverRewritesPMProse()` proves it two ways: **structurally** — only `saveReportSection()` (the PM typed it) and `resetReportSection()` (the PM asked for the seed back) may write an entry of `RPT_EDITS`; **behaviourally** — every rendered `data-pm="1"` section contains its stored text **verbatim, byte for byte**. The note sits **beside** the words; it never reaches into them. |
| **E-M4-16** | **The PM means it. They *want* to say it.** | The note nags forever, or reappears on every render, or is counted/reported/sold. | **It is DISMISSIBLE — always.** `dismissForecastNote(k)` and the PM sends anyway. Dismissal is per-section, in-memory, never metered and never a monetization surface (**never sell safety**). **Re-editing that section brings the note back** — new words, fresh advice. The note attaches to the sentence, not to the person. |
| **E-M4-17** | **A PM types "probability of success" into their own summary — and the D153 guard turns the console red.** | OSLO grades the human's words. **The exact failure D155 exists to prevent, arriving through a side door.** | **Fixed in this pass.** `_assertDisclaimerOnPackageNotInBody()` scanned the **whole** document body, so PM prose could trip it. It now **exempts `data-pm="1"` sections**, consistent with D149/D151/D152. **OSLO's paragraphs are graded. The PM's are advised.** |
| **E-M4-18** | **Somebody deletes the `To:` line "for D145 compliance."** | A memo with no recipient. It is not a memo; it is a document that arrived from nowhere. | **The `To:` line STAYS (D156).** D145 forbids **re-framing the assessment** by audience — it does **not** forbid **addressing** the document. The guard is and stays **section-scoped**: §1–§4 (+ §5 + appendix) are **byte-identical across every recipient**; **only §6 varies**. Recorded in a code comment at the render site *and* at the guard, so nobody "fixes" it later. |
| **E-M4-19** | **The report runs long.** | The build invents a truncation rule — "drop the oldest", "top 3 only", "collapse below the fold" — and it silently becomes canon. | **Selection is the value; the cut is spec'd, not invented (D157).** Risks are capped at **5, highest impact first** (`MEMO_RISK_CAP`, explicitly labelled **illustrative, not a ratified product value**), and the **appendix walks every workstream and is explicitly skippable** — so nothing is hidden. ⬜ **WHAT GETS CUT AND WHO DECIDES is an OPEN ITEM for the M4 spec.** Carried in `open-items.md` and surfaced in-product. **The build does not decide it.** |
| **E-M4-20** | **A Basic user opens the app and gets a red console at boot.** | `_assertNoGenericUpgradeCopy()` returns **false** on `TIER==='basic'`. | **DEFECT — FIXED (D158).** Root cause: **UP-6 only names "Basic" in its Free branch** (correctly — on Basic the honest copy is *"wait for the reset, or allow metered overage"*), while the guard demanded the tier name **unconditionally**. **The guard was checking the wrong condition.** MON-04 requires a prompt to name **the tier that RELIEVES the limit** — meaningful **only when the user is beneath it**. Fixed: `_beneathTier(p.tier)` gates the name requirement. **The limit name and the tier field stay required unconditionally.** ⚠️ **This is a fix to the GUARD, not a relaxation of MON-04** — below the relieving tier every friction prompt must still name it. |

---

# D159 / D160 / D161 — new edge cases (2026-07-12)

| # | Edge case | Expected behaviour | Verified |
|---|---|---|---|
| **E-PN-1** | User has never touched the toggle. | **Prototype notes are OFF.** `LS.get('protoNotes', false)`. The app reads as a product. No canon, no owner-TBD, no guard names anywhere in the DOM. | ✅ 0 meta hits |
| **E-PN-2** | User turns the toggle ON, then reloads. | The setting **persists** (`LS: protoNotes`). Every surface repaints with its annotation layer. | ✅ |
| **E-PN-3** | Toggle flipped while a surface is already painted. | `_pnRepaint()` re-runs every render function that BUILDS copy (Reports, Plans, Limits, Meters, Share, Access, Partial, Settings-collab, Tier chrome, Deps, History, Notif prefs). Static markup is served by `_pnSlots()`; tooltips by `_pnTitles()`. **No stale mixed state.** | ✅ |
| **E-PN-4** | Toggle OFF — someone greps the rendered DOM for governance strings. | **ZERO hits.** `pn()` returns `''` when off, so the strings never reach the DOM. This is **not** `display:none` — "hidden but present" is how meta copy creeps back. Annotations are **never** stored in `data-*` attributes for the same reason. | ✅ |
| **E-PN-5** | Toggle ON — the reviewer needs everything. | **672 governance strings reappear**, in one distinctly-styled annotation layer (hatched, monospaced, `PROTOTYPE NOTE` / `OWNER-TBD` / `CANON CITATION` / `RUNTIME GUARD` / `ESCALATION` headers). It **cannot be mistaken for product copy**. | ✅ |
| **E-RPT-D160-1** | User opens Reports. | **The document, and only the document.** No recipient picker, no section toggles, no options panel, no package wrapper, no advisory chrome. A slim toolbar; every drawer closed. | ✅ |
| **E-RPT-D160-2** | User needs to change the recipient / sections / format / schedule / export. | All five are **one click away** on the toolbar. Each opens a drawer; **one at a time**; `Esc` closes it and returns you to the document. | ✅ |
| **E-RPT-D160-3** | User is reading, and wants to know what the document travels on. | The package wrapper is **not on the reading surface**. It appears on the **export preview** (`Export ▾`), carrying the mark, the currency marker and **the disclaimer**. Canon (*every package carries an explicit disclaimer*) is satisfied — and still proven at runtime. | ✅ |
| **E-RPT-D160-4** | The PM types a forecast into their own prose. | The gentle note (D155) appears **beside that section, only then**, subtle and dismissible. It **never blocks, never edits, never refuses to send.** It is **not** a resident panel on the composer any more — it never should have been. | ✅ 0 resident |
| **E-RPT-D160-5** | Export drawer is closed — does D138 still hold? | **Yes, and the guard was strengthened to prove it.** With the drawer shut, `.rpt-act .btn` is not in the DOM, so a selector-only check would pass **vacuously**. `_assertNoDisabledLimitAffordances()` now also demands a **permanently-present, never-disabled** `#rptExportBtn` on the toolbar. **A drawer is a disclosure, not a hiding place.** | ✅ negative control fails correctly |
| **E-META-1** | A limit-reached prompt fires with notes OFF. | It still **names the specific limit** and — when the user is beneath it — **the specific tier that relieves it** (MON-04), and still offers its resolutions, **free one first**. What went is the paragraph explaining *why* we chose that lever. | ✅ |
| **E-META-2** | The seat cap is hit with notes OFF. | It is still **enforced provisionally**, still **never the reason to buy**, still **never evicts anyone**, and is still held as a `RECOMMENDATION` in config (`_assertSeatCapsFlagged()` fails loudly if anyone marks it RATIFIED). The **words** *"recommendation — not ratified"* moved behind the toggle: that is governance vocabulary, not product copy. | ✅ |
| **E-META-3** | The monthly analysis budget is unset with notes OFF. | **Nothing is enforced.** `_assertNoFabricatedNumbers()` still fails loudly if anyone fills the number in. The user sees *"Nothing is being enforced. OSLO will not invent a number to meter you with."* The **suspended cost basis, the §4 escalation and the illustrative arithmetic** moved behind the toggle. | ✅ |

---

# D162 — Issue Panel disclosure · edge cases (2026-07-12)

| # | Case | Behaviour | Verified |
|---|---|---|---|
| **E-D162-1** | User opens an issue that has a **clarification**. | The panel opens **collapsed**: the read, one primary action, and four rows. **No textarea is on screen.** The Clarification row names what OSLO needs (question preview, truncated). | ✅ `open=false`, 0 visible textareas |
| **E-D162-2** | The issue has **no recommendation** but OSLO **is** asking. | The single primary action becomes **"Answer"** — it opens the Clarification row and focuses the input. One click, not two. | ✅ |
| **E-D162-3** | User expands **two rows**, then selects a resolution path (which re-renders the panel). | Both rows stay open — row state persists **while the panel is open**. Posting a comment likewise leaves the Comments row open and re-focuses the box. | ✅ |
| **E-D162-4** | User closes the panel and re-opens the same issue. | **Fresh, minimal read** — all rows collapsed again. Disclosure state is per-open, not sticky forever. | ✅ |
| **E-D162-5** | Keyboard-only user tabs to a row. | The row header is a real `<button>`: **Enter/Space toggle**, focus ring is visible, `aria-expanded` flips `false`→`true`. No custom key handling to get wrong. | ✅ |
| **E-D162-6** | **The assisted-apply cap is hit while the Recommendations row is collapsed.** | ⚠️ **The dangerous one.** The row is **forced open before the prompt is raised**, so the recommendation is on screen, in full, at the exact moment OSLO declines to type it for you. `_assertRecommendationNeverHidden()` now **also fails if `.ip-rec` sits inside a closed row** — the old computed-style check would have passed vacuously. | ✅ + negative control fails correctly |
| **E-D162-7** | A user with a fully-spent invite allocation, on Free, in Alpha, opens any issue. | **"Share for review" is present, enabled, and unmetered.** No counter, no caption, no reassurance — **CR-2 is obeyed, not announced.** | ✅ `disabled=false` |
| **E-D162-8** | An issue has **review responses** (approve / reject / alternative). | Responses are kept **in full, forever**, with **Attested by \<name\>**. The honest line is said **once** — *"Evidence, not a verdict. ISS-03 is still Open."* — and the contract (never resolves · never re-opens · OSLO never self-accepts · Approve and Reject weigh the same) is on the **ⓘ**. | ✅ |
| **E-D162-9** | An issue is **resolved**. | Recommendations, Clarification and the share action are gone; the resolved banner is **one line** (*"✓ Resolved by the analysis update."*), not a paragraph about how confidence was refined. | ✅ |
| **E-D162-10** | User hovers a weakness annotation in an artifact. | The flyout is a **peek**: severity · dimension · the read (truncated) · **Open issue →** · **Ask about this →**. **No "Share for review →"** firing a modal out of a hover. | ✅ |

---

# D163 + D164 — edge cases (2026-07-12)

| # | Case | Behaviour | Verified |
|---|---|---|---|
| **E-D163-1** | A limit is hit and the prompt fires. | The prompt does **exactly three things**: the eyebrow **names the limit hit**, the body **names the tier that relieves it**, the buttons **offer the resolutions (the free one first)**. **≤30 words, whole dialog.** No essay, no rationale, no "what we will never do". | ✅ every prompt measured (26 · 25 · 22 · 27 · 25 · 28 · 30 · 27) |
| **E-D163-2** | A **Basic** user hits the monthly analyses limit. | The prompt does **not** try to sell them Basic (you cannot sell someone the tier they are standing on). It offers **wait for the reset** and **overage against a spend cap you set**. Still ≤30 words. | ✅ 27w |
| **E-D163-3** | The analysis budget gates a re-read after an edit. | The prompt adds **one** honest clause — *"Saved — only the re-read waits."* — and stays inside budget. | ✅ 29w |
| **E-D163-4** | An oversized project arrives (UP-4). | **ONE notice**, not two: *"OSLO read only part of your plan. The figures below cover only that part. Free reads ~20 documents / ~50k words; Basic reads ~40 documents / ~100k words."* **Not one honest fact was cut** — what was cut is the *explanation* ("your project was not rejected"; "the disclosure is not here to sell you anything"). | ✅ 30w |
| **E-D163-5** | A stranger is chosen as a reviewer in the share-for-review dialog. | **The load-bearing facts survive, as short labels:** *costs nothing · no invite · no seat · they see this issue and nothing else · the grant ends when the issue resolves, or in 14 days.* **Cutting one of those would be a CR-2 honesty cut and is prohibited.** What is cut is the paragraph explaining *why* OSLO never gates the seed of the loop. | ✅ |
| **E-D163-6** | The user opens the Share dialog. | Keeps: *a new person costs an invite* · the balance · the seat count and how many are filled · *Viewers take no seat* · **"Asking for a read is free — no invite, no seat."** Drops: the *why*, and *"nothing you produce here is metered — on every plan"* (owner-reassurance). | ✅ 60w, at budget |
| **E-D163-7** | Any toast fires. | **≤12 words.** A toast confirms what happened; it is not a place to teach doctrine. Multi-branch toasts are within budget on **every** branch. | ✅ 26 toast paths exercised, 0 over |
| **E-D163-8** | The user hovers any ⓘ or control. | **≤20 words.** A 50-word tooltip is a paragraph wearing a hover. | ✅ 0 tooltips over budget (was 16) |
| **E-D163-9** | The prototype-notes toggle is **ON**. | Every deleted rationale, canon citation, guard name and escalation is **there, in full** — relocated, never deleted. **OFF is the product; ON is the governance review.** | ✅ 4-quadrant boot: 31/31 assertions, 0 console errors |
| **E-D164-1** | The PM **clicks anywhere in the readout and types.** | **It just works.** There is **no Edit button, no edit mode, no Save/Cancel row and no "Editing" chip.** `#rptEd` is **one always-live contenteditable** holding every section — the same model as `#artdoc`. ⚠️ **This is what was missing:** the first pass shipped a per-section *enter → edit → close* mode. **You do not enter edit mode on a document.** | ✅ `_assertReadoutIsOneContinuousDocument()` + NC `oneDocument_editModeReturns` |
| **E-D164-2** | The PM selects text and presses **⌘B** (or uses the selection toolbar). | The **same** floating selection toolbar as an artifact, driving `#rptEd` through `_edDoc()`. The format applies, undo is pushed, the draft is marked dirty. **No analysis runs, no meter moves, no provenance chip appears.** | ✅ governor unchanged |
| **E-D164-3** | The PM presses **⌘Z / ⌘⇧Z**, or **"/"**, or **⌘F**. | Undo/redo, the slash menu and find/replace all work — **the same code, the same bar.** The four actions also sit in the **readout toolbar** (D160: chrome off the reading surface), calling `artUndo` · `artRedo` · `_insertBlockFromButton` · `openFind`. **Undo/redo is now ONE stack for the whole document** (`_edKey() === 'rpt'`), not one per section. | ✅ |
| **E-D164-4** | The PM types **"/"** inside the **decisions table** (§6). | The slash menu is **suppressed inside a table cell — exactly as it is in an artifact.** ⚠️ **THIS WAS THE SEAM.** `_syncSlashFromInput()` read `elx.closest('#artdoc td')`, so in a **readout** table the suppression never fired: the *same keystroke behaved differently in the two documents*. That is "the feature exists, but not here", and it is what D164 exists to delete. Now `_edSel('td')`. | ✅ NC `noArtdocHardcode` bites |
| **E-D164-5** | The PM's section contains **bold**, a **list**, or the **decisions table**. | It **survives editing intact.** The **table row + column controls** (add/insert/delete row, column ops, row drag-reorder) are **attached in the readout too** — adding a row to a memo table is **editing**, not asserting a plan fact. **What stays artifact-only is the PROVENANCE**: the row dot and the cell chip (D149 — no OSLO vocabulary in the memo). | ✅ closes O-D164-1 |
| **E-D164-6** | The PM inserts a block with **"/"** or a markdown shortcut. | The block lands **inside the section the caret is in**, never at the document root (a block outside every `[data-sec]` is invisible to the D149/D150/D155 section-scoped guards). ⚠️ **Caught in verification:** `_attestNewBlock()` was stamping **"Confirmed by you"** onto every block the PM inserted into their memo — **OSLO vocabulary in the one document that forbids it.** Now gated on `_edIsArtifact()`. | ✅ + guard entry added |
| **E-D164-7** | The PM types into a section, changing OSLO's wording. | On a **debounced commit** the section is compared to OSLO's live seed; **because it diverged**, `data-pm="1"` is set and the text is stored — **in place, without re-rendering the document**, so the caret never moves. **Ownership is a property of the TEXT, not of a mode the user was in.** | ✅ |
| **E-D164-8** | The PM types a section **back to OSLO's exact wording**. | It **stops being theirs**: `data-pm` is removed, the "your words" badge and the Reset affordance disappear, and the D149/D151 guards start policing that section again. Ownership tracks the text, in both directions. | ✅ |
| **E-D164-9** | The PM edits a section, then re-renders it (changes recipient · opens a drawer · switches tier). | The section renders **BYTE-VERBATIM.** `_assertOsloNeverRewritesPMProse()` proves it on every render — and it now proves it by **BYTE EQUALITY on the extracted body**, not by a substring scan. ⚠️ The old substring check was doubly wrong: it only ever caught *deletion*, and once the editor started attaching **block grips** to the live document it would have gone red on a correct document. **Fix the guard, never the doctrine** (D166 §3). | ✅ NC `osloNeverRewritesPMProse_rendered` bites |
| **E-D164-10** | The PM has an **unsaved keystroke in flight** when the readout re-renders. | `renderReports()` **flushes the PM's in-flight keystroke first** (gated on `_rptEdPending`, which only a keystroke sets). **Discarding a word the PM typed is OSLO destroying their prose by omission** — the same sin as rewriting it. ⛔ The gate is load-bearing: a *programmatic* change to `RPT_EDITS` (Reset · Next week · migration) can never be reverted by a stale DOM read. | ✅ |
| **E-D164-11** | The PM writes a **forecast** into their own section. | The **gentle note** fires — beside the words, never inside them. It is **`contenteditable="false"`**, so it lives *inside* the live document without ever becoming *part* of it, and `_rptSecBody()` strips it before a single character is stored. It **never blocks**, never edits, never refuses to send, always dismissible. **Dismissing updates in place — it does not re-render**, because the PM may be mid-sentence. | ✅ NC `forecastNoteNeverBlocks` bites |
| **E-D164-12** | The PM edits the readout, then opens a **plan artifact**. | The editor host **follows the VIEW** (`_edSyncHost()` in `showView`) — there is no mode to leave any more. Leaving the Readout **always** restores `#artdoc`. **If the host were ever not given back, the artifact editor would be driving a document the user is not looking at.** | ✅ NC `hostFollowsTheView` bites |
| **E-D164-13** | The user is on **Free** and edits the readout. | **It works.** Every section, every week, from scratch. **The gate is REUSE, not EDIT** — on Free nothing persists; on Basic the words come back, applied. **No tier check exists anywhere on the edit path** (`onRptInput` → `_rptEdDirty` → `_rptCommit`). | ✅ NC `editFreeOnEveryTier` bites |
| **E-D164-14** | An editor structural op fires on the readout (slash insert · block move · paste · undo · format). | It funnels through `_commitFromStructuralEdit()`, which **returns early on the readout host** and marks the draft dirty. **No artifact version. No History event. NO ANALYSIS.** A readout packages; it never produces. | ✅ NC `editorProducesNothing` bites |
| **E-D164-15** | A block grip / find highlight / table control is in the DOM when the commit fires. | `_rptCleanHTML()` strips the chrome (and **unwraps** `.find-hit`, never removes it — it wraps the PM's own words). ⚠️ **Defect fixed:** the old strip list said `.blkgrip`; the class is **`blk-grip`** — it matched **nothing**, so every block grip would have been stored into the PM's saved text. | ✅ |
| **E-D164-16** | A Basic account had readout edits persisted by the **pre-D164 (markdown) build**. | `_rptMigrate()` inflates them to HTML once, on load. **Nobody's words are lost by the editor change.** | ✅ |
| **E-D164-17** | ⚠️ **The readout LOOKS like a preview, even though it is an editor.** *(Owner, 2026-07-12: "it is contained in a grey box with padded margins left and right, while the artifact starts at top-left of the space.")* | ⛔ **DEFECT — FIXED (D164 visual parity).** The readout was **drawn as a PAPER SHEET**: `.memo` was a card (surface fill · 1px border · 6px radius · `34px 40px` padding · a 12px drop shadow · **a literal `background:#fff` in a dark-default app, D127**) floating inside a **centring** `.rw-page` flex box at its own `74ch` measure. `#artdoc` is **none of those things** — it sits flush at the top-left of `.aw-center` at 720px. **A card says PREVIEW.** The PM does not preview a readout; **they write it.** Now: `.rw-page` = `padding:24px 34px 90px` (the `.aw-center` treatment), `.memo` = `background:none; border:0; radius:0; box-shadow:none; padding:0; margin:0; max-width:720px`. **CSS only — no logic, no DOM, no copy.** | ✅ `_assertReadoutIsFlushLikeAnArtifact()` + NC `flush_cardReturns` · `flush_lightCardReturns` · `flush_cardPaddingReturns` · `flush_centringReturns` |
| **E-D164-18** | ⚠️ **The SAME editor renders two different-looking documents — and every DOM guard still passes.** | ⛔ **The subtlest half of the same defect.** `.memo p{font-size:13px;color:var(--muted)}` sat at the **same specificity** as `.doc p` but **later in the sheet**, so it beat the artifact editor's typography **on source order alone**. The markup was byte-identical; `#rptEd` genuinely carried `.doc`; every structural D164 guard was green — **and the two documents still looked nothing alike.** **A DOM guard can never see this.** All `.memo`/`.m-doc` re-declarations on `p · li · ul · ol · table · th · td` are **deleted**; `.doc` governs the readout body outright. **What a memo IS is its furniture** — title · byline · currency marker · `To:` line · seven fixed sections · `.m-risk`/`.m-alt`/`.m-app`/`.m-sign` — **not a smaller font.** | ✅ `_assertReadoutIsFlushLikeAnArtifact()` reads the **CASCADE** + NC `flush_typographyDrifts` bites |
| **E-D164-19** | Someone "tidies up" by giving the readout its own width, or re-centres it "just on mobile". | **The parity guard is DERIVED, not hardcoded.** It never asserts *"720px"* — it asserts **`.memo`'s measure === `.doc`'s measure** and **`.rw-page`'s padding === `.aw-center`'s padding**, read from the authored cascade. Change the artifact's metrics and **the readout is required to follow**; the guard cannot go stale. Centring is forbidden **at any width** — the `@media` blocks are parsed too. | ✅ NC `flush_measureDivorcedFromDoc` + `flush_centringReturns` (injected inside `@media`) |
| **E-D164-20** | The parity guard itself goes **vacuous** — `.doc` is renamed, or loses its `max-width`, and there is nothing left to compare against. | **It must say CANNOT VERIFY, not pass by default** (D166 §1 — *a guard that grades nothing is worse than no guard*). The guard fails loudly, naming the missing reference, if `.memo` · `.rw-page` · `.doc` · `.aw-center` is absent from the cascade, if fewer than 200 rules parse, or if `.doc` / `.aw-center` declare no measure / no padding. ⚠️ **Comment-stripping is load-bearing:** the code comments now quote the removed `background:#fff` as a record of what went — a guard that read them would **fail on its own documentation**. | ✅ NC `flush_refusesToGradeNothing` bites |
| **E-D164-21** | The card is gone — **does the package wrapper go with it?** | ⛔ **NO. `#rptPkgHost` / `#rptPkg` are UNTOUCHED.** The wrapper is **export metadata**: **always rendered**, **always carrying the ratified disclaimer** (D153), and **shown only on the export preview** — verified: `.open` is `false` at rest, `true` on the export drawer, `false` again on close, and `#rptPkg` is **never** a descendant of `#rptDoc`. *A cover is something the document travels ON, not something you read THROUGH.* The parity guard asserts this too, so removing the card can never quietly fold the cover into the page. | ✅ `_assertDisclaimerOnPackageNotInBody()` + parity guard check (f) |
| **E-D164-22** | The white card is removed — **does contrast survive in BOTH themes?** | **Yes, and dark improves.** The document now sits on `--bg` in both (dark `#111315`, light `#FBFAF7`) instead of a `--surface` / `#fff` card. **Dark gets MORE contrast** (body 15.05:1 → **16.92:1**; subtle 5.34:1 → **6.01:1**). **Light still clears AA everywhere**: body prose **15.87:1** · table cells **7.69:1** · byline / table headers / block grips **5.01:1** · *your words* badge **6.02:1** — every token above the 4.5:1 bar (3.0:1 for the 21px title). **Dark remains the default (D127), and there is no longer a white sheet inside it.** | ✅ light + dark AA sweep, 15 tokens, 0 failures |
| **E-FIX-1** | ⚠️ **The assisted-apply cap becomes hit while an issue panel is ALREADY open.** | ⛔ **A REAL, PRE-EXISTING DEFECT, caught by the regression harness.** `_ipRows()` decided the Recommendations row's state **only on the panel's first open** — so if the cap landed *while the panel was open*, the recommendation stayed **collapsed**. That is metering **understanding**, and it is **PROHIBITED** (DL-103 §7d · D126 · D128). **Fixed:** the row is now **forced open for as long as the cap is hit**, `ipToggleRow()` refuses to collapse it while capped, and the demo trigger re-renders an open panel. | ✅ + negative control |

---

# D165 — OSLO Chat as a conversation · edge cases (2026-07-12)

| # | Edge case | Behaviour | Proven by |
|---|---|---|---|
| **E-D165-1** | The user asks about an issue whose `why` is a **single long sentence** (ISS-05, 24 words). | The opening measures **41 words**. The budget is **50** (D167), and the guard grades **every issue in the model**, not just ISS-01. | `chatOpeningShort` |
| **E-D165-2** | The issue is **Resolved**. | The opening collapses further — one line, one action (`See it on the timeline →`), a 2-chip handoff. No recommendation, no options: there is nothing to decide. | behavioural |
| **E-D165-3** | The issue is **Addressed** (acted on, awaiting the analysis update). | The opening adds exactly **one clause** — *"You've acted on it — it's Addressed, waiting on an analysis update."* Still inside the budget. | `chatOpeningShort` |
| **E-D165-4** | The evidence is **not thin** (attested basis, or reliability above Low). | The epistemic line **does not fire**. It is honest, not decorative — it appears only when the basis is derived-only **and** reliability is thin. **The trigger is unchanged from D109.** | `_thinBasis()` |
| **E-D165-5** | The user asks the **same context twice** (`askOslo` on ISS-01, then ISS-01 again). | **No second divider.** `_chatDivider` is keyed on `_pinKey(ctx)` — a divider marks a **change** of context, not every message. | behavioural |
| **E-D165-6** | The user asks a **pull question with no issue in context** ("What are my options?" from a cold thread). | It resolves against **`S.top`** — the most consequential open issue. It never fabricates a subject, and it never falls back into the old wall. | `_pullId` |
| **E-D165-7** | A **pre-D165 thread** is restored from localStorage (it contains `.ca-cons` subtitles and open textareas). | It renders sanely: `.ca-cons{display:none}` hides the stale subtitles, and `_chatRestore()` **stands down every past answer box** (all buttons disabled, not just the first — under D165e the first button is the collapse head). | `_chatRestore()` |
| **E-D165-8** | The user answers a clarification **in chat** rather than in the panel. | `answerClarificationFromChat` → `_submitClarification(id, val, 'chat')` — the **same** function the panel calls. The History entry is **byte-identical** (verified by diffing both paths). | `chatClarSamePath` |
| **E-D165-9** | The same clarification is raised **twice** in the thread. | `_retireClarBoxes()` stands down the earlier box — id removed, textarea disabled, **every** button disabled. Exactly one live answer box per issue, so the newest answer can never silently fail. | `_retireClarBoxes()` |
| **E-D165-10** | The user asks OSLO to **apply the fix / close the issue / select a path / upgrade them**. | OSLO states the boundary in **one sentence** and hands back. **Computing the reply mutates nothing** — proven by snapshotting `_istatus`, `_selpath`, the governor, the read, the artifact bases and History across a 12-question battery. | `chatNeverMutates` |
| **E-D165-11** | `prefers-reduced-motion` is set. | No thinking state, no streaming — the (now much shorter) reply renders instantly. The handoff chips and the single action are present immediately. | `_reduceMotion()` |
| **E-D165-12** | The chat rail is **not yet seeded** (the user is still on the splash / intake). | The D165c guard does **not** pass vacuously. Beyond the live-DOM check it runs a **mechanism proof**, driving the real `renderChatChips()` through both the underway and empty states. **A guard that grades an absent surface is the failure that already shipped three times.** | `chatOneChipSet` |
| **E-D165-13** | The user asks a tier question ("What does my plan include?"). | It is **also** a conversation now: a short answer, one action, and pull chips — **"What does Basic add?"** · **"What's never limited?"** — each its own turn. **D126 still holds: OSLO names the exact limit it hit.** | `_s10Reply()` |
| **E-D165-14** | A "shortening" pass **deletes** the evidence or the reliability basis instead of moving it. | The build **fails at boot**. `chatDetailPullable` asserts each pull turn comes back **non-empty**, and that the reliability turn still states **Coverage · Evidence · How assessable** as labelled pairs. *(Its own first draft was vacuous here — it matched the bare word "Moderate", which the qualifier sentence already contains. The negative control caught it.)* | `chatDetailPullable` |


---

# D167 — the chat O-D closures · edge cases (2026-07-12)

| # | Edge case | Behaviour | Proven by |
|---|---|---|---|
| **E-D167-1** | An issue carries an **outstanding clarification** (ISS-01, ISS-02). | The opening turn carries the **collapsed request**: `❓ <question, truncated to 10 words>… ▸`. Body `display:none`. Click → expands to the **full question** (`.cc-q`) plus the textarea. **The chip stays too.** | `chatOpeningCarriesAsk` · `chatClarCollapsed` |
| **E-D167-2** | The clarification question is **long** (ISS-01 = 19 words). | The **head is truncated to 10 words**; the **full question is never lost** — it is the first line inside the body, and it is also the `title=` tooltip. **Truncation is presentation; the ask is intact.** | `chatOpeningCarriesAsk` (asserts the body has a textarea behind the head) |
| **E-D167-3** | The issue is **Addressed** — the user already answered. | **The request is NOT re-surfaced.** The ask is no longer outstanding, and re-prompting for an answer already given is nagging, not surfacing. (The chip still routes there.) | `_ansIssue` (`st !== 'addressed'`) |
| **E-D167-4** | The issue is **Resolved**. | Early return — one line, one action, a 2-chip handoff. **No request.** | behavioural |
| **E-D167-5** | ⚠️ **A guard GENERATES an opening turn — and the opening turn now retires the live answer box for that issue.** | ⛔ **A real hazard the D167 change introduced.** `_assertChatOpeningIsShort` renders the opening for **every issue at boot** — which would have called `_retireClarBoxes()` and **disabled a box the user was about to type into**. **Fixed:** generation-with-side-effects is fenced behind `_CHAT_PROBE`; while a guard is probing, `_retireClarBoxes()` is a no-op. The HTML produced is byte-identical. | `chatNeverMutates` — which now snapshots **`_CHAT_MSGS` and the thread's innerHTML** too |
| **E-D167-6** | The **prototype-notes toggle is ON** and the user asks a tier question. | `pn(...)` renders **above** the handoff chips (`chat-acts → pn → chat-follow`). **The chips end the turn.** | behavioural (verified with notes ON) |
| **E-D167-7** | ⚠️ **The notes toggle is ON and a word-budget guard runs.** | ⛔ **CAUGHT BY THIS PASS.** `_probeWords` stripped `.pnote` — **a class that does not exist** (the real one is `.pn`) — so with notes **ON** a 36-word tier answer measured **234 words** and the budget guard fired **on the governance rail**. **Prototype notes are review apparatus (D161), never product copy, and no guard may grade them as such.** Fixed by one helper, `_productText()`. | `chatPullShort` on **notes-ON** |

---

# D166 — THE GUARD AUDIT · what the negative controls found (2026-07-12)

**53 negative controls run against 39 guards. Four guards were VACUOUS. All four are fixed; all 53 controls now bite.**
**The doctrine did not move. Only the guards did.**

| # | Guard | The vacuity | The fix |
|---|---|---|---|
| **V-1** | `d138` — *no limit-bearing affordance is disabled* | ⛔ **The exemption swallowed the subject.** The test was `el.disabled && !el.dataset.validation` — and **`#shareInviteBtn` carries `data-validation="email"` STATICALLY IN THE MARKUP**, so that expression was **always false for it**. **A seat cap could have disabled the single most likely control in the product (UP-SEAT) and D138 would have reported green forever.** | The exemption is now conditioned on the **LIVE INPUT STATE** (`_validationExcusesDisable()`): validation may disable Send **only while the form is actually invalid.** A **valid** address with a dead button can only mean a limit — and that is what D138 forbids. An **unknown** `data-validation` value now excuses **nothing** (otherwise the attribute is a magic word that switches the guard off). |
| **V-2** | `noteNeverBlocks` — *the forecast note never blocks the send* | ⛔ **The export-drawer trap, reintroduced one guard later.** The behavioural half filtered `.rpt-act .btn` for disabled controls — but under **D160 those live in a drawer that is CLOSED BY DEFAULT**, so the list was **empty** and the filter passed for free. **The note could have disabled every send control in the product.** | It now grades the **permanent toolbar entry point** (`#rptExportBtn`) as well as any open drawer controls — **and FAILS if it finds nothing to grade.** A send path you cannot find is not a send path you have verified. |
| **V-3** | `budgetInAnalyses` — *the budget is in analyses, never tokens* | ⛔ **`#meterBox` does not exist until the Limits surface is rendered.** At boot the DOM half found nothing and **passed for free** — a token figure could have sat in `renderMeters()` for months. | Now a **mechanism proof**: it renders the **real** meters through the **real** `renderMeters()` into a **detached host** and grades the output. It cannot be dodged by never opening the surface, and it **fails if the meter renders empty**. |
| **V-4** | `_copyViolations` — *the shared copy scanner* (feeds `noTierQuality` · `noLatencyLever` · `noOutcomePricing` · `reportsNoHealth`) | ⛔ **Silent coverage rot.** `if(!el) return;` — a **missing** surface was skipped **without a word**. Rename one div and it stops being scanned; rename all seven and **every copy prohibition in the file passes on an empty set, forever.** Not hypothetical: **D160 renamed the Reports surfaces and the list had to be chased by hand.** | `_copyScan()` now returns the surfaces it **could not find**, and every caller **FAILS** on a missing one. A surface may legitimately be **EMPTY** (a closed drawer). It may never be **ABSENT**. |

### Latent vacuities closed in the same pass (they had a subject *today*, but the failure mode was live)
- **Six report guards** (`reportsNoHealth` · `rptStructure` · `rptNoOsloWords` · `rptNoForecast` · `rptPlanIsPMs` · `rptDiscOnPackage` · and the DOM half of `neverRewritesPM`) opened with `if(!doc || !doc.innerHTML.trim()) return true;` — **"I could not find the document, so everything is fine."** That is the exact sentence that let the export drawer ship broken. **A missing subject is a failure to verify, not a pass.** They now route through `_rptDocOrFail()` and **return `false`**.
- **`_probeWords` stripped `.pnote`, a class that does not exist.** See **E-D167-7**.

### The one control that must NOT bite (a false-positive check)
- **`d138c`** — the Invite button disabled while the address field is **empty**. That is **form validation, not a limit**, and the guard correctly **does not fire**. A guard that fired here would be forcing the product to ship a Send button that sends nothing.

---

# D168 — REPORT vs MEMO · edge cases (2026-07-12)

**Two objects, one lifecycle. You edit a REPORT; what travels is a MEMO.** Everything below is guarded by a
**mechanism or state proof** (D166) and ships with a **negative control that bites**.

| # | Case | Behaviour | Guard |
|---|---|---|---|
| **E-D168-1** | The PM opens the Reports workspace. | They see a **REPORT**: flush at the top-left, no card, no shadow, `.doc` typography, continuously editable. **D164 artifact parity is intact — nothing regressed.** The card is nowhere on the reading surface. | ✅ `_assertReadoutIsFlushLikeAnArtifact()` (derives from `.doc` / `.aw-center`; now also pins `#rptDoc` to `.report`) + NC `report_wearsTheMemosClass` |
| **E-D168-2** | The PM opens the **Export** drawer. | The **package** appears — cover · mark · **disclaimer** (D153) · currency chip — and inside it **the MEMO on paper**: the card, the reading measure, its own quieter voice. The identity line reads **"Memo preview — what travels"**. It is what will land, byte for byte (same factory, `_mkMemo`). | ✅ `_assertMemoIsPaper()` + `_assertDisclaimerOnPackageNotInBody()` |
| **E-D168-3** | The PM exports. | The report **becomes a memo**: dated, deep-frozen, named *Memo 1*, listed under **Sent**, and shown on the preview as **"Memo sent · <date> · PDF"**. History: *"Memo sent — a dated snapshot (PDF)"*. **No analysis runs. No meter moves. The confidence index does not change.** | ✅ `_assertReportPackagesNeverProduces()` (now covers `_mkMemo` · `_memoBodyHTML` · `_renderMemo` · `_memoOnScreen` · `viewMemo`) |
| **E-D168-4** | The PM **edits the report after sending**, and a **new analysis lands**. | **The memo does not move.** Byte-identical (`JSON.stringify`), and it still renders the words it was cut with — not the words the report now holds. What changes is its **label**: the Sent row and the memo flip to **"previous analysis"**. **A fact is not edited; it is qualified.** | ✅ **`_d168StateProof()`** — cut · edit · re-analyse · byte-compare. NC `memoNeverMovesWhenTheReportDoes` |
| **E-D168-5** | Something tries to write a sent memo. | It **cannot.** `_deepFreeze()` freezes the memo **and its cover** at creation, so `memo.body = 'x'` and `memo.cover.disclaimer = 'x'` are both no-ops. A **shallow** `Object.freeze` would have left the cover writable — and the guard catches exactly that. | ✅ `_assertMemoIsImmutable()` + NC `immutable_shallowFreezeOnly` |
| **E-D168-6** | A render path tries to push into `REPORT_SNAPSHOTS[]`. | It **may not.** Only `genReport()` and `runScheduledReport()` may create a memo. `renderReports()` runs on every keystroke commit — if it could reach the register, a memo could be "refreshed" behind the PM's back. The guard reads the **source** of every render/commit path. | ✅ NC `immutable_renderPathWritesMemos` |
| **E-D168-7** | The **13px `--muted` body voice** — deleted under D164, escalated by the worker. | **The escalation was right, and it is closed: that voice belongs to the MEMO.** It is reinstated on **`.memo .m-body`** and it is **guarded** — delete it and `_assertMemoIsPaper()` goes red. It **cannot leak back onto the report**: the memo body carries **no `.doc`**, `.report` re-declares no typography, and any selector touching both is a failure. | ✅ NC `memo_voiceDeleted` · `memo_voiceEqualsEditorVoice` · `memo_leaksOntoTheReport` |
| **E-D168-8** | The app is in **dark** (the default, D127) and the memo is on screen. | The card is drawn from **tokens** (`var(--surface)` · `var(--border-2)`), with an explicit `:root[data-theme="light"] .memo` rule for light. **The old `.memo{background:#fff}` — a white sheet floating in a dark app — is a guard failure, not a style choice.** AA passes on **both** surfaces in **both** themes (min ratio measured: **5.01:1**, report byline in light). | ✅ NC `memo_whiteCardInDarkApp` |
| **E-D168-9** | The PM's **gentle note** (D155) is showing when they export. | It **does not travel.** `_rptCleanHTML()` strips the note, the "your words" badge, the Reset affordance, the block grips and every `contenteditable` before a single byte is frozen. **The memo carries the document, never the chrome.** | ✅ `_assertReportAndMemoAreNotConfused()` (mechanism half) |
| **E-D168-10** | Someone calls the live document a **memo** — in a label, a toast, or OSLO's own seeded prose. | **Guard red.** And the reverse: the memo's identity line may never say *report*/*readout*, and `_mkMemo()` may never name a memo *Snapshot N* or *Report N* (read from **source** — a closed drawer must not make the check vacuous, which is exactly how the export-drawer guard went vacuous). | ✅ NC `naming_liveDocumentCalledAMemo` · `naming_sentArtifactCalledAReport` · `naming_memosNamedSnapshots` |
| **E-D168-11** | ⚠️ **The PM types the word "memo" into their OWN section** — *"I'll send a memo to legal about this."* | **Nothing happens. It is their prose, and OSLO does not correct it** (D152/D155). The naming guard **exempts `data-pm="1"` sections by design** — the first draft of it did not, and it went red on the true sentence *"the report the sponsors are owed"*. **The guard was fixed; the doctrine was not** (D166 §3). | ✅ **NC `naming_pmMayWriteTheWordMemo`** — a control that proves the guard **does not** bite |
| **E-D168-12** | The PM clicks a **sent memo** in the Sent list. | It opens **that memo**, rendered from **its own frozen bytes** — never re-derived from the current report or the current read. Clicking it again returns to the live preview. It is `contenteditable="false"`: **a memo has left OSLO, and you do not edit it.** | ✅ `_assertMemoIsPaper()` (structure half) + NC `memo_becomesEditable` |

---

# D169 — HISTORY OPENS THE SENT MEMO · edge cases (2026-07-12) · closes **O-D168-2**

> *"What did I actually tell them in June?"* is a question a PM **will** ask. OSLO already holds the answer —
> **frozen and byte-exact.** A sent memo is the most auditable artifact in the product: it went to the board,
> **under the PM's name, on a date.**
>
> ⛔⛔ **THE LOAD-BEARING RULE: the memo is shown AS IT WAS SENT — never re-rendered from current understanding.**
> A re-render would look *perfectly plausible on screen* and would **silently rewrite history**: June's memo
> showing July's read, under June's date, over the PM's signature. It is therefore caught by **mechanism**, not by
> looking — the open path is proven **incapable of reaching the live composer**, and the state proof counts the
> composer's invocations and requires **zero**.

| # | Case | Behaviour | Guard |
|---|---|---|---|
| **E-D169-1** | The PM scrolls History and finds *"Memo sent — a dated snapshot (PDF)"*. | The row is **clickable and keyboard-operable** (`role="button"`, `tabindex="0"`, Enter/Space), and carries **"open the memo →"**. It opens the memo on its package. | ✅ `_assertHistoryOpensTheFrozenMemo()` (wiring half) + NC `d169_historyRowNoLongerOpensTheMemo` |
| **E-D169-2** | ⛔ **The report has been rewritten AND a new analysis has landed since that memo was sent.** The PM opens it. | **They read exactly what they sent.** The memo is **byte-identical** to the moment it was cut (`JSON.stringify`), it shows the June words and **not** the July words, and the surface is handed the **frozen register entry itself** — not a copy, not a rebuild. | ✅ **`_d169StateProof()`** — cut · move the report · move the read · open from the History event · byte-compare + **identity** + **composer-invocation count = 0** |
| **E-D169-3** | ⛔ Someone wires the open path to the live composer ("just re-render it from the current report"). | **Guard red.** The open path may call **exactly one thing**: `_memoById()` → `_rptMemoView`. `_mkMemo` / `_memoBodyHTML` / `_rptCleanHTML` / `genReport` are **forbidden on it, read from its own source**. | ✅ **NC `d169_openPathReRendersFromCurrentUnderstanding`** — the load-bearing control |
| **E-D169-4** | The memo arrives — **on the cover it travelled on.** | The package shows **its** mark, the **ratified disclaimer** (D153), and the currency marker of **the run it was cut from** — not today's run. The **label** moves (**"previous analysis"** once the read overtakes it); **the words never do.** *A fact is not edited; it is qualified.* | ✅ `_d169StateProof()` (cover half: the memo's own run present, the newer run absent, disclaimer present) |
| **E-D169-5** | Opening it. Does anything happen? | **No.** No analysis runs, no meter moves, no trend point, **no History event** — *looking at the record is not an event in it.* The report behind it is untouched. The memo is `contenteditable="false"` and carries **none** of the report's editor chrome. | ✅ `_assertReportPackagesNeverProduces()` (now covers `openMemoFromHistory`) + NC `d169_openingAMemoWritesToHistory` |
| **E-D169-6** | A **scheduled** memo is sent. | Same object, same rules: its History event carries the memo id and **opens the memo**, labelled *"PDF (scheduled)"*. One factory, one lifecycle. | ✅ `_assertHistoryOpensTheFrozenMemo()` (both send paths must record the id) |
| **E-D169-7** | `_memoOnScreen()` starts handing back a **copy** of a sent memo instead of the register entry. | **Guard red.** *A sent memo is retrieved, never reconstructed.* A copy is a re-render waiting to happen — it is unfrozen, and nothing then guarantees it matches what travelled. | ✅ NC `d169_sentMemoIsRebuiltNotRetrieved` |
| **E-D169-8** | The History event loses its memo id (`pushHistory` drops it). | **Guard red.** The id is the record's **only handle** on the frozen bytes; without it the most auditable artifact in the product is unreachable from the record of its own sending. | ✅ NC `d169_historyEventDropsTheMemoId` |
| **E-D169-9** | ⚠️ **The state proof itself is vacuous.** | It is not, and that is **proved**: a control forces the memo surface to re-render from current understanding on every open, and **`_d169StateProof()` must go FALSE.** *A proof that cannot fail proves nothing* (D166 §2). | ✅ **NC `d169_stateProofDetectsASilentRerender`** — a negative control **on the proof** |

---

# D170 — A GATED ATTEMPT THAT SURFACES NOTHING (P1) · D170c — POPOVERS · D171 — SEND

> ⛔⛔ **D138 HAS THREE CLAUSES.** *the affordance stays enabled* · *the attempt is gated* · ***the prompt appears,
> with resolutions.*** **The guards only ever verified the first.** `_assertNoDisabledLimitAffordances()` proved the
> Export button was not disabled. It was not disabled. **It also did nothing at all.**
>
> **D166, EXTENDED (D170a):** *Do not merely prove the control is LIVE. Prove the ATTEMPT HAS A CONSEQUENCE.*

**THE RULE THAT WAS MISSING, and it is not a relaxation of MON-04:**
> **A cadence cap governs what the PRODUCT INITIATES. It may never silence the product's ANSWER to a click the user
> made and the product refused.** Throttling an unsolicited nudge is politeness. Throttling a disclosure the user's
> own action demanded is a **dead control**.

| # | Case | Behaviour | Guard |
|---|---|---|---|
| **E-D170-1** | ⛔ **THE REPORTED P1.** Free · `RPT.fmt='link'` (a Basic-only format) · Export. | The prompt **renders**: *"Free exports as PDF"* — the limit (**export formats**), the tier that relieves it (**Basic**), and **two resolutions** (export as PDF free · upgrade). | ✅ `_assertGatedAttemptSurfacesAPrompt()` row `export-format-readout` · NC `aGatedAttemptMayNotReturnSilently` |
| **E-D170-2** | ⛔ **THE SAME ATTEMPT, A SECOND TIME TODAY.** (The actual root cause: `UP-EXPORT` is `cool:'day'` and the prompt log **persists in localStorage**.) | The prompt renders **again, every time**. **Cadence caps do not apply to gated attempts.** The user's click always has a consequence. | ✅ the affordance table runs against a **SATURATED prompt log** (`n:999`, every id fired, every month burnt) · NCs `cadenceCannotSwallowAGatedAttempt` · `dayCapCannotSwallowAGatedAttempt` |
| **E-D170-3** | ⛔ **The prompt fires from inside another surface** — `doExport()` from the export dialog (`.scrim`, z-172); `applyFix()` from the issue flyout (z-260). | It renders **on top**. `.upx-scrim` was **z-index 96 — the lowest overlay in the product**: it rendered *behind the very thing the user was looking at*. The DOM said green; the screen said nothing. | ✅ `_assertPromptSurfaceIsOnTop()` — **derived from the authored cascade** (max z-index of every rule), so it cannot go stale · NC `promptCannotRenderUnderTheSurfaceItFiredFrom` |
| **E-D170-4** | The user hits a limit **mid-analysis**, or **before first value**. (MON-04: never interrupt a pass; never sell before you have delivered. **Both ratified.**) | The prompt is **DEFERRED, never dropped** — queued and fired at the first legal moment — and the user is told **immediately** which limit they hit (a toast, ≤12 words, **no CTA**). **Nothing is ever dropped on the floor.** | ✅ `_deferUP()` / `_upDrainDeferred()`; the ratified guards are preserved and made non-silent |
| **E-D170-5** | A prompt renders with **no resolutions** — only *"Not now"*. | **Guard red.** MON-04: the limit · the tier · **the resolutions.** A prompt whose only button is a dismiss is a wall with an apology on it. (The dismiss is **excluded** from the resolution count.) | ✅ NC `promptMustCarryResolutions` |
| **E-D170-6** | A prompt never names **the tier that relieves the limit**. | **Guard red.** The naming site is **title + body** — the *disclosure*. **The resolution buttons are excluded**: `_resUpgrade()` says "Basic" on every friction prompt, so scanning them would make the clause **true for free** — a vacuous guard, which is the disease. | ✅ NC `promptMustNameTheRelievingTier` |
| **E-D170-7** | Someone adds a new limit-bearing affordance and **does not add an attempt row**. | **Guard red.** Every `friction` prompt must be covered by a row that **fires it for real**. *A control graded on its paint is how this bug got in.* | ✅ `_assertEveryLimitAffordanceIsFireable()` · NC `registryMayNotLoseAnAffordance` |
| **E-D170c-1** | The PM opens **Recipient** to check who the memo is addressed to, while reading paragraph 4. | **The document does not move.** The menus are **popovers** (`position:fixed`), anchored to their button, **out of the flow**. A drawer pushed the document down and yanked it back — *the user was reading a paragraph, and the paragraph moved* (D160). | ✅ `_assertToolbarMenusArePopovers()` — reads the **cascade** (last `position:` wins) · NC `popoverMayNotBecomeADrawerAgain` |
| **E-D170c-2** | Keyboard only. | **Esc closes** · **focus is trapped** (Tab cycles inside) · focus **returns to the button** that opened it · `aria-expanded` on every button · **click-outside closes** · **one open at a time**. | ✅ `_assertToolbarMenusArePopovers()` (live-DOM half) |
| **E-D171-1** | ⛔ **THE DEFECT.** The PM addresses the readout to the Sponsor… and **cannot send it to the Sponsor.** | **Send** sits in the toolbar beside Export. It freezes a **memo** and it goes to the **named person**, on a link that routes **back into OSLO** — *distinct from Export's outside-review link* (canon: `INVITE_AND_SHARE_MODAL_EXPERIENCE_SPECIFICATION_V1`). | ✅ `_assertSharingIsFreeOnEveryTier()` (the affordance must exist) · NC `theReadoutMustHaveASend` |
| **E-D171-2** | ⛔⛔ **A Free user sends the memo to their sponsor.** | **It sends.** **No prompt. No lock. No meter. No tier check anywhere on the path.** **CHG-061 — sharing is a viral primitive and the seed is never gated:** the memo has to be able to land in an executive's inbox from a Free account, or the loop the product grows on never starts. *(Export **formats** are tier-bound — Free = PDF. **A format is not a send.**)* | ✅ `_assertSharingIsFreeOnEveryTier()` — **three ways**: no tier branch in the source · a **real frozen memo on Free** · **no lock chip** in the DOM · NCs `sendMayNotBeTierGated` · `sendMayNotWearALock` |
| **E-D171-3** | Both roads out of OSLO. | **ONE factory** (`_mkMemo`) — same freeze, same cover, same disclaimer, same currency marker. **A memo is a memo however it travelled.** Only `sent_via` (`'shared'` \| `'exported'`) differs, and it is **frozen with the rest**: you cannot re-route a memo after the fact any more than you can re-word it. | ✅ `_assertOneMemoFactory()` · NC `thereMayBeOnlyOneMemoFactory` |
| **E-D171-4** | Sending. Does it run an analysis? | **No.** *A package "packages existing understanding; it never produces new understanding."* No meter, no trend, no run. | ✅ `_assertSendPackagesNeverProduces()` · NC `sendMayNotRunAnAnalysis` |
| **E-D171-5** | The read moves on **after** the memo was shared. | The recipient's copy is **relabelled "previous analysis"** — and **never silently refreshed**. The label is **derived** from the memo's own run against the live trend (a stored flag would go stale); **the words never move**. *A fact is not edited; it is qualified.* | ✅ `_assertSharedMemoIsReadOnly()` · NC `aSentMemoMayNotBeSilentlyReDated` |
| **E-D171-6** | The recipient opens what they were sent. | **Read-only, always** — `contenteditable="false"`, never the editor host, and it carries **none** of the report's chrome. It arrives on **its cover**: mark, ratified disclaimer, currency marker. | ✅ `_assertSharedMemoIsReadOnly()` · NC `aSentMemoMayNotBeEditable` |
| **E-D171-7** | History. | **Both roads append an event** (`share` / `export`) carrying the memo id, and **D169 opens the frozen memo from either.** The row says **how it travelled**. | ✅ `_assertHistoryOpensTheFrozenMemo()` · behavioural: share → `share:MEMO-n`, export → `export:MEMO-n`, both open |

---

# D172 — A SCHEDULED READOUT IS AN AUTOMATED **SHARE** · THE WORKSPACE IS **REPORTS**

| # | Edge case | Behaviour | Guard |
|---|---|---|---|
| **E-D172a-1** | ⛔ **The schedule fires.** Where does the memo go? | **To people.** A scheduled readout is an automated **SHARE** — `sent_via:'shared'`, a **scoped read-only token grant** for the recipient, and a **sent** History event. **Nobody schedules a PDF onto their own disk.** | ✅ `_assertScheduledSendIsAShare()` — structural (`'shared'`, never `'exported'`) **and** state (fire it for real: a frozen memo, a grant, a `share` event, an unmoved trend) · NCs `aScheduledSendMayNotBeAnExport` · `aScheduledShareMayNotSkipTheGrant` |
| **E-D172a-2** | The analysis is **behind the plan** when the schedule fires. | **D147 still binds.** It **re-checks currency at send time**, from the live state, and the memo goes out **labelled "previous analysis"**, saying so on its face. **It is never passed off as current.** | ✅ `_assertScheduledReportRechecksCurrency()` · behavioural: `cover.currency` → *previous analysis* |
| **E-D172a-3** | Could the schedule "freshen" the read first, so the sponsor gets something current? | **No — and this is the trap D147 exists to spring.** It **never runs an analysis to freshen itself**: that would make a package **produce** understanding. It packages what exists, and it labels it honestly. | ✅ `_assertScheduledReportRechecksCurrency()` (no meter/governor/deep-pass call in the source) · `_assertScheduledSendIsAShare()` (trend + governor unmoved) |
| **E-D172a-4** | History → click the **"Scheduled memo sent to …"** row. | **D169 opens the frozen memo** — the exact bytes that were sent, on its cover, read-only. Not a re-render. | ✅ `_assertHistoryOpensTheFrozenMemo()` · `_d169StateProof()` · behavioural: byte-identical after the report is edited |
| **E-D172b-1** | ⛔⛔ **A Free user sends the readout to their sponsor — five times in a week.** | **All five send.** **The share is FREE on every tier, manual, unlimited.** No prompt, no lock, no meter, **no tier check anywhere on the send path** (CHG-061 — the viral seed is never gated). | ✅ `_assertSharingIsFreeOnEveryTier()` · `_assertSchedulingIsTheGateNotTheShare()` · NC `theShareMayNotBeGatedInstead` |
| **E-D172b-2** | ⛔ **A Free user tries to SCHEDULE it.** | **The automation is Basic.** The control stays live (D138), the **attempt** is gated, and a prompt renders: **"Basic sends it for you every Friday"** — naming the limit, naming Basic, with **two resolutions, the free one first: _Send it now_.** *The prompt sells the automation. It never sells the ability to share.* | ✅ `_assertSchedulingIsTheGateNotTheShare()` (structural + Free state proof + the free resolution IS a send) · `_assertGatedAttemptSurfacesAPrompt()` row `report-schedule` · NC `theAutomationMayNotBeFreeOnFree` |
| **E-D172b-3** | Is that a breach of CHG-061? | **No.** Sharing is guaranteed on Free as a **viral primitive**. **Cron is not a viral primitive.** The **seed of the loop** — a human choosing to put a memo in front of an executive — is **completely ungated**. **Same shape as D154** (editing free; persistence gated): **meter the labour, never the understanding.** | ✅ the pair guard proves both halves at once, on one Free account, in one pass |
| **E-D172c-1** | The recipient clicks the link. **Do they have to sign up?** | **No.** A shared memo is a **scoped, token-granted, read-only view** — the **same mechanism as the CRR reviewer grant** (DL-102 A): **the link IS the invite, and the invite IS the authentication.** One click. No password, no form, no account. And **nobody is anonymous** (DL-021): they were sent it, **by name**. | ✅ `_assertSharedMemoUsesTheGrantMechanism()` (no password field, no form, no account-creating call) · NC `aSharedMemoMayNotAskForASignup` |
| **E-D172c-2** | What can the recipient see? | **That one memo.** Not the plan, not the issues, not the other memos. It carries its **cover, disclaimer and currency marker**, and it is **read-only** (`contenteditable="false"`, never the editor host). Revocable at any time (D117/CR-6). | ✅ `_assertSharedMemoIsReadOnly()` (now grades the **recipient's** surface too) · NC `aSentMemoMayNotBeEditable` |
| **E-D172c-3** | The read moves on **after** they were sent it. | Their copy is **relabelled "previous analysis"** — **never silently refreshed, never rewritten.** The label is derived live; **the words never move.** | ✅ `_assertSharedMemoIsReadOnly()` · NC `aSentMemoMayNotBeSilentlyReDated` · `theRecipientViewMayNotRebuildTheMemo` |
| **E-D172c-4** | Two grant mechanisms? | **One.** `_grantScopedAccess()` is the **only** admission path — the reviewer grant and the memo grant are the **same function with a different `kind`**. `_mkLink()` is the only link factory. **A second mechanism is the one that quietly admits a stranger.** | ✅ `_assertSharedMemoUsesTheGrantMechanism()` · NC `aShareMayNotInventASecondGrantMechanism` |
| **E-D172c-5** | Does a memo grant cost a seat or an invite? | **Neither.** No Membership (N-2), no seat, no invite, **no tier check, no meter** (CR-2 / CHG-061). The Access panel counts **Review grants** and **Shared memos** separately — a person holding a memo grant is **not** a reviewer. | ✅ the runtime CR-2 ledger assertion inside `_grantScopedAccess()` · `_assertEvidenceNeverGated()` |
| **E-D172d-1** | What is this workspace called? | **Reports.** The document inside it is the **Readout**. The nav, the crumb and the tooltip say **Reports**; the readout's own toolbar says **Readout**. **Never call the workspace a Readout; never call the document Reports.** | ✅ `_assertReportsHostsOneReportType()` (nav + crumb + toolbar) · NC `theWorkspaceMayNotBeCalledAReadout` |
| **E-D172d-2** | ⛔⛔ **A second report type.** | **There isn't one.** `REPORT_TYPES` holds **exactly one** entry — the Readout — and a second type is an **ADDITION** to that registry, not a rebuild. ⛔ **NO SPECULATIVE UI FOR REPORTS THAT DO NOT EXIST.** *That is exactly how the six-card scaffold happened the first time.* **D143 stands:** the six "report types" it killed were **sections of one memo**, and **they stay dead.** | ✅ `_assertReportsHostsOneReportType()` (registry **and** DOM: no type picker, no gallery, no cards) · NCs `aSecondReportTypeMayNotBeRegistered` · `theSixCardScaffoldMayNotReturn` |

---

# D173 — THE PAYOFF (owner-directed, 2026-07-12)

| # | Case | Behaviour | Proof |
|---|---|---|---|
| **E-D173-1** | Apply a fix to the critical Wi-Fi issue. | The payoff states **all three**: *Feasibility: Very Low → Low* · *Issues 6 → 5 · Critical 1 → 0 · Open questions 2 → 1 · Confirmed artifacts 0 → 1 of 7* · *Feasibility is still the limit.* **29 words.** | ✅ behavioural (real `applyFix` path) · `_assertPayoffWithinBudget()` |
| **E-D173-2** | ⛔ **A count OSLO cannot compute.** D173b names *"Dependencies confirmed 5 of 8 → 6 of 8"*. | **It is not shown.** The model holds **no dependency register** — `REVIEWS` is a list of review requests, not dependencies with a confirmed state. **Omitted, not invented** (Anti-Assumption). Escalated as **O-D173-1**. | ✅ `_assertEveryPayoffCountIsComputed()` — an uncomputable `get()` cannot reach the snapshot; an orphan key in the snapshot fails the guard · NCs (3) all bite |
| **E-D173-3** | Someone hard-codes a count into the payoff. | **Guard failure, loudly.** Every integer the payoff prints must exist in the before/after snapshots. A number in neither is a fabrication. | ✅ NC: printing *"Dependencies confirmed 5 of 8 → 6 of 8"* fails the guard |
| **E-D173-4** | A count moves, but no band crosses (e.g. a moderate clarification). | The payoff renders **without a band line** — counts + limiter only. Honest: no band moved, so none is claimed. | ✅ behavioural (`_submitClarification('ISS-02')` → 23 words, no band row) |
| **E-D173-5** | **Nothing moves.** | **The payoff does not render** (`moved:false`). OSLO does not manufacture an event. | ✅ `renderPayoff` returns without showing the card |
| **E-D173-6** | ⛔⛔ **The read FALLS** — two attested disagreements drop Alignment Moderate → Low. | *"Your read fell — because you learned something."* **Same card, same classes, same weight, no negative colour, no alarm.** The limiter is still named. | ✅ `_assertRiseAndFallAreVisuallyEquivalent()` (model + DOM signature + colour tokens + CSS modifiers + the fall must still *say* it fell) · 5 NCs bite |
| **E-D173-7** | Someone styles the fall red — or the rise green. | **Guard failure.** No colour token may appear in any `.payoff` rule, and no `.payoff.up/.down` modifier may exist. **D003: the maturity ramp is neutral; severity colour is for issues.** | ✅ NC: `.payoff.down .pay-band{color:var(--danger)}` fails the guard |
| **E-D173-8** | Someone makes the fall *quiet* instead of *plain*. | **Guard failure.** Neutral must never mean silent — the fall names itself. | ✅ NC: blanking `.pay-note` fails the guard |
| **E-D173-9** | ⛔ **A delta on the 0–100 index** (`62/100 ▲+4`). | **Impossible by construction:** `idx` is **not in the payoff snapshot**, so a delta cannot be formed. And the rendered index line must read exactly `N/100`. | ✅ `_assertNoIndexDelta()` · NCs: a `+4` chip · printing `58/100 → 62/100` — both bite |
| **E-D173-10** | Someone **deletes** the index. | **Guard failure.** The recommendation is **DEMOTE, not destroy**: the owner may calibrate it (DL-062 F1) and restore it. | ✅ NC: removing `#ov-idx` fails the guard |
| **E-D173-11** | Someone puts the index **back in the 52px hero slot**. | **Guard failure** until it is calibrated. The band is the hero; the number is a secondary aggregate. | ✅ NC: restoring `font-size:52px` fails the guard |
| **E-D173-12** | The payoff grows into a paragraph. | **Guard failure at 46 words.** The fitter trims the optional clause, then trailing counts — **never** the action, the band, the fall note or the limiter. | ✅ `_assertPayoffWithinBudget()` · NCs bite in both directions |
| **E-D173-13** | The user acts, then the payoff shows *"Confirmed artifacts 0 → 1"*… but the artifact was attested **before** the snapshot. | **Fixed defect (found in build):** the snapshot was being taken **after** `basis='attested'`, so the row silently vanished — **the user's own action was invisible in its own payoff.** `before` is now taken at the top of `applyFix` / `_submitClarification`. | ✅ behavioural: the row is present |
| **E-D173-14** | The trend, the History chips and the Overview trend row. | **Neutral in both directions** (was green ▲ / amber ▼) and the Overview trend row is **computed** (was hard-coded to rise, so a fall was *drawn as a rise*). | ✅ `renderOvTrend()` / `_readDirection()` · behavioural: fall ⇒ *"↘ Softened"* |

## D174 — THE OVERVIEW HERO IS THE MATURITY RAMP

| # | Edge case | Behaviour | Proof |
|---|---|---|---|
| **E-D174-1** | ⛔⛔ **A user reads the ramp as a RAG / health indicator.** | **P1 defect if possible (DL-104 §5).** The ramp carries **no severity colour** (cascade allowlist: only `--text · --muted · --subtle · --border* · --surface*` — not even the brand `--primary`), **no percentage fill**, and **no health vocabulary** anywhere in the hero. | ✅ `_assertRampIsNeutral()` (reads the **CASCADE**, not the DOM) + `_assertRampIsNotAHealthBar()` · **13 NCs bite** |
| **E-D174-2** | Someone fills the steps **below** the read, so the ramp reads as *progress*. | **Guard failure.** Every unlit step has the identical class `rstep`; exactly one carries `.on`. A 1..n fill is a progress bar, and a progress bar is a health bar with better manners. | ✅ NC: `class="rstep done"` on step 1 → bites |
| **E-D174-3** | Someone gives the lit step a colour (green at High, amber at Low). | **Guard failure.** The lit step is separated by **weight**, never hue: `--text` 9px vs `--subtle` 7px. | ✅ NC: `.rstep.on .rbar{background:var(--primary)}` and `{background:var(--danger)}` → both bite |
| **E-D174-4** | The lit band is **hard-coded** (the markup ships "Moderate"). | **Impossible by construction:** the static markup contains **no band word** — `#ov-ramp` is empty and `_rampHTML()` derives all five steps from `_BANDORD`, lighting `indexOf(read.band)`. | ✅ `_assertLitBandIsComputedFromState()` walks **all five** bands, then moves the live read and checks the ramp moved · 3 NCs bite |
| **E-D174-5** | A band is dropped from the scale (four steps, or six). | **Guard failure.** The scale is the five bands of DL-086/098, in order. | ✅ NC: a four-step builder → bites |
| **E-D174-6** | **The read FALLS.** | The ramp **steps down**, in the same weight and the same neutral colour, and the direction reads *"↘ Softened — <cause>"*. **Identical class, identical DOM signature, no inline style, no colour token.** | ✅ behavioural: rise vs fall signature identical (`SPAN.trend-arrow\|B.\|SPAN.tcause`) |
| **E-D174-7** | Only **one** analysis run exists — there is no direction to state. | **The direction row is not rendered.** A direction needs two runs; a value OSLO cannot compute is **not shown** (D173). | ✅ `_directionIsComputable()` · guard asserts *shown ⇔ computable*, in both directions |
| **E-D174-8** | The direction is stated **without its cause**. | **Guard failure** (D056: direction **and** cause, never a magnitude). | ✅ NC: stripping the cause from `#ov-trend-lab` → bites |
| **E-D174-9** | The direction carries a **magnitude** (*"↗ +4"*, *"↗ 62/100"*). | **Guard failure.** | ✅ regex clause in `_assertHeroElementsAreComputed()` |
| **E-D174-10** | ⚠️ **The Extended pass lands and the direction stays hidden.** | **Fixed defect (found in build):** `deepComplete()` re-renders the read **before** it appends the run, so at render time there was still only **one** run to compute a direction from. **`pushTrend()` now re-paints the hero** — the hero follows the record whatever order a caller writes it in. | ✅ behavioural: land → Extended pass ⇒ *"↗ Strengthened — deeper analysis firmed the read (Feasibility rose Very Low → Low)"* |
| **E-D174-11** | The limiter is typed in, or disagrees with the CAF bars. | **Guard failure.** `#ov-limit` renders `_limitingOf(r).dim` — the **same** computation that marks the `.cafrow.lim` row and that the chat and the payoff use. | ✅ NC: naming Clarity, and stubbing `_limitingOf` → both bite |
| **E-D174-12** | The index climbs back into the hero slot (uncalibrated). | **Guard failure** until the owner calibrates it (DL-062 F1). The CSS clause grades **the lit band** vs the index. | ✅ NC: shrinking `.ramp .rstep.on .bandhero` to 11px → bites |
| **E-D174-13** | The hero grows into a paragraph. | **Guard failure above 40 words** of prose (band labels are a *scale*, not prose). The live hero is **19 words**. | ✅ `_assertHeroWithinBudget()` · NC bites |
| **E-D174-14** | ⚠️ The **Provisional / Current** chip in the hero card uses `--warning` / `--success`. | ✅ **CLOSED BY D175** — the chip is **neutral**. See the D175 table below. | ✅ `_assertHeroCardCarriesNoSeverityColour()` |

## D175 — THE ANALYSIS-STATE CHIP IS NEUTRAL (amends D040 · closes O-D174-1)

> **The defect was not in what either element SAID. It was in what they said TOGETHER.**
> Amber ("Provisional") and green ("Current") **one line above a five-step maturity ramp** is the adjacency a
> reader turns into **RAG** — the **P1 health-framing class (DL-104 §5)** arriving **through a side door**.
> **And the guard could not see it, because the guard was scoped to the ELEMENT, not the ADJACENCY.**

| # | Edge case | Behaviour | Guard |
|---|---|---|---|
| **E-D175-1** | ⛔⛔ Someone puts `--warning` / `--success` back on the chip. | **Guard failure — twice.** The chip obeys the **RAMP's strict allowlist** (`--text · --muted · --subtle · --border* · --surface*` — **not even the brand `--primary`**, because the chip sits on top of the ramp), and the **card-wide** guard bans every severity/health token anywhere in the hero card. | ✅ `_assertAnalysisStateChipIsNeutral()` + `_assertHeroCardCarriesNoSeverityColour()` · **NC-01/03 bite** |
| **E-D175-2** | ⛔ The amber comes back **with the token name filed off** — `background:rgba(217,164,65,.08)` is `--warning` in a raw literal, and **that is exactly how the defect was originally written**. | **Guard failure.** A token blacklist only catches the colours somebody already named. Every colour **literal** in the card is graded by **CHROMA**: greyscale and transparent pass; **anything with hue does not**. | ✅ `_chromaticLiterals()` · **NC-02 bites** |
| **E-D175-3** | ⛔⛔ **THE ORIGINAL DEFECT'S SHAPE:** a severity colour is added to something **inside the hero card but outside the confidence focus** — the footer links, the false-confidence flag, the CAF label. | **Guard failure.** **The scope is now the CARD.** D174's guard graded `.conf-focus` and this one element sat outside it — *that scoping is what let the defect through*. The scope is **derived from the live card** (every class/id on a hero element, minus generic state modifiers, plus the ancestor chain), so it **cannot go stale**. | ✅ `_heroCardScope()` + `_heroCardCssRules()` · **NC-04 (`.conf-foot`) and NC-05 (`.card-flag`) bite** |
| **E-D175-4** | ⛔ The colour hides in a **state that is not on screen** (`.ustate.cur` was green while the chip read *Provisional*) or in an **`@media` block**. | **Guard failure.** The guard reads the **AUTHORED CASCADE, not the DOM** — a DOM guard cannot see a rule for a state nobody is in. ⚠️ **The `@media` case was a real hole in the guard's first draft** (the selector was read as everything before the *first* `{`, so `@media(…){.ustate.cur{…}}` was invisible) — **its own negative control caught it** (D166 §2), and `_cssSelOf()`/`_cssBodyOf()` now shed the at-rule prefix. **Fix the guard, never the doctrine.** | ✅ **NC-06 bites** (it did not, before the fix) |
| **E-D175-5** | Someone reaches for **inline style** — the second door into a card. | **Guard failure.** Every `[style]` in the hero card is scanned for severity tokens and chromatic literals. | ✅ **NC-07 bites** |
| **E-D175-6** | The **brand accent** (`--primary`) is put on the chip. | **Guard failure.** ⛔ **AMENDED BY D176a: `--primary` is now banned CARD-WIDE**, not just on the chip and the ramp — so this bites **both** guards now. | ✅ `chipIsNeutral` **and** `heroCardNoSeverity` |
| **E-D175-7** | ⛔ **Neutral becomes UNREADABLE** — someone "neutralises" the chip by making both states identical. | **Guard failure.** Provisional and Current must differ in the cascade **by weight or by shape** — `600` vs `700`, **hollow dot** vs **filled dot**. Since the allowlist makes hue impossible, **any** difference is one a colour-blind reader sees too. | ✅ `_assertAnalysisStateIsLegible()` · **NC-09 bites** |
| **E-D175-8** | ⛔ The **INFORMATION** is removed instead of the colour — the label is rewritten (*"Current"* → *"OK"*) or the chip is deleted. | **Guard failure.** **D040's labels are honest and they stay:** the three states are driven through the **real painter** and must read exactly **Provisional · Current · Last-good**. D175 removed the colour, **not the information**. | ✅ **NC-10 (label) and NC-11r (chip removed at runtime) bite** |
| **E-D175-9** | The guard's **subject disappears** — every `.ustate` rule is deleted, or the hero card is gone. | **CANNOT VERIFY ⇒ FAIL** (D166 §1 — *a guard that grades nothing is worse than no guard*). Vacuity floors: **≥4 chip rules**, **≥20 hero-card rules**, or the guard fails loudly and names what is missing. | ✅ **NC-12 bites** |
| **E-D175-10** | The guard becomes **over-broad** and starts grading other surfaces (`.issue .card`, `.tog.on`) — every issue in the product is severity-coloured, so a sloppy scope would fail the whole app. | **It must NOT fire.** A rule enters scope only if its **subject compound** carries a **hero anchor class** (a generic state modifier like `.on` cannot pull a rule in) **and** every **ancestor compound** is on the card's real ancestor chain. | ✅ **NC-15: `.issue .card{border-color:var(--danger)}` + `.tog.on{color:var(--success)}` → guards stay GREEN** |
| **E-D175-11** | The **D161 prototype-notes rail** (which *legitimately* speaks in `--warning`) is inside the hero card when notes are ON. | **It is stripped before grading.** Notes are **review apparatus, not product copy** — a guard that graded the governance rail would be **failing on its own documentation** (the exact D166 failure). | ✅ **NC-16: notes ON ⇒ `.pn` renders inside the hero ⇒ 72/72 guards still green** |
| **E-D175-12** | The chip's `Last-good` state (analysis errored). | It wears the **provisional** treatment — hollow dot, muted — because that is what it is: **the read on screen is the last good one**. Label unchanged. | ✅ `_assertAnalysisStateIsLegible()` drives all three states through `renderOverview()` |

---

## D176 — THE LIMITER ROW, AND THE CAF BARS (owner: approved, 2026-07-12)

| # | Edge case | Behaviour | Guard / control |
|---|---|---|---|
| **E-D176-1** | ⛔⛔ Someone puts the **brand orange** back on the CAF limiter row (`.cafrow.lim`) — or on any other element in the hero card. | **Guard failure.** The card's banned list is now `--success · --warning · --danger · --error · --crit · --conf-* · --primary*` **+ every chromatic literal**. **The limiter is a FACT, not a WARNING: emphasis by weight, never by hue.** | ✅ `_assertHeroCardCarriesNoSeverityColour()` · **NC-D176-01** (`.cafrow.lim .cn{color:var(--primary-light)}`) bites |
| **E-D176-2** | The orange returns **hidden in an `@media` block**. | **Guard failure.** The selector is what sits before the **last** `{`, with the at-rule prefix shed — the lesson NC-06 taught this guard once already. | ✅ **NC-D176-02** bites |
| **E-D176-3** | The orange returns as an **inline style**, or with **its name filed off** (a raw `rgb(217,122,58)`). | **Guard failure — both doors.** Inline styles in the card are scanned, and every colour **literal** is graded by **CHROMA** (greyscale passes; a hue nobody thought of does not). | ✅ **NC-D176-03 / NC-D176-04** bite |
| **E-D176-4** | The orange returns through a **bare subject** — `.cr-limit b{color:…}`, where the last compound carries no card class. | **Guard failure.** ⚠️ **The first draft of the card guard MISSED THIS** — it required the *subject* compound to be a hero element. **Its own negative control caught it** (D166 §2); a bare subject anchored by an ancestor is now in scope. | ✅ **NC-D176-04** bites (it did **not**, before the guard was fixed) |
| **E-D176-5** | ⛔⛔ A **percentage fill** returns to a CAF / confidence / reliability element — in the **cascade**. | **Guard failure.** A bar filled to N% asserts a **cardinal magnitude** on an **uncalibrated** scale (DL-062 F1) and wears **progress-bar grammar** (DL-104 §5 — P1). | ✅ `_assertNoPercentageFillOnMaturitySurfaces()` · **NC-D176-05** (`.cafrow .rbar{width:55%}`) bites |
| **E-D176-6** | The fill returns **disguised** — `transform:scaleX(.55)`, or inside an `@media` block. | **Guard failure.** A scale is a fill with its hat pulled down; `clip-path` with a `%` is too. | ✅ **NC-D176-06 / NC-D176-07** bite |
| **E-D176-7** | ⛔⛔ The fill is written by the **RENDER PATH** — `el.style.width = r.feasW + '%'`. **This is how the CAF bars were actually painted.** | **Guard failure.** No painter of a maturity surface may write a width **at all**: the guard reads the **source of every painter** (`renderOverview` · `renderCafRows` · `renderConfPop` · `renderHero` · `renderRamp` · `_rampHTML` · `_cafRampInto` · `renderFalseConfidence`). **A cascade-only guard would never have seen the original defect.** | ✅ **NC-D176-09 — the load-bearing control** — bites |
| **E-D176-8** | A CAF band is **hard-coded** (the ramp drawn for a level the read does not hold), or a **second, hand-rolled ramp** appears. | **Guard failure.** Each row's ramp is compared **byte-for-byte** with `_rampHTML(lvl,{compact:true})` for the level **on the read**, and the whole grade is re-run **after moving the read**. **One ramp, one mental model.** | ✅ `_assertCafDimensionsRenderAsBands()` · **NC-D176-10** bites |
| **E-D176-9** | The **limiter is marked on the wrong row** (typed in, rather than derived). | **Guard failure.** `.lim` must be exactly the dimension `_limitingOf()` names — on **both** surfaces. | ✅ **NC-D176-11** bites |
| **E-D176-10** | A CAF ramp is **deleted** (so there is nothing to grade). | **CANNOT VERIFY ⇒ FAILURE.** A missing subject is a failure to verify, not a pass (D166 §1) — and the guard also fails if it grades fewer than **12** dimension-renders (3 dims × 2 surfaces × 2 reads). | ✅ **NC-D176-12** bites |
| **E-D176-11** | The guard becomes **over-broad** and attacks the **Attention heat map**. | **It must NOT fire.** Those cells **are issues**, and **severity colour belongs to issues** (D003 / D060). The heat map is **correct as-is and untouched**. | ✅ **must-not-fire control** — `.heat-cell.l3{background:var(--danger)}` ⇒ guards stay **green** |
| **E-D176-12** | The guard becomes over-broad and attacks the **true-count bars** (`Dependencies confirmed 2 / 3`). | **It must NOT fire.** That ratio is **exactly defensible**, its **denominator is on screen**, and **D173b blesses true counts** — *"no fills"* must never quietly become *"no counts"*. **Owner call still open → O-D176-2.** | ✅ **must-not-fire control** — `.prog-trk i{width:66%}` ⇒ fill guard stays **green** |
| **E-D176-13** | The read **falls** and a CAF ramp steps **down**. | **Nothing changes but the position.** Same weight, same colour, no alarm — a fall is not red (D173c / D003). | ✅ the state proof inside `_assertCafDimensionsRenderAsBands()` re-grades every ramp on a **moved read** |

---

# D177 — the Extended pass finds things (owner, 2026-07-12)

| # | Edge case | Behaviour | Proof |
|---|---|---|---|
| **E-D177-1** | **The Extended pass moves no counts** (the bug that shipped): the payoff claims a deeper read while nothing countable moved. | **P1 DEFECT.** The narrative claims something happened; the counts say nothing did. | ✅ **NC-D177-01** bites — `_assertDeepPassMovesBandAndCounts()` goes red |
| **E-D177-2** | The pass finds the issues but the **band does not move**. | **Failure.** A deeper read of the same evidence **firms** the assessment — that is what the extra budget buys. | ✅ **NC-D177-02** bites |
| **E-D177-3** | The deeper read surfaces **nothing critical**. | **Failure** against the D177a demo contract (*Issues 6 → 8 · Critical 1 → 2*). | ✅ **NC-D177-03** bites |
| **E-D177-4** | The **model** moves and the **surface** drops the counts. | **Failure** — the same hollowness wearing a clean shirt. Graded on the **rendered DOM**, not the model. | ✅ **NC-D177-04** bites |
| **E-D177-5** | The **plain-language line is lost** (more issues *and* a higher band, and the payoff never says so). | **Failure.** That sentence is the clearest statement of the doctrine in the product. | ✅ **NC-D177-05** bites |
| **E-D177-6** | A finding **cites evidence that does not exist**, or is **filed against a section that is not a plan artifact**. | **Failure — a fabricated fact.** A Deep Pass re-reads the **same** inputs; it may not invent evidence. | ✅ **NC-D177-06 / -07** bite |
| **E-D177-7** | `DEEP_FINDINGS` is **empty** (nothing to grade). | **CANNOT VERIFY ⇒ FAILURE.** A missing subject is a failure to verify, not a pass (D166 §1). | ✅ **NC-D177-08** bites |
| **E-D177-8** | The guard becomes over-broad and **fires because the counts went UP while the read went UP**. | **It must NOT fire.** That is the shipped state and it is **correct** — confidence is understanding maturity, not health. A guard that reddened here would be enforcing the exact misreading the doctrine exists to prevent. | ✅ **must-not-fire control** — `mustNotFire_moreIssuesAndAHigherBandIsFine` stays green |
| **E-D177-9** | The **word budget** would trim the `Issues 6 → 8` row away while the note still says *"found two more"*. | **The note is dropped with it.** A sentence may never outlive the count it describes (`_payoffFit`). | ✅ mechanism in `_payoffFit()` |
| **E-D177-10** | The user is **mid-edit in an artifact** when the pass lands. | **The analysis update does not take the caret.** The open artifact re-reads its annotations only when it is not dirty (`!_pendEdit`); otherwise the mark appears on the next open. | ✅ guard in `deepComplete()` |
| **E-D177-11** | The Extended pass **fails** (D041) and is then **retried**. | Last-good is preserved; the retry runs `deepComplete()`, which surfaces the findings **once** (`_deepPassSurfaceFindings()` is idempotent) and pays off normally. | ✅ idempotence by construction |
| **E-D177-12** | The **boot self-check** runs the real `deepComplete()`. | **It leaves no trace.** `ISSUES` · `_istatus` · `HISTORY` · `TREND` · the chat · the payoff · the analysis state are all restored (verified: after boot, HISTORY 1 · TREND 1 · chat 0 · ISSUES 6 · provisional). A guard that corrupts the epistemic record it runs beside is not a guard (D166). | ✅ verified in jsdom |
| **E-D177-13** | The user opens **Scope or Schedule before** the Extended pass. | The draft reads **exactly as the Fast Pass wrote it** — the span is inert plain text (`recording`, `Pending`). The **weak text was always there**; only the *mark* is new. | ✅ verified: no `data-fid` in the body pre-pass |

---

# D178 — a Deep Pass **asks**, it does not only find (owner, 2026-07-12)

| # | Edge case | Behaviour | Proof |
|---|---|---|---|
| **E-D178-1** | **The Deep Pass raises no clarification** — it finds the funding-vs-commitment gap and only **flags** it. | **Failure.** *Finding an issue and knowing what would close it are different acts — and OSLO can do both.* A pass that finds and never asks leaves the user holding a problem with no route out of it. **Open questions never moves**, so the third count is missing too. | ✅ **NC-D178-01** bites — `_assertDeepPassMovesBandAndCounts()` goes red |
| **E-D178-2** | The ask is **raised in state** and the **payoff swallows it** (the `Open questions` row is trimmed off the model). | **Failure.** The count moved and the user was never told. Graded on **model *and* rendered DOM**. | ✅ **NC-D178-02** bites |
| **E-D178-3** | **OSLO asserts instead of asking** — the clarification stops being a question (*"the sponsorship floor is $250k"*). | **Failure — a fabricated fact.** OSLO **does not know** the answer and must say so. An assumption dressed as a finding is exactly what the Anti-Assumption protocol exists to prevent. | ✅ **NC-D178-03** bites (the `clar.q` must contain a `?`) |
| **E-D178-4** | The **word budget** would trim the `Open questions 2 → 3` row away while the note still says *"I have one more question."* | **The sentence is dropped with the row.** An ask may never outlive the count that evidences it (`_payoffFit`) — the same discipline as the *"found two more"* line. | ✅ mechanism in `_payoffFit()` |
| **E-D178-5** | The user **answers the ask in the chat** instead of the panel. | **Identical event.** `answerClarificationFromChat()` → `_submitClarification(id, val, 'chat')` — the same project-info update, the same lifecycle, the same analysis update, and a **byte-identical History entry**. Only the surface that reports back differs. | ✅ verified in jsdom — panel vs chat History entries compared byte-for-byte, **identical** |
| **E-D178-6** | The user answers, and expects the **issue to close on the spot**. | **It does not.** `ISS-07` goes **open → Addressed** (awaiting an analysis update) → **Resolved** *only when the update lands*. **Advisory-only: nothing resolves by hand** (D088). | ✅ verified: `open → addressed → (analysis update) → resolved`, on **both** surfaces |
| **E-D178-7** | The ask renders **expanded** — a big empty textarea in the turn the user came to **read**. | **Failure** (D162c/D165e). It is a **one-line prompt** in both the panel row and the chat block, expanding on click. | ✅ verified: panel `aria-expanded="false"`, no `.open`; chat `.chat-clar` has no `.open` |
| **E-D178-8** | The user **answers the ask**, and the payoff still claims *"I have one more question."* | **Impossible by construction.** `asked` is the **rise** in the `questions` row; answering makes it **fall** (`3 → 2`), so `asked` is `0` and no ask is claimed. Verified: the post-answer payoff reads `Open questions 3 → 2` and carries **no** ask sentence. | ✅ state proof in `_payoffModel()` |
| **E-D178-9** | A guard runs `deepComplete()` **after** the real pass has already put a live answer box in the user's thread. | **The guard leaves no trace.** Raising a clarification retires the earlier box for that issue (`_retireClarBoxes`) — which, inside a guard, would **disable a box the user was about to type into**. `_CHAT_PROBE` fences exactly that housekeeping. | ✅ verified in jsdom — the live `chatClarBox-ISS-07` is **byte-for-byte unchanged** after running the guard + all 13 negative controls |
| **E-D178-10** | The Extended pass **fails** (D041), then retries. | The ask is surfaced **once** — it rides on `_deepPassSurfaceFindings()`, which is idempotent, and `postDeepPassComplete()` re-derives the asked ids from what the pass actually surfaced. | ✅ idempotence by construction |

---

# D179 — Overview layout: edge cases

| ID | Edge case | Behaviour | Verified |
|----|-----------|-----------|----------|
| **E-D179-1** | **Nothing has moved yet** (a fresh Fast Pass, no user action). | **No payoff strip, no ghost, no delta.** `_MOVE` is `null`, `_PREV_RUN` is `null`, and `_progressRows()` returns absolutes with `delta: null`. The counts still show; the arrows do not. **A value OSLO cannot compute is not shown** (D173). | ✅ jsdom: at boot, `#payoff` hidden · no `.rstep.ghost` on any ramp · Progress reads `Issues 6 · Critical 1 · Open questions 2 · Confirmed artifacts 0 / 7` with **no arrows** |
| **E-D179-2** | The **overall band does not move**, but a **CAF dimension does** (the demo: Feasibility Very Low → Low, band holds at Moderate). | The movement is drawn on the **dimension's** ramp. The **hero ramp draws NOTHING** — no ghost, no arrow. **Never fake a movement that did not happen.** | ✅ jsdom: `#cg-feas-ramp` ghost[0] lit[1] arrow ✓ · `#ov-ramp` ghost = **none** · guarded by `_assertDeepPassMovesBandAndCounts()` and `_assertGhostBandIsComputedFromPreviousSnapshot()` |
| **E-D179-3** | The user **dismisses** the payoff. | The strip goes **and the ghosts go with it** (`_MOVE = null`, ramps repainted). **The state is untouched** — the ramp, the band word, the qualifier, the limiter and the index never depended on the payoff being there. **The Progress deltas stay**: they are *"since the last run"*, and the run still happened. | ✅ jsdom: post-dismiss — `display:none` · `_MOVE === null` · no ghost on any ramp · ramp still lit at **Moderate** · Progress still reads `8 ↑2` |
| **E-D179-4** | The user **reloads** after an analysis update. | **The payoff is gone.** Nothing about it is persisted (`_MOVE` / `_PREV_RUN` are in-memory; no path touches storage). **The state remains, because the state is what remains.** | ✅ `_assertPayoffIsNotPersisted()` — source proof on `renderPayoff` · `dismissPayoff` · `_payoffModel` · `renderProgress` |
| **E-D179-5** | A count **cannot be computed** (e.g. *Dependencies confirmed* — the model holds **no dependency register**). | It is **absent from the snapshot, therefore absent from Progress.** **Omitted, not invented** (Anti-Assumption). Escalated in `open-items.md`. | ✅ live probe: a registry row whose `get()` returns `null` **never reaches the chips**; a computable probe **does** (so the proof is not vacuous) |
| **E-D179-6** | A count moved, and then **moved back** to where it started. | **No delta.** `_progressRows()` compares against `_PREV_RUN` — the snapshot *before the last update*. Equal ⇒ `delta: null` ⇒ no arrow. *"Issues 6 → 6"* is noise, and D173 forbids fabricating a delta. | ✅ by construction (`p !== v` gate) |
| **E-D179-7** | Two analysis updates land back to back. | The **second** rebases `_PREV_RUN` on its own `before` snapshot. The deltas always read *"since the last run"* — **never cumulative, never stale.** | ✅ jsdom: fix → deep pass; the deltas re-derive against the deep pass's own baseline |
| **E-D179-8** | The read **FALLS** (a reviewer rejects; Alignment drops a band). | **The same strip, the same classes, the same weight.** The ramp ghosts the **higher** band, the arrow points **back** (`⟵`), the line reads *"Your read fell — you learned something."* **No negative colour, in the strip or on the ramp.** | ✅ `_assertRiseAndFallAreVisuallyEquivalent()` (identical DOM signature) + builder proof: rise and fall use the **same class vocabulary**; only the glyph reverses |
| **E-D179-9** | An **alignment move that does not cross a band** (55 → 47: still *Moderate*). | **No payoff, no ghost, no arrow.** A sub-band wobble is not a band transition, and OSLO will not draw one. *"Small wobbles don't count — only a change in band means something."* | ✅ jsdom: one reject → `alignW 55 → 47`, band unchanged → `_MOVE === null`, strip hidden |
| **E-D179-10** | The word budget bites (a rise, a fall, an ask **and** a reliability move in one run). | The fitter drops **only optional clauses**, in a fixed order (`released` → `asked`). **It can no longer cost the user a number or a transition** — the counts are in Progress and the movement is on the ramp; **neither is in the strip to be trimmed.** | ✅ `_assertPayoffWithinBudget()` — rise **and** fall, both ≤20 words, both retaining act · transition · limiter · `dimMoves` |
| **E-D179-11** | A **guard** runs the real Extended pass **after the user has already applied a fix.** | `applyFix()` **mutates `READ.provisional`** — so the Fast-Pass read no longer exists in the model, and the deep-pass guard had nothing honest to run from. **It was ORDER-DEPENDENT: green at boot, red after one fix.** A guard defect, not a product one. **Fixed:** `_READ0` freezes the read at load; the guard stages it and restores the live one. | ✅ **found by the D179 behavioural harness.** Self-check is now **83/83 green after** applying a fix, answering a clarification **and** running the deep pass, in any order |
| **E-D179-12** | The **chat opening turn** on an issue that **only exists after the Extended pass** (ISS-07). | `_assertChatOpeningIsShort()` graded only the issues that **exist** — so **the newest copy in the product was the one copy no budget guard could see**. ISS-07's opening ran to **57 words against a budget of 50**. **Fixed both ends:** the sentence is split (nothing lost), and the guard now **stages `DEEP_FINDINGS` into `ISSUES` for the length of the check**. | ✅ **found by the D179 behavioural harness.** ISS-07 opening now **≤50 words**; the guard refuses to run if a deep finding is not being graded |

---

# D180 — Progress: grounding, not clearing (edge cases)

| # | Edge case | Behaviour | Proof |
|---|---|---|---|
| **E-D180-1** | The user **grounds an artifact** while a deeper read **finds more issues** (the D180c property). | **GROUNDED rises. Issues rises. Neither is a verdict.** Same class, same weight, same colour on both arrows. **Nothing in the panel treats the rising issue count as a regression.** | ✅ `_assertGroundingRisesWhileIssuesRise()` — a **state proof** (real basis flip + real issues added), plus the must-not-fire control |
| **E-D180-2** | Someone re-adds **"Artifacts read 7 / 7"** — this time **through the registry**, computed by a `get()` that returns `PLAN_SECTIONS.length`. | **RED.** It is computed and **it can never move**, and a value that cannot change is **not progress** — it is decoration. **The guard perturbs state and requires every row to move.** | ✅ NC-D180-01 `theDefect_aConstantReturnsAsProgress` |
| **E-D180-3** | A **completion percentage** / a **bar toward zero** / an **ARIA progressbar** / a **cascade fill** lands on Progress. | **RED, on all four doors.** *Progress is grounding, not clearing.* | ✅ NC-D180-03/04/05/06 |
| **E-D180-4** | *Resolved* is given a **denominator** (`Issues resolved 1 / 8`). No %, no bar, no "remaining". | **RED.** A count of what is outstanding printed over a total **is a burndown in three words** — it names a total to reach. | ✅ NC-D180-07 `theSubtleBurndown_aDenominatorOnResolved` |
| **E-D180-5** | The **rising issue count is painted red** (`.pg-d` on the issues chip gets `--danger`). | **RED.** **This is the doctrine leak**: more issues would read as "worse", and confidence would read as health. | ✅ NC-D180-08 `theLeak_theRisingIssueCountIsPaintedAsBad` |
| **E-D180-6** | GROUNDED stops being read off the **basis** (its `get()` returns a number). | **RED** — twice: the property guard (it no longer rises when the user grounds) **and** the constant guard. | ✅ NC-D180-09 `theLeak_groundingDoesNotRise` |
| **E-D180-7** | A user **answers a question** that the analysis update has not yet resolved. | The question **leaves OPEN and appears under CLOSED** the moment it is answered — **never counted in both rows at once** (counts have one home). The issue itself stays honestly at **Addressed** until the update lands (D088). | ✅ jsdom: `_submitClarification()` → questions 2 → 1 ↓1 · answered 0 → 1 ↑1 |
| **E-D180-8** | The Progress panel is **empty** (no counts rendered). | **The guards FAIL to verify** — they do not pass for free. A missing subject is a failure (D166 §1). | ✅ NC-D180-11 `c_refusesToGradeNothing` |
| **E-D180-9** | **The panel with NO previous run** (first read, no analysis update yet). | **No deltas, and no legend.** A value OSLO cannot compute is not shown (D173) — the arrows appear only once there is a run to compute them against. | ✅ boot render: 25 words, zero arrows |

# DL-109 — Provenance (edge cases)

| # | Edge case | Behaviour | Proof |
|---|---|---|---|
| **E-DL109-1** | A deeper read **infers more** (11 → 13 load-bearing) **while the band rises**. | **Not a regression, and drawn as none.** Same class, same weight, same colour on every arrow; no regression and no debt vocabulary. **Six guards must stay GREEN through it.** | ✅ `_assertRisingInferenceIsNotARegression()` · **must-not-fire `mustNotFire_moreInferencesAndAHigherBand`** |
| **E-DL109-2** | Someone re-admits **AE-06** — *"Understanding debt: 9 items owed. Pay down your assumptions."* | **RED**, whichever door it comes through: copy · **painter** · **registry label** · **cascade class**. | ✅ NC-D109-01/02/03/04 |
| **E-DL109-3** | The **load-bearing count is typed in** (`get(){ return 9; }`). It still looks right on screen. | **RED, twice** — the state proof (bind an inferred item to a critical issue and it must move) **and** the constant proof. **A copy scan would never have caught this.** | ✅ NC-D109-05 `theNumberIsTyped_bites` |
| **E-DL109-4** | The **user grounds the flagged artifact** (Scope). | **The finding is legitimately gone**, and the flag with it. **The guard does NOT call that a failure** — it constructs the condition itself and proves the mechanism in **any** state. *(The first draft of this guard went red here. **A guard that punishes the user for doing the work is a guard defect** — D166 §3.)* | ✅ `_assertInferenceMapNamesTheArtifact()` (constructed both ways) |
| **E-DL109-5** | The Inference map's **proportion becomes a bar** — a cascade `width:64%`, or the pips stop matching the model. | **RED.** **The mark IS the item.** If the drawing and the count can disagree, the drawing is a bar wearing pips. | ✅ NC-D109-07 · NC-D109-09 |
| **E-DL109-6** | **The inferred pip goes amber.** | **RED.** **Guessing is not a failure state** (D003) — and this is exactly how the debt frame would arrive **as a colour**. | ✅ NC-D109-08 `inferenceIsPaintedAsSeverity_bites` |
| **E-DL109-7** | The false-confidence flag is **hard-coded** to name Scope — or is **silent** while an artifact qualifies. | **RED both ways.** An authored finding is a fabricated one (D173); silence on the most dangerous condition in the plan is the reason the surface exists. | ✅ NC-D109-10 · NC-D109-11 |
| **E-DL109-8** | The Readout's assumptions are **re-pointed at open clarifications**. | **RED.** An issue is not an assumption. Proven **both ways**: the register must reach the section, and a clarification must not. | ✅ NC-D109-12 `theProxyReturns_bites` |
| **E-DL109-9** | A **`derived_from`** appears on a ContextItem (an inference chain, smuggled in). | **RED.** Lineage is **not in the schema** and is an **owner-open decision** (DL-109 §5). Also caught: any field pointing at another ContextItem id, and any painter that computes a *depth*. | ✅ NC-D109-13 `theChainIsSmuggledIn_bites` |
| **E-DL109-10** | **All assumptions get confirmed.** | The register renders *"OSLO has not had to assume anything here."* **The list shrinking is not a burndown** — there is no total, no percentage and no target, and **some assumptions never need validating at all** (§3c). | ✅ `_infAssumHTML()` empty state |
| **E-DL109-11** | An artifact has **no extracted items**. | **No row is drawn.** An empty row is not a zero — and a zero-length bar is a magnitude claim OSLO cannot make. | ✅ `_infMapHTML()` |
| **E-DL109-12** | A count cannot be computed. | **It is not shown, and it is never invented** (D173). Structural facts filter themselves out; a registry row whose `get()` returns `null` never reaches the snapshot. | ✅ `_assertEveryPayoffCountIsComputed()` · `_assertProgressCountsAreComputed()` |

# D181 — "Load-bearing" = the read points at it · Age the clock, not the past (edge cases)

| # | Edge case | Behaviour | Proof |
|---|---|---|---|
| **E-D181-1** | ⭐⭐ **An artifact READS STRONG and most of it is OSLO's** (Scope: 3 grounded · 4 inferred, High reliability, nothing critical open). **No critical issue cites its inferences. No issue on the limiting dimension rests on them.** | **THEY ARE LOAD-BEARING — clause (c).** *Scope reads fine **because of** four things OSLO made up.* **An inference is load-bearing in two ways: it supports a WARNING, or it supports a REASSURANCE — and the reassurance is more dangerous, because nobody is looking at it.** The strict reading would have said "not load-bearing" and **missed FALSE CONFIDENCE entirely.** | ✅ `_assertLoadBearingIsComputed()` — the flagged artifact's inferences are graded **by identity**, live **and** constructed |
| **E-D181-2** | An inference **bears on the limiting dimension** (`dim:'Feasibility'`) and **nothing points at it** — no critical issue, no open issue, no flagged artifact (CI-45 · CI-56 · CI-57). | **NOT COUNTED.** It is an inference; it is **not holding anything up.** *(This is the over-count the LOOSE reading shipped: **11**.)* | ✅ state proof: push such an item → the number **must not move**. NC `theLooseReadingReturns_bites` |
| **E-D181-3** | ⛔ **Clause (c) is deleted** ("simplify it back to the strict reading"). | **RED.** Scope drops out of the number, and the product goes blind to false confidence. **Clause (c) is non-negotiable** (D181a). | ✅ **NC `clauseC_isDeleted_bites`** |
| **E-D181-4** | ⛔⛔ **THE USER GROUNDS THE FLAGGED ARTIFACT.** Deep pass → load-bearing **20**; the user confirms Scope → **12**; clause (c) → **0**; the flag goes. | **THE FALL IS THE USER'S SUCCESS.** Clause (c) legitimately stops firing — the artifact rests on **them** now, not on OSLO. **No guard may call this a failure** (D166 §3 — one in this file once did). **Every DL-109 guard stays GREEN.** | ✅ **must-not-fire `mustNotFire_groundingTheFlaggedArtifactRetiresClauseC`** · the clause-(c) proof is **CONSTRUCTED**, so it holds in **every** state |
| **E-D181-5** | The limiting dimension **moves** (Feasibility → Clarity), or a critical issue **resolves**. | **The number moves with it.** Every clause reads live state; there is nowhere to type a number in. | ✅ `_assertLoadBearingIsComputed()` · NC `theNumberIsTyped_bites` |
| **E-D181-6** | ⭐ **`simNextWeek()` — the clock advances three weeks.** | **The assumptions AGE.** *"Unvalidated for 2 minutes"* → ***"Unvalidated for 3 weeks · 1 issue depends on it."*** The timeline's Initial run reads *"3 weeks ago"* — **the same clock, so no two surfaces disagree.** **Demonstrated, not asserted.** | ✅ `_assertSimNextWeekAgesTheAssumptions()` — the age must **grow, in weeks, ON SCREEN** |
| **E-D181-7** | ⛔ **Somebody ages the DEMO PROJECT instead of the clock** (back-dates `_RUN_AT.Initial` three weeks for a nicer screenshot). | **RED. D100 dies quietly** — Slice 7's first-run state assumes the project is new, and **it genuinely is.** *"A small lie told to make a surface look better."* | ✅ **NC `thePastIsBackDated_bites`** · `_assertD100FirstRunStateHoldsAtWeek0()` |
| **E-D181-8** | The age becomes a **typed constant** (`_ciAgeMs → 3 weeks`). It looks identical at week 0. | **RED, twice** — the ageing state proof **and** the D100 week-0 proof. **Only a state proof catches this.** | ✅ **NC `ageingIsTyped_bites`** |
| **E-D181-9** | A week passes and **the user did nothing.** | **Velocity reads 0 · 0** — *understanding is stalling*, honestly stated. **A direction, never a target**; there is no total, no denominator, and no zero to reach. **An empty week is information, not a number to hide.** | ✅ inside `_assertSimNextWeekAgesTheAssumptions()` |
| **E-D181-10** | ⚠️ **A vocabulary scanner goes red on the demo plan's own prose** — *"the report the sponsors are **owed**"* (a sponsor deliverable in the sample project). | **The scanner is wrong, not the copy.** DL-109 §3 bans framing **INFERENCE** as a debt. `_assertNoDebtVocabulary()` scans **the surfaces where OSLO frames its own epistemic state** (Progress · the Inference map · the registry · the painters · the cascade) — **and it is word-boundaried** (`lowest` ≠ *owes*, `reliability` ≠ *liability*). **Do not edit correct product copy to satisfy a naive scanner** (D166 §3). | ✅ `_assertNoDebtVocabulary()` green; word-boundary controls verified |
| **E-D181-11** | ⚠️ **PRE-EXISTING DEFECT, found by the D181 behavioural harness** (not by the boot guards): the user **acts on** the deep-pass Scope issue, then asks chat about it → the opening turn is **55 words against a 50-word budget** (D167). It never showed at boot **because no issue is `addressed` at boot**. | **Copy fixed, budget untouched.** *"You've acted on it — it's Addressed, waiting on an analysis update"* **restated itself** — exactly the second sentence D163 bans. Now: ***"Addressed — waiting on an analysis update."*** **The honest label survives, once, short.** | ✅ `chatOpeningShort` green through the full act-then-ask sequence |

---

# D182 — THE PROBE FENCE. Edge cases. (owner-reported P1, 2026-07-13)

> **A PROBE MUST NEVER BE ABLE TO TOUCH THE USER.** Third leak of this class: a probe retired a live chat answer
> box · a probe corrupted the append-only History · a probe fired an upgrade prompt at a user who did nothing.

| # | Edge case | Behaviour | Guard |
|---|---|---|---|
| **E-D182-1** | ⛔⛔ **THE P1.** The negative controls monkeypatch `sendMemo` / `_rptCommit` (the readout's **autosave** path) to call `fireUP('UP-REPORT')` to prove the guard bites — and `_assertSharingIsFreeOnEveryTier()` then calls the **real** `sendMemo()`. | **Reproduced: 34 live prompts raised, the user's `promptLog` spent, and an entry left in the deferral queue.** Now the prompt is **computed and recorded** (the guard still grades the mechanism) but **blinded by `html[data-probe]`, never persisted, never queued.** | ✅ `_assertNoProbeCanRaiseAPrompt()` · **NC `aProbeThatFiresAPromptAtTheUser_bites`** |
| **E-D182-2** | ⛔⛔⛔ **THE DELAYED BOMB — the owner's symptom, exactly.** A probe DEFERS a prompt (GUARD 1 / GUARD 2). The queue drains **on a 400 ms timer**, long after the guard finished and restored everything. The prompt lands on the user **with no attempt behind it.** | `_deferUP()` is a **no-op under the fence** — the deferral is recorded, never enqueued. Same for the two **asynchronous** fire paths (`_markFirstValue` → UP-8, `_maybeUP7` → UP-7), which escape a synchronous fence entirely. | ✅ **NC `aProbeThatArmsTheDeferralQueue_bites`** |
| **E-D182-3** | ⛔ **THE `"null"` BUG.** `LS.set(k, LS.get(k, null))` does **not** undo a write — a key the user never had comes back as the literal string `"null"`. `_upAffordanceTable()` was **inventing five localStorage keys** in every user's browser. | `_lsFullSnap()` / `_lsFullRestore()` — **raw, byte-for-byte. Absent means ABSENT.** | ✅ `_assertProbeLeavesNoResidue()` |
| **E-D182-4** | ⛔⛔ **THE CHILD COUNT WAS A LIE.** `_assertDeepPassMovesBandAndCounts()` restored the chat by trimming `scroll.children` back to a saved count. But `pushChat()` **replaces the first-run empty state** — one child out, one child in. **The count never moved, the trim never fired, and the probe left an OSLO message in the user's chat** (and `_chatPersist()` wrote it to their storage). | Restored **byte-for-byte** (`innerHTML`), never by arithmetic. | ✅ `_assertProbeLeavesNoResidue()` — **it found this; review did not** |
| **E-D182-5** | ⛔⛔ **THE RETURNING USER.** The boot guards ran clean — *because at boot `firstMRI` is false and `fireUP()` refuses at GUARD 1.* A **returning user has first value**, so the prompts actually **route**, and `_upFired()` wrote `promptLog` / `promptSeen` **on every boot**. The user's nudge budget was being eaten by the test suite — and **the next prompt they actually earned would be swallowed by a cadence guard**: D170's P1, arriving by a second road. | `_upFired()` and the month-cooldown write are **no-ops under the fence**. **A probe's prompt is not the product's prompt.** | ✅ boot matrix now includes **first-value = YES** |
| **E-D182-6** | A guard is nested inside another guard (`_s10SelfCheck` → `_assertX` → `_upAffordanceTable`). | The fence is **re-entrant**: one snapshot, one restore, at depth 0. Balance is guarded. | ✅ **NC `mustNotFire_theFenceIsBalancedAndTheBlindTracksIt`** |
| **E-D182-7** | ⚠️ **THE SELF-MATCH.** `_assertNoProbeCanRaiseAPrompt()`'s clause (d) scans every guard for source that lowers the fence — and its **own source necessarily contains the forbidden pattern, in order to look for it.** First run, **the guard failed on itself.** | Patterns built with `new RegExp` from fragments; the scanner is **excluded from its own scan**. **A scanner must not be its own subject.** (Same shape as D183d, where a guard's own *comment* tripped its vocabulary scan.) | ✅ fixed; boot green |
| **E-D182-8** | ⛔ **THE FENCE MUST NOT GAG THE PRODUCT.** A fence that silenced D138 would have traded one P1 for a worse one. | **A real user attempt still raises a real prompt**, naming the limit and the relieving tier. Verified by firing `genReport('link')` on Free **outside any fence**. | ✅ **must-not-fire `mustNotFire_aRealUserAttemptStillPrompts`** |
| **E-D182-9** | A future guard learns to switch the fence off (`_PROBE.depth = 0`, or simply is never fenced). | **Every probe is fenced BY CONSTRUCTION** (`_fenceEveryProbe`, run at boot before any probe). **There is exactly ONE exemption and it is NAMED** — `_d182NegativeControls()`, the suite that grades the fence and must stand outside it. Clause (d) fails if a second exemption appears or any `_assert*` lowers the fence by hand. | ✅ `_assertNoProbeCanRaiseAPrompt()` clause (d) · **NC `aFenceThatGradesNothing_bites`** |

---

# D183 — COPY AND POSITIONING. Edge cases. (owner, 2026-07-13)

| # | Edge case | Behaviour | Guard |
|---|---|---|---|
| **E-D183-1** | ⛔⛔ **THE READOUT MUST KEEP SAYING "I".** **D152 REQUIRES the first person there — it is the PM speaking, under their own name** (*"I'm getting the network commitment in writing this week"*). A naive first-person sweep would have **broken a ratified decision to satisfy a copy rule.** | The D183a guard's subject is **OSLO's own voice** — a registry of the panels OSLO writes. It does **not** reach the readout body, the chat thread, or the user's own controls (*"Yes, delete my account"*, *"Shared with me"*). **A copy rule that cannot tell WHO IS SPEAKING is not a copy rule.** | ✅ **must-not-fire `mustNotFire_thePmStillSaysIInTheReadout`** |
| **E-D183-2** | ⛔ **THE CHAT MUST KEEP SAYING "I".** Chat is a **conversation**; D165's whole shape depends on it. | Excluded from the scan by construction. | ✅ **must-not-fire `mustNotFire_theChatStillSaysI`** |
| **E-D183-3** | ⚠️ **WORD BOUNDARIES.** *"**AI**-first PM"* contains **I**. *"so**me**"* contains **me**. A first-person scan without boundaries goes red on correct copy. | `_FIRST_PERSON` is anchored on `[^A-Za-z’']` both sides. **Third time this class has been named** (DL-109 §3: *"lowest"* contains *"owes"*, *"reliability"* contains *"liability"*). | ✅ verified against `AI-first` in the landing copy |
| **E-D183-4** | ⚠️ **A GUARD'S OWN COMMENT TRIPPED ITS OWN VOCABULARY SCAN.** `_assertInferencesAreCalledInferences()` scanned `String(_progressHTML)` for *"things"* — and `_progressHTML`'s **comment explains the rule and therefore quotes the banned word.** | **The copy, not the commentary** (`_edCodeOf` strips comments — every vocabulary guard in the file uses it for exactly this reason). Same lesson as **E-D182-7**. | ✅ fixed; boot green |
| **E-D183-5** | ⛔⛔⛔ **"OUTCOME CONFIDENCE 62/100" IS THE FORBIDDEN FORECAST, ARRIVING THROUGH THE LABEL.** Adopting canon's label makes the number *more* dangerous, not less: it reads as *"62% likely to hit your outcome."* | **The label is adopted and the number is DELETED** — hero, pill, popover, chat, reports. The **ramp** cannot be misread: an ordinal band on a five-step scale is not a probability. Closes D173d / DL-062-F1: **DELETE.** | ✅ `_assertNoZeroToHundredIndexAnywhere()` (DOM · render paths · snapshot · **cascade**) |
| **E-D183-6** | ⛔ **THE WORSE TRADE:** delete the number and quietly lose the **label** too. | The guard's non-vacuity clause **requires the pill to say "Outcome Confidence"**. A guard that policed only the number would have let the positioning rot. | ✅ **NC `b_theLabelRegressesToPlainConfidence_bites`** |
| **E-D183-7** | ⛔ **THE STUTTER.** *"Confidence 62 MODERATE \| Moderate reliability"* — **two different things wearing the same word.** | Grounding speaks its own vocabulary — *barely / thinly / partly / largely / well grounded* — which **shares no token** with `_BANDORD` (*Very Low / Low / Moderate / High / Very High*), checked on **word boundaries**. **Confidence = how MATURE the read is. Grounding = how much of it is REAL.** | ✅ `_assertGroundingSharesNoWordWithTheBand()` |
| **E-D183-8** | The grounding word is **typed**, not computed — ground every claim and it does not move. | It is derived through **`_ciEvidenceId()`**, the one function every provenance count on every surface goes through. **State proof:** ground every inferred claim and the word must move. ⚠️ The first cut of the proof mutated `evidence_id` — **the wrong field.** `_ciEvidenceId()` reads **`it.ev`**. The probe proved nothing and passed. Fixed. | ✅ **NC `c_aTypedGroundingWord_bites`** |
| **E-D183-9** | ⛔ **THE WORD BUDGET ALMOST BOUGHT ITSELF ROOM BY HIDING THE ASK.** The grounding transition (*"Grounding: thinly → largely grounded."*) is longer than *"Reliability: Moderate → High."* and blew the 20-word payoff budget — so `_payoffFit()` **dropped the ASK**, and D178 went red. | **The grounding transition now drops BEFORE the ask.** Grounding is *also* readable in Progress (your evidence vs OSLO inferred, both counted); **the ASK is the one thing the user can act on next** (D178). **Order matters, so it is stated, not assumed.** | ✅ `_assertDeepPassMovesBandAndCounts()` — **it caught this** |
| **E-D183-10** | The `DOWNGRADE_NEVER_TAKES` list is **user-facing copy** (Plans renders it) *and* a guard's subject. | It says **documents**; the guard's expected vocabulary moved with it. **The rule is unchanged — the record is never taken** (D128). | ✅ `_assertDowngradeNeverRemovesUnderstanding()` |
| **E-D183-11** | ⛔ **"Section" is NOT always a plan document.** The **readout's** §1–§5 and *"Optional sections"* are the readout's own sections — not plan documents. Sweeping them would have been wrong. | D183e's scan targets **"artifact"** in user copy; the readout's sections are untouched. | ✅ `_assertPlanDocumentsAreCalledDocuments()` |
| **E-D183-12** | ⛔ **A RISING INFERENCE COUNT IS STILL NOT A REGRESSION** (D177 / DL-109 §3). The *"things" → "inferences"* rename must not smuggle in a debt frame. | Unchanged. **Progress goes UP while the issue count goes UP.** | ✅ **must-not-fire `mustNotFire_moreInferencesIsStillNotADebt`** |

## D184 / D185 — consent, and the panel that lectured (owner, 2026-07-13)

| # | Edge case | Handling | Proof |
|---|---|---|---|
| **E-D184-1** | ⛔⛔⛔ **AN APPLY BUTTON WITH NO FIX ON SCREEN.** The button asked *"apply?"* about a change the user had never seen. **OSLO is advisory-only (D001): advice the user cannot see is not advice — it is an instruction.** | The recommendation is **resident, above the button, in the same block**; the button's label is **built from the same object**. A button whose subject is off screen **cannot be constructed**. | ✅ `_assertApplyAffordanceShowsItsRecommendation()` |
| **E-D184-2** | ~~**THE BUTTON DID NOT SAY *WHICH*.**~~ **SUPERSEDED BY D190a (owner, same day).** The label that named the change read *"Apply: Confirm the venue's 500-person Wi-Fi capacity…"* — **truncated before it could be read.** | The affordance is **short and constant** — **"Apply this fix"** — and **the fix above it is the subject.** The **consent** clause (the fix is visible, out of a drawer, above the button) is **untouched**. | ✅ **NC `theLabelEatsTheFix_bites`** · **NC `theLabelVariesWithTheSubject_bites`** |
| **E-D184-3** | ⛔ **NO RENDERABLE RECOMMENDATION.** A blank/absent `rec`. | **NO BUTTON.** `_primaryRec()` returns `null` and the block is not built — **an action whose subject is absent is REMOVED, not degraded** (D173, applied to actions). | ✅ state proof inside the guard + **NC `aButtonWithNoSubjectSurvives_bites`** |
| **E-D184-4** | ⚠️ **THE RANK MUST NOT BE AN INDEX.** (The guard that hard-coded `ISSUES[0]` is why.) | `_recRankScore()` reads the **live limiting dimension**, the user's selection, and whether OSLO can draft it. Across issues, `_leadRecId()`. **State proof:** make Alignment the limit → the lead moves **ISS-01 (Feasibility) → ISS-05 (Alignment)**. | ✅ `_assertRecommendationRankIsComputed()` + **NC `theRankBecomesAFixedIndex_bites`** |
| **E-D184-5** | ⛔ **DL-103 §7d MUST GET *MORE* TRUE, NOT LESS.** The assisted-apply cap may never hide the recommendation. | The recommendation now lives **outside every disclosure row**, so the cap **cannot** hide it by construction. The cap opens **`#ipAlts`** — the *Other options*, under the recommendation — where **the free manual door** lives (D190c moved the door; the cap's reach moved with it). | ✅ **must-not-fire `mustNotFire_theCapStillNeverHidesTheRecommendation`** + `mustNotFire_manualEditingIsStillFree` |
| **E-D184-6** | ⚠️⚠️ **GUARD DEFECT (caught by its own NC, not by review).** The first form of the D184 guard only asked *"is the recommendation in the same block?"* — and a `.ip-rec` **pushed back inside a disclosure row** still satisfied it. | The guard now also asserts the recommendation **is not inside a `.ip-row` at all**. **A change the user must EXPAND to read is a change they cannot consent to.** | ✅ **NC `theFixGoesBackIntoAClosedDrawer_bites`** — it went RED, exactly as designed |
| **E-D184-7** | ⚠️ **THE PERSISTED CHAT THREAD IS A RECORD, NOT A LIVE SURFACE.** A guard that reddened on a reply OSLO gave last week would be **grading the past, not the product.** | The DOM scan skips `#chatscroll`; the chat's apply affordance is graded on its **RENDER PATH** instead (a detached reply, same eyes) — the stronger subject anyway. | ✅ guard clause (5) |
| **E-D185-1** | ⛔⛔⛔ **THE PANEL DEFENDED ITSELF.** ~90 words of prose, ~10 words of information. Three paragraphs, each justifying the row above it. | **Deleted, not shortened.** Every explanation moved **behind the ⓘ** (DL-107 / D159). Resident prose: **12 words**, budget 25. | ✅ `_assertConfPopIsAReadout()` + **NC `aNarrationParagraphReturns_bites`** |
| **E-D185-2** | ⛔ **HONESTY BY AMNESIA.** Deleting the doctrine outright would pass a word count and fail the user. | The guard's clause (4) **requires the ⓘ to still carry it**. Strip the tips → RED. | ✅ **must-not-fire `mustNotFire_theDoctrineIsStillOneTapAway`** |
| **E-D185-3** | ⛔⛔ **`data-cpp-state` COULD HAVE BECOME A HIDING PLACE** — tag a paragraph as a "state token" and it escapes the prose budget. | The **sentence clause grades ALL resident text, tagged or not.** A resident sentence of more than 8 words is an explanation, wherever it hides. | ✅ **NC `aParagraphTaggedAsStateToDodgeTheBudget_bites`** |
| **E-D185-4** | ⛔ **THREE ROWS ALL READING "MODERATE" IS A TABLE WITH NO INFORMATION IN IT.** | The resident line surfaces **what is THIN** (*"Thinnest: Evidence — Low."*), ranked from state; the three rows are **on demand**. | ✅ `_assertReliabilityBasisSurfacesTheWeakest()` |
| **E-D185-5** | ⛔ **A TIE HAS NO WEAKEST — AND OSLO DOES NOT INVENT ONE** (D173). Today all three basis dimensions genuinely read *Moderate*. | It says the one true thing: **"Even across the basis — Moderate."** Two dimensions tied at the bottom → **both** are named. | ✅ guard clause (b) + **NC `aTieInventsAWeakest_bites`** |
| **E-D185-6** | ⛔ **A WORD BUDGET MAY NOT DELETE A DISCLOSURE.** The D052 false-confidence flag is a **disclosure**, not decoration. | The flag still fires when the condition holds; its **body states the condition and stops** (*"Band High · reliability Low. Thin evidence, not a strong plan."*), and its paragraph moved to the flag's own ⓘ. The **Overview card keeps the fuller disclosure** — it is a reading surface, not a 300 px console. | ✅ **must-not-fire `mustNotFire_theFalseConfidenceFlagStillFires`** |
| **E-D185-7** | ⚠️⚠️ **GUARD DEFECT (first run).** The sentence clause **joined every resident text node** and split on punctuation — so the panel's LABELS ("Outcome Confidence", "Stage", "Clarity", "High"), which carry no full stops, **fused into one 20-word "sentence"** and the guard reddened on a readout doing exactly what it was told. **A label is not a sentence.** | Sentences are graded **within each text node**, which is where a sentence actually lives. (D166 §3: fix the guard, never the doctrine.) | ✅ boot green |
| **E-D185-8** | ⚠️⚠️ **GUARD DEFECT (first run).** The rank guard scanned `_leadRecId`'s source for `[0]` — and `_leadRecId` **ends in `.sort(…)[0]`**, which is the *correct* way to take the head of a **computed** ranking. **The guard called the fix a defect.** | The subject is not "an index anywhere" — it is **a fixed index into the issue set** (`Object.keys(ISSUES)[0]`), which is the shape of the bug that shipped. | ✅ boot green; NC still bites |
| **E-D186-1** | ⛔⛔⛔ **THE PHRASE MEANT THE OPPOSITE OF WHAT IT LOOKED LIKE.** *"20 inferences are holding it up"* — **the owner, who wrote the doctrine, read it as *"delaying it."*** It means **SUPPORTING** (D181: the read POINTS AT them). *"Hold up" is ambiguous in English (support / delay), and it sat on the single most valuable number in the product.* | **Label → `YOUR READ RESTS ON`. Copy → *"N inferences your read rests on."*** Swept from **every** surface: the hero limiter, the CAF tip, the how-calculated list, the Assumptions caption, the assumption tag, the readout tag + empty state, the chat capability list, the chat read answer, the count registry's `word`, and the guard messages. | ✅ `_assertNoHoldingItAnywhere()` — DOM · **attributes** · **TOUR registry** · **`PN_SLOTS` notes** · **render paths** + **NC `d186_theDeadPhraseReturns_bites`** |
| **E-D186-2** | ⛔⛔ **"BLOCKER" WOULD HAVE BEEN WORSE THAN THE AMBIGUITY.** It tells the user to **REMOVE THE THING CARRYING THEIR PLAN** — and on the CAF limiter it reads as *"the PROJECT is blocked"*, importing the health framing **D003 forbids**. | **The word does not appear anywhere.** The CAF limiter stays a **limiter**, in D185.4's form: *"Feasibility — the lowest. Ground it to lift the read."* **ESCALATED — the owner may override; it will not be done silently.** | ✅ guard clause (g) + **NC `d186_blockerArrives_bites`** |
| **E-D186-3** | ⛔ **DELETING THE PHRASE COULD HAVE TAKEN THE NUMBER WITH IT.** Honesty by amnesia is the worse trade. | Guard clause (f) **requires the replacement to be on screen** — the row, its count, and the words *"rests on"*. | ✅ **NC `d186_theNumberIsDeletedNotRenamed_bites`** |
| **E-D186-4** | ⚠️⚠️ **THE TOUR IS COPY NO DOM GUARD CAN SEE.** A tour step is a string in a registry until it is spotlit — and the last sweep found the tour still **teaching** a deleted concept. | The guard reads `TOUR` **and** `PN_SLOTS` directly. | ✅ **NC `d186_theTourStillTeachesIt_bites`** |
| **E-D187-1** | ⛔⛔⛔ **RED WOULD LIE.** *"Issues 8 ↑2"* after an Extended analysis **does not mean the plan got worse — it means OSLO looked harder.** Same for *inferences ↑* (D177) and *critical ↑* (an issue **found** is an issue **avoided**). | **NO RED TOKEN EXISTS** on any progress/trend surface — proven in the **cascade**, where it would be written. There is no `red` field in `COUNT_VALENCE` to set. | ✅ `_assertTrendColourIsEarnedOnly()` clause (2) + **NC `d187_aRedRuleAppears_bites`** |
| **E-D187-2** | ⛔⛔ **`grounded` IS NOT GREEN — AND THAT SURPRISED US.** *"Claims on your evidence"* rises when the user confirms a document, **but it ALSO rises when a deeper read extracts a NEW claim from a document already confirmed** (`_ciEvidenceId()` reads the artifact attestation, so `CI-69` on Resources is born grounded). **OSLO can move it without the user.** | **NEUTRAL.** The green D187 names — *"you grounded"* — is the week's **grounding velocity** (`_ciVelocity().you`), gated on `_ATTEST_AT[art]`: **it cannot be non-zero unless the user attested something.** That one is honest, and earned. | ✅ `COUNT_VALENCE.grounded.why` states the mechanism; guard clause (1) rejects it as user-driven |
| **E-D187-3** | ⛔ **A FALL ON A USER-DRIVEN COUNT.** An issue is re-opened; `resolved` falls. | **Neutral.** Green is for a **RISE** the user earned. A fall is never a prize — and never a punishment (D173c). | ✅ guard clause (5) |
| **E-D187-4** | ⛔⛔⛔ **THE SEVERITY TOKEN WEARING A DIFFERENT NAME.** A guard that graded the token *name* would sail straight past `--earned: #4D8B6B`. | **The guard grades the CHROMA**, resolved from the authored cascade **in both themes**, and requires real distance from `--success` (measured: **77.8** dark · **52.5** light). It reads the **last** `:root` declaration — because that is what the cascade does. | ✅ **NC `d187_earnedIsTheSeverityTokenRenamed_bites`** |
| **E-D187-5** | ⛔ **D180 MUST NOT REGRESS.** A read that FELL must never be drawn as one that ROSE. | The arrow is `delta > 0 ? '↑' : '↓'`; no movement ⇒ **no arrow**. The valence **colours**; it never **points**. | ✅ guard clause (4) + **NC `d187_theArrowIgnoresTheData_bites`** |
| **E-D187-6** | ⛔⛔ **D180c MUST NOT REGRESS.** The green may not split the issue and grounded deltas that D180c spent a whole guard making equal. | Both are non-user-driven ⇒ **both wear `pg-d`, byte for byte.** | ✅ **must-not-fire `mustNotFire_issuesAndGroundedStillDrawTheSame`** |
| **E-D188-1** | ⛔⛔ **THE STRIP HAD NO LABELS — IT HAD THREE CAPTIONS.** *"6 dependencies OSLO assumed, and nobody confirmed"* — ~8 words, three times, in a strip meant to be scanned in one pass. | **`NUMBER · LABEL`, ≤ 3 words:** **Unconfirmed dependencies · Unowned parties · Untraceable numbers.** The sentence is **behind the ⓘ** — *one tap away, not deleted* (DL-107 / D185). | ✅ `_assertStatLabelsAreLabels()` + **NC `d188_theSentencesReturnAsLabels_bites`** |
| **E-D188-2** | ⛔ **CONCISE BY AMNESIA.** Short labels with the consequence simply **lost**. | The guard **requires the ⓘ**, and requires it to say something (≥ 4 words). | ✅ **NC `d188_theConsequenceIsLostNotMoved_bites`** |
| **E-D188-3** | ⚠️⚠️ **GUARD DEFECT (caught by its own harness).** The label was extracted with `String.replace(info.textContent, '')` — which deletes the **first "i" anywhere**: *"Unconfirmed"* became *"Unconfrmed"*. **A scanner that damages its subject is not measuring it.** | The ⓘ **node** is removed from a clone; the string is never touched. | ✅ behavioural harness |
| **E-D189-1** | ⛔⛔⛔ **DL-107, FOR THE THIRD TIME.** *"A direction, not a target"* — the product narrating its own epistemics as a subheading. **Nobody reads a caption and forms a belief about targets; they read the number.** | **DELETED.** The rule it was narrating is enforced **in the code** (no denominator, no fill, no burndown) and explained **behind the ⓘ**. | ✅ `_assertNoPreemptiveCaptions()` (grades the **shape**, not the string) + **NCs `d189_theNarratingCaptionReturns_bites` · `d189_aDifferentPreemptionInTheSameSlot_bites`** |
| **E-D189-2** | ⛔⛔⛔ **A DISCLOSURE IS NOT NARRATION, AND A WORD BUDGET MAY NOT DELETE ONE.** | The sweep's subject is the **caption slot** (`.inf-note`) and **nothing else**. The all-clear still says *"not a guarantee of success"*; the D052 flag still fires; **O-SWEEP-1…4 stay ESCALATED, not swept.** | ✅ **must-not-fire `mustNotFire_theDisclosuresSurvived`** + the guard's own must-not-sweep clause |
| **E-D189-3** | ⚠️⚠️⚠️ **GUARD DEFECT #24 — FOUND, NOT INVENTED.** `_OSLO_VOICE_SURFACES` carried **`#issuePanel`** and **`#ipBody`**. The real id is **`issuepanel`**; there is **no `ipBody` at all**. *CSS id selectors are case-sensitive* — **both resolved to ZERO elements, and the Issue panel, the most-opened surface in the product, was NEVER graded for first person by any guard.** **The guard passed because it never looked.** | Selectors fixed; **the guard now FAILS if any surface in its list resolves to nothing** (*a selector that matches nothing is not a passing test — it is a blind one*); it **opens every issue under the probe fence** and grades the panel OSLO actually paints; and the **user's own controls** (*"✎ Write **my** own fix"*) are exempt **by declaration** (`data-voice="user"`), never by regex. | ✅ **NCs `d189_aBlindVoiceSelector_bites` · `d189_osloSaysIInTheIssuePanel_bites`** + **must-not-fire `mustNotFire_theUserStillSaysMyOnTheirOwnControl`** |
| **E-D189-4** | ⚠️⚠️ **GUARD DEFECT #25 — THE SAME FAMILY.** `_assertPlanDocumentsAreCalledDocuments()` **only ever graded what had been PAINTED.** `#limitsBody` is empty until `renderLimits()` runs, so a live **D183e** violation — *"**Plan artifacts** · History"* and *"**Artifacts** · History · asking for a read…"* in the **What's metered** modal — sat there for the whole build. Only a notes repaint ever exposed it. **An unpainted surface is not a clean surface.** | Copy fixed (**"Documents"**); the guard now **paints the deferred surfaces before grading them.** | ✅ boot green on **notes-ON** (where it had been failing) |


---

## D190 — the recommendation block, corrected (owner, 2026-07-13)

| ID | Edge case | Behaviour | Guard |
|---|---|---|---|
| **E-D190-1** | ⛔⛔ **THE LABEL EATS THE FIX.** *"Apply: Confirm the venue's 500-person Wi-Fi capacity…"* — the button repeating a change that is already **resident above it**: too long to scan, **truncated before it can be read**. | The affordance is the constant **"Apply this fix"** (chat: `Apply this fix →`), from **one reader** (`_applyLabel()`). **Measured across all six issues: one label, six different fixes.** | ✅ `_assertApplyAffordanceShowsItsRecommendation()` clause 3 + 3b · **NC `theLabelEatsTheFix_bites`** · **NC `theLabelVariesWithTheSubject_bites`** |
| **E-D190-2** | ⛔⛔⛔ **THE CONSENT CLAUSE MUST STILL BITE.** A shorter label must not become a *quieter* one: the fix must still be readable before it is appliable (D184/D001). | **Blank the recommendation → the guard goes RED.** The removal rule also holds: no renderable recommendation ⇒ **NO BUTTON**. | ✅ **NC `aButtonWithNoSubjectSurvives_bites`** (verified RED) · **NC `theFixGoesBackIntoAClosedDrawer_bites`** · **NC `theButtonClimbsAboveTheChange_bites`** |
| **E-D190-3** | ⛔⛔⛔ **THE SAME DOOR, OFFERED TWICE.** *"Other options (2)"* as a button in the recommendation block **and** as a disclosure row below Evidence — **two places, two registers.** | **ONE HOME.** The options expand **in place** (`#ipAlts`), **under the recommendation**. **The row under Evidence is deleted.** The panel's rows are now **Evidence · Clarification · Comments · Reviews**. | ✅ `_assertOptionsHaveOneHome()` (leaf-render count · declared opener · undeclared-handler scan · position) · **NC `aSecondOpenerForTheSameSet_bites`** |
| **E-D190-4** | ⛔⛔⛔ **THE FREE MANUAL DOOR LOSES ITS HOME.** The door (*"✎ Write my own fix in \<document\>"*) lived **in the row D190c deletes** — and it is **what the assisted-apply cap opens**. Lose it and the cap starts metering **understanding** (PROHIBITED, D126/D128). | The door moved **into `#ipAlts`**. **At the cap** (`_capHit('fixes')`): the recommendation is **visible and out of every drawer**, `#ipAlts` is **forced open**, the door is **on screen**, and **the user cannot collapse it**. | ✅ **must-not-fire `mustNotFire_atTheCapTheFixIsVisibleAndTheFreeDoorIsOpen`** + `_assertRecommendationNeverHidden()` |
| **E-D190-5** | ⚠️ **AN OPTION SET WITH NO ALTERNATIVES.** An issue with a recommendation but no `paths[]`. | `#ipAlts` renders **open**, carrying the **free manual door** alone; **no opener button** is drawn (there is nothing to open). *The free door is never conditional on OSLO having alternatives.* | ✅ guard clause 2 (non-vacuity: the guard skips issues with no alternatives and **FAILS if none exists anywhere**) |
| **E-D190-6** | ⚠️⚠️ **AA, SURFACED BY THE MOVE.** The options moved onto `--surface-2`. The **"Confirmed by you"** pill (3.74:1) and the **✓** tick (3.72:1) were **already below AA in dark theme** — `--success` is a fill green being used as text — and the move made it worse. | **Fixed, not shipped:** `--success-fg` (dark **#6FB894**, light **#3E7357**, unchanged) carries success **as text**. **`--success` is untouched**, so D187's `--earned` distance guard grades the same values. **AA: 5.61 / 5.58 dark · 4.63 / 4.66 light.** | ✅ contrast harness, both themes |

---

## D191 — a decision, once made, could not be unmade (owner P1, 2026-07-13)

**The four objects are not the same thing, and every edge case below turns on which one is being touched.**

| Object | Undoable? |
|---|---|
| **The SELECTION** | **YES — freely, including back to NO selection.** An intention, not an act. |
| **The APPLIED EDIT + the ATTESTATION** | **YES — and ALWAYS TOGETHER. Never separately.** |
| **The READ** | **NO.** Only an analysis update moves it. Withdrawing triggers a **NEW** read, it does not restore the old one. |
| **HISTORY** | **NO. APPEND-ONLY.** An undo is a **NEW EVENT**, never an erasure. |

| ID | Edge case | Behaviour | Guard |
|---|---|---|---|
| **E-D191-1** | ⛔⛔⛔ **THE ATTESTATION STANDS OVER TEXT THAT IS GONE.** The document is rolled back and *"Confirmed by you"* survives — OSLO asserting the user's word about a sentence that no longer exists. | **Impossible by construction.** `_withdrawUnit()` is the **only** function in the product that may drop an attestation, and it **cannot return without having restored the document too.** They are not two steps; they are one. | ✅ `_assertWithdrawMovesEditAndAttestationTogether()` — the **sweep** (nothing else touches an attestation) + the **run** (both halves moved) · **NC `theEditGoesButTheAttestationStands_bites`** · **NC `theAttestationGoesButTheEditStays_bites`** |
| **E-D191-2** | ⛔⛔ **RELIABILITY IS RE-DERIVED BY GUESSING.** The withdrawal "puts Reliability back" by inverting the rule that raised it. **Reliability is COMPUTED FROM ATTESTATION** — a wrong restore leaves the read resting on a number nobody earned. | **It is CAPTURED, before the fix moves it** (`_decision[id].relBefore` / `.basisBefore` / `.attestBefore`), and restored **exactly**. Never inferred. | ✅ guard clause *reliabilityRestored* (and it **declares itself vacuous** if Reliability did not rise on apply) · **NC `theReliabilityIsGuessed_bites`** |
| **E-D191-3** | ⛔⛔⛔ **THE WITHDRAWAL ROLLS THE READ BACK.** The most natural-feeling regression in this decision — and it would make OSLO's read **a thing the user can move by hand.** | **The withdrawal record holds NO band, NO CAF width, NO Confidence** — *you cannot roll a read back from a snapshot that does not contain one.* The hand-path leaves the read **byte-identical** (last-good, **D098g**); the **analysis update** re-reads the plan **as it now stands** and produces a **new run + a new trend point**. | ✅ `_assertNoHandPathMovesTheRead()` — the **record shape** + the **source scan** + the **state proof** · **NC `theWithdrawWritesTheReadDirectly_bites`** (routed through a helper, so the **state proof** must catch it) · **NC `theRecordStartsCarryingABand_bites`** |
| **E-D191-4** | ⛔⛔⛔ **THE WITHDRAWAL TIDIES UP ITS OWN HISTORY.** It deletes — or, subtler, *amends* — the event it withdraws. **A record you can edit is not a record.** | **HISTORY NEVER SHRINKS.** The withdrawal is a **new event** (`type:'withdrawn'`); the origin event is **never touched**, never relabelled, never removed. Measured: **1 → 4 → 7 rows** across apply → withdraw. | ✅ `_assertWithdrawalNeverShrinksHistory()` (byte-compares the origin event before/after) · **NC `theWithdrawalDeletesItsOriginEvent_bites`** · **NC `theWithdrawalRelabelsItsOriginEvent_bites`** |
| **E-D191-5** | ~~**A RESOLVED ISSUE OFFERS A WITHDRAW.**~~ ⛔⛔⛔ **SUPERSEDED BY D192a — AND IT WAS THE DEFECT.** *The analysis update resolves the issue ~1.9s after the apply, so barring the withdraw there made the attestation permanent again.* **See E-D192-1.** | — | — |
| **E-D191-6** | ⛔ **THE SELECTION CLEARS TO A DEFAULT.** *"No option chosen"* stops being representable, so the issue can never go back to a true Open. | **It clears to NOTHING.** `delete _selpath[id]` · `_istatus[id]='open'` · the decision record is dropped. **Open ⇄ Addressed is not a ratchet.** No meter moves, and **no analysis runs** — nothing in the plan changed. | ✅ `_assertEveryDecisionTransitionHasAnInverse()` (the **round trip**, driven on the live product) · **NC `theSelectionClearsToADefault_bites`** · **NC `theSelectionStaysAddressed_bites`** · must-not-fire `mustNotFire_clearingASelectionRunsNoAnalysis` |
| **E-D191-7** | ⛔⛔⛔ **A NEW ONE-WAY TRANSITION IS ADDED.** The next commit's version of this P1: a function that addresses an issue, or attests a document, with no way back. | **THE SWEEP.** Every function in the product is scanned for writers of `_istatus[…]='addressed'` and `.basis='attested'`. **Any writer not in `_ISSUE_TRANSITIONS`, with a declared and existing inverse, FAILS THE BUILD.** *(This is the clause that found `_submitClarification` — see O-D191-1.)* | ✅ `_assertEveryDecisionTransitionHasAnInverse()` clause 3 · **NC `aNewOneWayTransitionIsAdded_bites`** · **NC `aNewOneWayAttestationIsAdded_bites`** · **NC `theTransitionLosesItsInverse_bites`** (the P1 itself, put back) |
| **E-D191-8** | ⛔ **THE METER KEEPS THE MONEY.** The assisted apply is taken back and billed anyway — or refunded **silently**, which is a meter nobody can audit. | **Refunded AND recorded.** `_meterRefund('fixes')` decrements the count **and** increments `usage.refunds.fixes`, which renders on the Usage & Limits row: *"0 applied · no limit / **1 refunded (withdrawn)**"*. **The ANALYSIS RUN is not refunded — it really happened.** | ✅ `_assertWithdrawalRefundsTheAssistedApply()` · **NC `theAssistedApplyIsNotRefunded_bites`** · **NC `theRefundIsNotRecorded_bites`** |
| **E-D191-9** | ⚠️ **THE DOCUMENT HAS MOVED ON SINCE THE FIX.** ⛔ **RULED — D193a. The old behaviour (roll back to v2, taking the user's later edits with it) IS A DEFECT.** **See E-D193-1.** | — | — |
| **E-D191-10** | ⚠️ **TWO DECISIONS ATTEST THE SAME DOCUMENT.** ⛔ **RULED — D193b: refcounted by decision.** **See E-D193-3.** | — | — |
| **E-D191-11** | ⚠️ **THE MONTHLY ANALYSIS BUDGET IS REACHED WHEN THE USER WITHDRAWS.** | **THE WITHDRAWAL STILL STANDS** — it is the user's word, and the epistemic record is never metered (**D128 P1**). **Only the RUN defers**, and OSLO says so: the read stays at **last-good** (**D098g**) — *"it has not been rolled back, and it will not be: only an analysis update moves it."* Code-identical to `applyFix`'s deferred branch. | ✅ `_withdrawRun()` (the `_deepAllowed('user')` branch) |

---

## D192 / D193 — the erratum, and the two questions D191 exposed (owner, 2026-07-13)

> ## ⛔ **WITHDRAWING A FIX IS NOT HAND-MOVING THE READ.** It is the user **editing their own document and retracting their own word.** The **read** then moves **BY ANALYSIS** — which re-opens the issue, **because the gap is genuinely back.**
> ## ⛔ **OSLO MAY NEVER DELETE THE USER'S OWN WRITING.** (D152/D155 — the product does not touch the user's words.)

**The four objects, amended.** The READ is still not undoable, and HISTORY is still append-only. What changed is **which object the resolved-issue rule was binding**: it binds **the read**, not **the document**.

| ID | Edge case | Behaviour | Guard |
|---|---|---|---|
| **E-D192-1** | ⛔⛔⛔ **THE ATTESTATION BECOMES PERMANENT AGAIN ~1.9s AFTER THE APPLY.** D191 §5 barred withdraw on a **resolved** issue — and the analysis update **resolves the issue about two seconds after the fix is applied.** So the way out of *"Confirmed by you"* (on the user's own document, with **Reliability raised**) **lived for two seconds.** *In the ordinary happy path, D191 did not hold.* | **D192a — the resolved clause is GONE.** `_wdAvailable()` = *"is there a decision?"*, nothing more. Withdraw is offered on the panel **and** on the History row of a **resolved** issue, and the panel **states that the word still stands**: *"Your confirmation still stands on Resources — and it is still yours to withdraw."* | ✅ `_assertWithdrawSurvivesResolution()` — it **drives the real analysis update to resolution** and grades the affordance there · **NC `theWithdrawDiesOnResolution_bites`** (D191 §5, put back) |
| **E-D192-2** | ⛔⛔⛔ **THE HAND-PATH RE-OPENS THE ISSUE ITSELF.** The obvious way to implement the above — and it **breaks the standing prohibition**: a `resolved` status was put there **by an analysis update** (D088), and **the user may never move the read by hand.** | **`_withdrawCore()` does not touch a `resolved` status.** It restores the **document** and drops the **word**; the status stays exactly where the analysis left it (**last-good, D098g**). The **ANALYSIS UPDATE** (`_analysisUpdateAfterWithdrawal`) re-derives it: **the change is out of the document ⇒ the gap is back ⇒ OPEN**, with its own lifecycle event. **By analysis. Never by hand.** | ✅ `_assertWithdrawSurvivesResolution()` clause *handDidNotMoveTheStatus* + *reopenedByAnalysis* · **NC `theHandPathReopensTheIssueItself_bites`** · **NC `theAnalysisUpdateNeverReopens_bites`** · `_assertNoHandPathMovesTheRead()` (unchanged) |
| **E-D192-3** | ⛔⛔ **THE LIFECYCLE CHEVRON DRAWS A ONE-WAY RATCHET** (`Open → Addressed → Resolved`, every earlier state filled in). **The states are reversible. The diagram was asserting otherwise.** | **D192b.** The arrows are **`⇄`**. **No trailing fill** — only the state the issue is *actually in* is lit (*a filled-in past is a claim the past is settled*). The ⓘ says it plainly: *"These states move both ways… Either way it is the analysis that moves it — never a manual step."* | ✅ `_assertLifecycleIsNotDrawnAsARatchet()` — glyphs · fill · **and the MECHANISM: the issue is driven Resolved → withdrawn → re-opened and the track must FOLLOW** · **NC `theLifecycleDrawsAOneWayArrow_bites`** · **NC `theLifecycleFillsInThePast_bites`** |
| **E-D192-4** | ⛔ **AN ANSWERED CLARIFICATION ATTESTS TOO** — same two lines as `applyFix`. | **D192c (approved as proposed).** Same record, same inverse (*"Withdraw this answer"*), registered in `_ISSUE_TRANSITIONS`. | ✅ `_assertEveryDecisionTransitionHasAnInverse()` (the sweep pulled it in) |
| **E-D193-1** | ⛔⛔⛔⛔ **THE USER EDITED THE DOCUMENT AFTER THE FIX, THEN WITHDREW — AND OSLO DELETED THEIR WRITING** to undo a change **OSLO** made. *The tool overruling the human, with their own prose as the casualty.* | **THE RESTORE IS CONDITIONAL, AND THE CONDITION IS MEASURED.** `_docTouchedSince()` compares **content and version identity** against the document exactly as OSLO left it (`bodyAfter` / `verAfter`) — **not a flag someone remembered to set.** **Untouched ⇒ restore. Edited since ⇒ ⛔ DO NOT RESTORE:** the attestation is withdrawn **alone**, and OSLO **says so plainly** — *"Your edits since are kept. OSLO's change is still in \<document\> — remove it yourself if you want it gone."* **A withdrawal may never reduce the user's authored content.** *(Measured: 12,299 bytes → 12,299 bytes; the user's sentence survives byte-for-byte.)* | ✅ `_assertWithdrawalNeverDeletesTheUsersWriting()` — the edit is staged through the **editor's own commit path**, and the guard **also proves an untouched document IS still restored** (else it would pass by the restore being dead) · **NC `theRestoreDestroysTheUsersEdits_bites`** · **NC `theNonRestoreIsNotStated_bites`** |
| **E-D193-2** | ⚠️ **THE USER IS MID-KEYSTROKE WHEN THEY WITHDRAW.** The editor autosaves on a ~1.5s debounce, so the last burst is **uncommitted** — invisible to the detection, and the restore would take it. | **`confirmWithdraw()` commits the pending edit FIRST**, through the editor's one commit path (`commitArtEdit`). **The user's words land before OSLO does anything.** | ✅ `confirmWithdraw()` clause · covered by the D193a guard's construction |
| **E-D193-3** | ⛔⛔⛔ **TWO DECISIONS ATTEST ONE DOCUMENT, AND WITHDRAWING ONE DROPS THE ATTESTATION** — *retracting a confirmation the user has NOT retracted.* | **D193b — REFCOUNTED BY DECISION, COMPUTED.** The attestation stands while **ANY** standing decision attests it (`_attestBy[art]`); it drops only when the **LAST** one is withdrawn. **And Reliability restores to its PRE-*FIRST*-ATTESTATION value** (captured at the 0 → 1 edge in `_ATTEST_BASE`) — not one step back, which would leave a raised Reliability with **nothing attesting it**. *(Measured on `Resources` with ISS-01 + ISS-03: `Low → Moderate → High`; withdraw one ⇒ **still attested, High**; withdraw the last ⇒ **derived, Low**.)* | ✅ `_assertAttestationIsRefcountedByDecision()` — **it CONSTRUCTS the two-decision document** (a vacuous pass here is a failure) · **NC `withdrawingOneOfTwoDropsTheAttestation_bites`** · **NC `theReliabilityRestoresOneStepBack_bites`** · **NC `theAttestationSurvivesAnEditedDocument_bites`** |
| **E-D193-4** | ⚠️ **A SECOND APPLIED FIX IS "THE DOCUMENT MOVING ON" TOO.** Two fixes land on one document; the user withdraws the **first**. Restoring to its pre-fix version would **destroy the second fix — which the user has NOT withdrawn** — leaving *"Confirmed by you"* over text that is gone. | **The same conservative rule covers it:** the document is not byte-identical to what *this* fix left, so it is **TOUCHED**, and **a touched document is never restored.** Withdrawing in **LIFO order restores cleanly**; out of order, OSLO keeps the text and says so. **Nothing of the user's is ever destroyed to undo something OSLO did.** | ✅ `_docTouchedSince()` (content/version identity — it does not care *who* changed the document, only that it changed) |

## D194 (owner, 2026-07-13) — the Progress rows: say it once, say it in the ratified vocabulary

> **⛔ DL-111 + erratum (2026-07-14).** The panel was later rebuilt from the two-`.pg-row` ledger these edges
> describe into a **foundation bar** — a computed **grounded-facts hero (ATTESTED claims only, never
> grounded+inferred)**, a **proportional solid bar of two provenance states** (*Confirmed by you* — grounded, cool
> accent, label only · *From OSLO* — **inferred**, hatched), and **load-bearing as its OWN line below the bar, a
> superset** (*"Your read leans on N inferences — the inferred claims above plus inferred assumptions, relationships
> and metrics · See them →"*, the map link intact). The E-codes below record the **D194 build**; the **structure**
> edges (say-it-once, two `.pg-row` hosts) are superseded, while the **single-source** guarantees and the
> incommensurable-populations point carry forward. **Three POPULATION guards were added — grounded == attested-only;
> exactly two provenance classes; load-bearing never inside/`+`-joined the bar — because the old guards passed these
> three lies while only proving *computed* and *registry-sourced*.** Severity red is scoped to Critical; deltas
> stay neutral.

| # | The edge / the defect | What the build does | Proof |
|---|---|---|---|
| **E-D194-1** | ⛔⛔⛔ **THE LOAD-BEARING ROW SAID IT TWICE.** Label **`YOUR READ RESTS ON`** + copy *"13 inferences **your read rests on**"*. **A straight D179e violation — one home — in the row carrying the single most valuable number in the product.** | **The label IS the sentence.** The value carries the NUMBER and the NOUN and nothing else. *(DL-111 + erratum moved load-bearing to its own **leans line below the bar** — **"Your read leans on 20 inferences — the inferred claims above plus inferred assumptions, relationships and metrics · See them →" ↓7**, one caption, the map link intact, a superset never `+`-joined to the bar.)* ~60% less text; nothing said twice. | ✅ `_assertNoProgressRowSaysItTwice()` — grades **every** row, not one dead string · **NC-D194-01** (the phrase, put back) · **NC-D194-02** (a cell that IS its heading) |
| **E-D194-2** | ⛔ **DELETING THE REPETITION COULD HAVE TAKEN THE COUNT OR THE LINK WITH IT.** Honesty by amnesia is the worse trade — again. | The guard **measures the happy path**: the row must still carry its `<b>` count, the noun *"inferences"*, a working delta (proven by **moving the baseline**, not asserting) and the `See them →` route to the Inference Map. | ✅ **NC-D194-03** (`theMapLinkIsLostWithTheRepetition`) · **NC-D194-04** (`theCountIsLostWithTheRepetition`) |
| **E-D194-3** | ⛔⛔⛔ **"AI INTERPRETATION \| YOUR UNDERSTANDING" — THE OWNER'S OWN PROPOSAL.** The **instinct is exactly right** (the epistemic classes *are* that distinction) — **and both words are already canonical at a different size.** `interpretation` is **one of the six `ContextItem.item_type`s** (DL-053); **`understanding` is the word CONFIDENCE is made of** — *"Your Understanding" = "the claims you grounded"* would make **understanding** name **OSLO's assessment** and **the user's evidence** on the same screen. And **OSLO never calls itself "AI."** | **Rejected — with the idea preserved.** The GROUNDED row says it in words canon already owns: **`From OSLO` 11 · `Confirmed by you` 17** (D011/D069). **One vocabulary, everywhere — and it TEACHES**, because those are the same two words the user meets on issues, documents, reports and the Inference Map. | ✅ `_assertEpistemicClassNamesAreSingleSourced()` · **NC-D194-06** (`theInterpretationUnderstandingHeadings_bites`) |
| **E-D194-4** | ⛔⛔ **A NAME THAT CAN DRIFT IN ONE SURFACE HAS ALREADY DRIFTED.** Eighteen surfaces typed *"From OSLO"* / *"Confirmed by you"* / *"Attested by <name>"* as literals. | **ONE REGISTRY (`EPI_CLASSES`), ONE READER (`epiClassName()`), ONE PAINTER (`_epiLabelHTML()` / `_paintEpiClassLabels()`).** Every class **label** carries `data-epi-class` and is repainted from the registry — including the **static** ones (the Outcome-Confidence chip, the Inference-map key), which now carry **no class name in the markup at all**. | ✅ **Mechanism proof:** the guard **renames the classes in the registry** and repaints — every label must follow; one that does not is a literal. **Plus a source scan of the render paths** (a path not on screen now will be next). · **NC-D194-05** · **NC-D194-07** |
| **E-D194-5** | ⛔⛔⛔ **A TWO-SLOT ROW CANNOT HOLD THE THIRD CLASS.** *Attested by \<name\>* (D115) is real canon. | **The row is a MAP OVER `EPI_CLASSES`, not two hard-coded cells.** The third class is **ABSENT today — and absent is correct**: reviewer evidence attaches to an **ISSUE**, not a `ContextItem`, so the count is **not computable** and **a count OSLO cannot compute is not shown** (D173). **It is never drawn as a zero.** | ✅ `_assertThirdEpistemicClassIsRepresentable()` — **gives a real `ContextItem` an `EV-AT3-` evidence id and re-renders: a third cell appears, named *"Attested by Dana Whitfield"*, with its own home** · **NC-D194-08** (the two-slot row) · **NC-D194-09** (drawn as a zero) · **NC-D194-10** (renamed) · **must-not-fire:** *absent is correct* |
| **E-D194-6** | ⛔⛔⛔ **MERGING THE BAR AND THE LOAD-BEARING LINE COLLAPSES DIFFERENT POPULATIONS INTO A RATIO.** The **solid bar** asks *"how much of this is grounded vs inferred?"* — a **comparison** (two provenance states). The **load-bearing line** asks *"how much of the READ leans on inference?"* — a **superset** of the inferred claims. Fused, the user reads the inferred-claims count against the load-bearing count as a **RATIO. It is not one.** | **Distinct by construction (DL-111 + erratum): load-bearing is its OWN line below the bar, never `+`-joined.** Neither presents the other's number as its complement: **no denominator, no percentage, no "of", no ratio glyph** on either. | ✅ `_assertPgxBarStructure()` (load-bearing never inside/`+`-joined) · `_assertProgressRowsStayDistinct()` · **NC-D194-11** (the merge, built) · **NC-D194-13** (20 exceeds 12 legitimately — different populations) |
| **E-D194-7** | ⛔⛔⛔ **AND THE TWO ROWS DO NOT EVEN SHARE A DENOMINATOR — FOUND BY THE GUARD, NOT BY READING.** **GROUNDED counts CLAIMS** (`item_type='claim'`, DL-109 §2a): **11 From OSLO · 17 Confirmed by you.** **LOAD-BEARING counts INFERRED ITEMS OF ANY TYPE** (DL-109 §2b / D181): **12**, drawn from **37** inferred items. **So load-bearing (12) legitimately EXCEEDS From OSLO (11) — and merged, the panel would read *"12 of the 11 things OSLO made up."*** | **Both numbers are right; neither is changed.** This is **the sharpest argument for D194d there is** — the rows are incommensurable, so the ban on merging is structural, not stylistic. **The adjacency itself is escalated (O-D194-1), never resolved here.** | ✅ the guard now asserts the invariant that **is** true (load-bearing ⊆ **inferred items**) and would go RED if every inference were "load-bearing" (a shrug, not a finding) · **must-not-fire** `theTwoRowsMayCountDifferentPopulations` |
| **E-D194-8** | ⚠️⚠️ **GUARD DEFECT #30 — AND IT REDDENED A CORRECT PANEL.** `_assertInferencesAreCalledInferences()` (D183d) read `el.textContent`, which **welds adjacent elements with no separator**. Once D194a removed the trailing phrase, the DOM read `<b>12</b> inferences` immediately followed by `<a>See them →</a>` ⇒ **`"12 inferencesSee them"`** ⇒ the guard's own `\binferences\b` boundary **could not see the word it was looking for.** | **FIX THE GUARD, NEVER THE DOCTRINE.** A new reader — `_pgReadableText()` — extracts text **leaf by leaf, joined by whitespace: the way the eye reads it.** *(Padding the markup with a cosmetic space so a broken regex could see it would have been shaping the product to fit the instrument.)* | ✅ the D183d guard is green on the real panel, and its NCs still bite |
| **E-D194-9** | ⚠️⚠️ **GUARD DEFECT #31 — MY OWN, AND IT WOULD HAVE FORCED A RENAME OF RATIFIED CANON.** The first draft of `_assertNoProgressRowSaysItTwice()` tested *"does the value contain the label?"* on every row — so the **OPEN** row (heading *"Open"*) went RED on its own count ***"Open questions 2"***. **That is not the label said twice**; *"Open questions"* is the count's own ratified name (D180a·2). | **The rule, stated precisely:** (i) a **phrase label (≥2 words)** may not reappear as a phrase in its own value — *that* is the D194a defect; (ii) **no cell may simply BE its row's heading**, on any row. A shared **noun** between a heading and a count's own name is neither. *(Duplicate **counts** are `_assertNoCountIsRenderedTwice()`'s subject, and it keys on `data-count-key` — the right instrument for the right defect.)* | ✅ **must-not-fire `aCountMaySharANounWithItsHeading`** — the control that pins the defect down |
| **E-D194-10** | ⚠️ **GUARD DEFECT #32 — VACUOUS, AND ITS OWN NEGATIVE CONTROL CAUGHT IT** (D166 §2 earning its keep). The source-literal test required the literal to **BE** the class name (`/^From OSLO$/`) — but a painter never types the name alone; it types `'<span class="sro-epi">From OSLO</span>'`, with the name **embedded**. **The test matched zero things.** | The name is now searched for **inside** every literal. **Case-sensitive, deliberately:** the class NAME is the capitalised token — *"it is not confirmed by you"* is a sentence, not a class name, and reddening on it would repeat the label/sentence confusion D188 already cost us. | ✅ **NC-D194-07** — it went straight through the first draft |

## D195 (owner, 2026-07-13) — a class that resolves to NOTHING · plans do not infer · the flag's counts are computed

| # | The edge / the defect | What the build does | Proof |
|---|---|---|---|
| **E-D195-1** | ⛔⛔⛔ **THE P1. THE *USAGE & LIMITS* DIALOG WAS `class="wm"` — A CLASS DEFINED NOWHERE.** Five other dialogs carry `.wmodal`. So the panel had **no background, no border and no radius**, and the page bled straight through it. **It was never a colour bug — it was a NAME bug, and the name looked perfect.** *(`.wm` would have satisfied any "does the dialog carry its panel class?" check anyone could have written.)* | **`class="wmodal"`.** And the class of defect is closed: **`_assertEveryDialogHasAnOpaquePanel()` grades the COMPUTED BACKGROUND of every dialog panel** — `getComputedStyle` → resolve the `var()` chain against that theme's `:root` → parse to RGBA → **alpha must be exactly 1** — **in BOTH themes, on all 25 panels.** *(Measured: `#1B1F24` dark / `#FFFFFF` light, alpha 1, radius 16px.)* | ✅ `_assertEveryDialogHasAnOpaquePanel()` · **NC-D195-01** (the P1, put back: point the dialog at `.wm` ⇒ RED) · **NC-D195-02/03** (`rgba(…,0)` · `transparent`) |
| **E-D195-2** | ⛔⛔⛔ **THE THIRD TIME A NAME RESOLVING TO NOTHING HAS PRODUCED A DEFECT.** Twice a **guard selector** matched **ZERO elements** and passed for free; now a **CSS class** matched **ZERO rules** and rendered a transparent modal. **The pattern is the bug** (D166). | **The general rule, guarded: `_assertEveryClassNameResolves()`.** A name resolves if **the cascade paints it** *or* **the product reads it** (`querySelector` / `closest` / `contains`). ⛔ **A WRITE IS NOT A READER** — `classList.add('wm')` proves nothing. **Three surfaces: the live DOM · the render paths · the writes.** | ✅ `_assertEveryClassNameResolves()` · **NC-D195-11/12/13** · **must-not-fire:** *a class with no rule but a real JS reader (`.pn-slot`, `.sb-art`) is NOT dead* |
| **E-D195-3** | ⛔⛔⛔ **AND THE SWEEP FOUND THE SECOND ONE — IN A RENDER PATH, WHERE NO DOM SCAN COULD SEE IT.** The Readout's **"Signed"** input carried **`class="in"`** — **defined nowhere.** The one styled input in the product is `.wm-in`, so the field the memo goes out under rendered as a **raw browser input** (white box, black text, square corners) inside a dark themed popover — **and it only paints when the popover is opened.** | **`class="wm-in"`.** *(This is exactly why the guard sweeps `class="…"` literals in the product's render paths and not just the booted DOM.)* | ✅ `_assertEveryClassNameResolves()` clause (b) · **NC-D195-11** (a dangling class in a render path) · **AA:** 13.15:1 dark / 15.05:1 light |
| **E-D195-4** | ⚠️ **SIX MORE DEAD NAMES, ALL HARMLESS — AND ALL REMOVED RATHER THAN GIVEN AN INVENTED RULE.** `.attach-hint` (a fully inline-styled span) · `.hart` (on `.heat-rowh`, which already carries the cursor and the hover) · `.pg-star` (on `.pg-row`; every reader keys on `[data-row]`) · `.hli` (a wrapper whose job is `role="listitem"`; the styled row inside it is `.hrow`) · `.pn-on` (a **write-only** body class) · `.superseded` (a **write-only** marker on retired chat clarification boxes). | **A dangling class on a decorative element is harmless — and it is still a name that means nothing.** Each was **deleted, not styled**: inventing a rule to make a dead name resolve would be **inventing product design to satisfy a guard.** *(The stand-down of a retired clarification box is — and always was — the id removal + the disabled controls. **Whether it should also LOOK retired is escalated, not invented** — O-D195-2.)* | ✅ `_assertEveryClassNameResolves()` green with **0 dangling names**, DOM + render paths + writes |
| **E-D195-5** | ⛔⛔ **A DATA VALUE IN THE CLASS NAMESPACE IS A DANGLING NAME WAITING TO HAPPEN.** The waitlist signal key was interpolated straight into `class="wl-sig <key>"`. `.wl-sig.review` has a rule; **`.referral` has none** — so `referral` was a class that resolved to nothing, **and the next key added would have been another one.** | **The key is now an ATTRIBUTE**: `class="wl-sig" data-sig="<key>"`, and the accent rule is `.wl-sig[data-sig="review"]`. **Identical rendering; the whole latent class of defect is gone.** *(Only the band-1 signal is accented — now a deliberate, addressable rule rather than an accident of naming.)* | ✅ `_assertEveryClassNameResolves()` · AA 4.67:1 dark / 4.75:1 light (unchanged) |
| **E-D195-6** | ⛔⛔⛔ **THE NEXT DIALOG COULD SHIP UNREGISTERED AND NOTHING WOULD GRADE IT.** A registry is only as good as the discipline of the person adding to it — **and that is exactly the discipline that failed.** | **FORGETTING IS MECHANICAL, NOT MORAL.** Two completeness sweeps close the registry against the future: **(i) every `*Scrim` overlay that holds anything must yield a registered panel**, and **(ii) every `role="dialog" \| "alertdialog" \| "menu"` element must BE a registered panel or CONTAIN one.** **Ship a new dialog, forget to register it ⇒ RED.** | ✅ **NC-D195-09** — a brand-new scrim + `role="dialog"` panel is injected; the guard finds it |
| **E-D195-7** | ⛔⛔ **THE GUARD ITSELF COULD PASS FOR FREE** — an empty registry, a selector that matches nothing, or a colour resolver that cannot resolve. **All three are how the last two guard defects happened.** | **The apparatus proves itself BEFORE it grades anything**: a token-painted probe, an `rgba(…,0)` probe, a `transparent` probe and an **unpainted** probe. **If the resolver cannot separate them, the guard goes RED without grading a single panel.** An empty enumeration, an unresolvable selector or an unreadable `--surface` ⇒ **RED**. | ✅ **NC-D195-07** (a selector resolving to nothing) · **NC-D195-08** (the enumeration emptied) · **NC-D195-10** (the resolver blinded) |
| **E-D195-8** | ⛔⛔ **AND THE 85%-OPAQUE PANEL — THE ONE THAT LOOKS FINE.** A panel at `rgba(…,.85)` ghosts the document through the dialog and the reader cannot tell which words belong to which surface. **A guard that only asked "is there a background?" would ship it.** | **Alpha is 1 or it is a defect.** The guard grades the **parsed alpha**, not the presence of a declaration. | ✅ **NC-D195-04** (85% opaque) · **NC-D195-05** (`var(--surfacce)` — the same bug one level down: a **token** that resolves to nothing) · **NC-D195-06** (broken in the **light theme only** — a guard that graded one theme would have shipped it) |
| **E-D195-9** | ⛔⛔⛔ **"WHERE THE PLAN INFERRED" HANDS THE INVENTION BACK TO THE DOCUMENT.** The owner asked for it. **Plans do not infer. OSLO does** — and the Inference Map exists for exactly one reason: **to show what OSLO MADE UP.** Read as *"what the plan assumed"*, the map becomes someone else's problem, in the source, and the feature is defeated. | **"Where OSLO inferred."** Same actor as the ratified class name **"From OSLO"** (D194c). **The guard is the RULE, not the string:** no surface may make the plan / document / artifact / report the **SUBJECT** of an inference verb. **"Inferred FROM the plan" is a SOURCE and stays legal** — that is the mechanism. | ✅ `_assertOsloIsTheInferringActor()` — DOM (scripts/styles excluded) · render paths · **the TOUR registry** · **NC-D195-14** (the owner's words, adopted) · **NC-D195-15** (a different noun, a render path) · **NC-D195-16** (the tour teaches it) · **must-not-fire:** *a plan may be the SOURCE* |
| **E-D195-10** | ⛔⛔ **THE OVER-CORRECTION: DELETE THE ACTOR ALTOGETHER.** *"Where inference happened."* No plan, no OSLO, nobody. **A map of inference that will not say WHOSE inference it is has lost its point** — and a guard that only banned a string would have passed it. | **The positive half is measured too:** the subtitle must be **on screen** and it must **name OSLO**. | ✅ **NC-D195-17** (`theActorIsDeletedAltogether_bites`) |
| **E-D195-11** | ⛔⛔ **THE FALSE-CONFIDENCE FLAG NOW CARRIES TWO NUMBERS.** *"8 of 11 items read as inference"*. **`NC-D109-10` already proves its SUBJECT is computed. NOTHING PROVED ITS NUMBERS WERE.** A literal would go on saying *"8 of 11"* long after it was 11 of 14. | **STATE PROOF, not a copy scan** (D166 §1): the condition is **constructed on the real model**, three inferences are **added to the flagged document**, and **the numbers on screen must move with them.** *(Measured: **4 of 7 → 7 of 10**.)* Both numbers come from `_ciArtStats()` — **the same registry the map, the ledger and the Progress rows read** (D179e). | ✅ `_assertFalseConfidenceFlagCountsAreComputed()` · **NC-D195-18** (both counts typed) · **NC-D195-19** (**half-computed is not computed** — the numerator follows, the denominator is a literal) · **must-not-fire:** *the flag's ABSENCE is correct when nothing qualifies* (D173) |
| **E-D195-12** | ⚠️⚠️ **GUARD DEFECT #33 — MY OWN, AND THE GUARD CAUGHT ITSELF.** The first draft of `_assertOsloIsTheInferringActor()` read the DOM with a plain text walk over `<body>` — and **`<script>` lives inside `<body>` in this file.** So the guard was handed **the entire source of the product, including its own doctrine comment explaining why "the plan inferred" is wrong** — and reddened on it. **A guard that is its own subject** (D166's own cautionary tale). | **`_screenText()` — the text a user can actually READ.** `SCRIPT` / `STYLE` / `NOSCRIPT` / `TEMPLATE` subtrees are rejected by the walker. **Code is not copy.** *(And the class-name guard has the same discipline from the other side: **guards and their negative controls are apparatus, not product**, and are excluded from the source sweep by the same rule the probe fence uses — otherwise the `.ip-probe` scaffolding an NC injects would fail the build.)* | ✅ the guard is green on the real product, and all four of its NCs still bite |

## D196 — the verb/state split (owner, 2026-07-13)

| ID | Edge case | Behaviour |
|---|---|---|
| **E-D196-1** | ⛔⛔⛔ **THE REJECTED WORD.** *"Stabilize the read"* is applied to **the false-confidence case**: *"Scope reads strong — but 8 of 11 items are inference."* | **That read is ALREADY PERFECTLY STABLE — it has not moved in months, because it rests on nothing.** *"Stabilize"* would **name it a success.** It also makes **stillness** the goal, against *"honest, revisable confidence"*, and it has **no opposite** (*Grounded ↔ Inferred* is the pair DL-109 teaches). **Rejected — and the guard now makes the alternative mechanical.** |
| **E-D196-2** | ⛔⛔ **THE OVER-CORRECTION: sweep *"grounded"* out too.** A worker "helpfully" renames the Reliability band to *"largely **evidenced**"* or *"largely **confirmed**"*. | ⛔ **BOTH ARE COLLISIONS.** *"Evidenced"* collides with **Evidence** — one of the three Reliability components (Coverage · Evidence availability · Assessability): **two sizes of one word, the exact D194b error.** *"Confirmed"* is worse: **a read is grounded by EVIDENCE and by *Attested by \<name\>* too — and that is NOT the user confirming.** → `NC-D196-08/09/10` all **RED**. |
| **E-D196-3** | ⛔⛔⛔ **THE HOVER-ONLY SITE NO GREP FOUND.** `#cg-feas-tipb` — the CAF-row tooltip in the hero — said *"…the lowest dimension — **ground it** to lift the read."* **Lowercase, inside a `.caftip` that only appears on hover.** | **The manual sweep missed it. THE GUARD FOUND IT** on its first run. *This is the whole argument for D196c: **the split is mechanical, not remembered.*** Now: *"…**confirm it** to lift the read."* |
| **E-D196-4** | ⛔⛔⛔ **A GUARD WAS HOLDING THE OLD WORD IN PLACE.** `_assertConfPopIsAReadout()` clause (5) asserted **`/\bground\b/`** on the limiter — *"a limiter without a verb is trivia"* (D185.4). | **So the moment the copy obeyed D196, the build would have gone RED for being right.** ⛔ **A guard that pins a word the doctrine has moved HAS BECOME THE DOCTRINE.** Clause (5) now asserts `/\bconfirm\b/`. **Fix the GUARD, never the doctrine** (D166 §3). **Guard defect #34.** |
| **E-D196-5** | ⚠️ **THE CTA WHOSE OBJECT IS A VARIABLE.** `#cpp-out` is built as `'Confirm ' + lim + ' →'`. **It is only on screen when the lead recommendation sits on the limiting dimension** — so in most states a DOM scan sees *"Open full breakdown → Overview"* instead. | **The guard reads the RENDER PATHS, string-literal by string-literal, with a tail rule for exactly this shape** (a literal that *ends* on the bare verb). → `NC-D196-03` puts `'Ground ' + lim` back **in a painter, not in the DOM** → **RED**. |
| **E-D196-6** | ⚠️⚠️ **THE WORDS THAT MUST SURVIVE THE SWEEP.** Five strings on screen contain the very substrings the two rules forbid. | ✅ ***"Confirmed by you"*** — a ratified **epistemic class** (D011/D069/D194c). ✅ ***"largely grounded"*** — the **STATE** (D183c/D196b). ✅ ***"you grounded"*** — **past tense**, D187's one green count; a **measured fact, not an order**. ✅ ***"Your evidence is solid ground."*** — a **NOUN**, the foundation metaphor (D186). ✅ ***"Progress is grounding, not clearing"*** — a **gerund with no object, addressed to nobody**. **All five are must-not-fire controls. A guard that reddens on any of them is a word filter, not a copy rule.** |
| **E-D196-7** | ⚠️ **WORD BOUNDARIES.** *"back**ground**"* · *"**ground**breaking"*. | **The verb rule requires a non-letter before the lemma and a direct object after it.** *"Lowest" contains "owes"* (DL-109 §3). → `mustNotFire_wordBoundariesHold`. |
| **E-D196-8** | ⛔⛔ **THE SWEEP EATS ITS OWN JUSTIFICATION.** A rename pass turns the epistemic class into *"Grounded by you"*. | ⛔ **The verb moved to *Confirm* PRECISELY BECAUSE the class name already said it.** A sweep that renames the class **has destroyed the reason it existed.** The guard measures the class name **and that it still renders on the foundation bar's attested segment** (DL-111) → `NC-D196-11` **RED**. |
| **E-D196-9** | ⚠️ **THE INTERNAL WORD DID NOT MOVE.** The code and the doctrine comments still say *"the user **grounds** an artifact"*, and the state machine still writes `basis='attested'`. | **Correct, and deliberate.** D196a's scope is **copy addressed to the user**. *Grounding an artifact* is still exactly what happens **to the read** when the user confirms a document — **and `attested` was already the internal name.** **No user-facing surface says it.** *(Guard-proven: 0 hits across 9 views, the tour, the notes and every render path.)* |

## D197 · D198 · D199 — the name, the marker, the guard gap (owner, 2026-07-13)

| ID | Edge case | Behaviour |
|---|---|---|
| **E-D197-1** | ⛔⛔⛔ **A GUARD WAS HOLDING THE OLD WORD IN PLACE — AND WOULD HAVE GONE RED FOR BEING RIGHT.** `_assertNoHoldingItAnywhere()` clause (f) demanded the literal string ***"rests on"*** inside `#pg-counts`. **D197 renames that label to `LOAD-BEARING` — so the guard would have failed, and the only way to green it again was to PUT THE DEAD LABEL BACK.** | ⛔ **A guard that pins a word the doctrine has moved is a RATCHET, not a guard.** Clause (f) now grades what it always existed to prove: **the count and its NAME survived the rename**, reading the name from `TERMS`. **Fix the GUARD, never the doctrine** (D166 §3). **Guard defect #35.** |
| **E-D197-2** | ⛔⛔⛔ **AND A SECOND GUARD WOULD HAVE GONE QUIETLY VACUOUS.** `_assertNoProgressRowSaysItTwice()` only graded labels of **≥2 words** — because the defect it was written for was the four-word phrase *"YOUR READ RESTS ON"*. **D197 makes the label ONE word.** The guard would have **stopped grading the only row it was ever written for**, then failed its own vacuity bar. | **A guard whose subject is defined by the accident of a label's word count is measuring the LABEL, not the RULE.** The rule is: **a row's name may not reappear in its own value — one word or five.** It is now graded on the row's **prose** (`.pg-chip` name-chips excluded), so *"Open questions"* under an **OPEN** heading is still legal, and *"13 load-bearing inferences"* under a **LOAD-BEARING** heading is not. **Guard defect #36.** |
| **E-D197-3** | ⛔⛔ **AND ITS NEGATIVE CONTROL WOULD HAVE DIED SILENTLY.** `NC-D194-01` injected *"…your read rests on"* into the value. **Under the new label that injects nothing** — the control would have gone on reporting `true` while testing **nothing at all.** | **A control that no longer injects the defect is not a control.** It now injects **`TERMS.loadbearing.label`** into the value, so it can never drift out of alignment with the label again. **Guard defect #37.** |
| **E-D197-4** | ⚠️⚠️ **THE OVER-SWEEP: kill *"rests on"* everywhere.** A worker "completes" the rename by deleting the phrase from the section header, the chat citations and the popover. | ⛔ **That would delete the metaphor the NAME WAS CHOSEN TO PAIR WITH.** *The read **RESTS ON** its **LOAD-BEARING** inferences* is **one picture.** The guard fires on the **complete-label form only** (`_isDeadLbLabel`), and it carries a **must-not-fire clause that FAILS if the prose disappears**: *"the Inference map no longer says what the read RESTS ON anywhere."* |
| **E-D198-1** | ⛔⛔⛔ **THE CHIP GOES BACK IN THE FLOW.** The defect verbatim. | **The in-flow child signature diverges → RED.** `NC-D198-01`. |
| **E-D198-2** | ⛔⛔⛔ **THE SUBTLE FORM: the gutter moves to the marked variant.** The row keeps its children, but `.mk-on` gets the `padding-left`. **A child-count check would see nothing wrong** — and a marked row's content now starts 20px further in. | **The COMPUTED BOX MODEL diverges → RED.** `NC-D198-03`. *This is why the guard measures the box, and not just the children.* |
| **E-D198-3** | ⛔⛔ **NOTHING IS MARKED AT ALL.** No load-bearing item, no false-confident document. | **"Marked and unmarked rows are identical" is TRUE FOR FREE when one of them does not exist.** The guard **fails** rather than passes. `NC-D198-08`. **A vacuous pass is a FAILURE** (D166 §1). |
| **E-D198-4** | ⛔⛔ **THE MARKER BECOMES COLOUR ALONE** (`content:none; width:0`) — a hue change on the row and nothing else. | **A colour-blind reader now sees no marker.** The guard grades the **VALUES**, not "is the property mentioned": `content` must not be `none`, `width` must parse **> 0**. `NC-D198-05`. |
| **E-D198-5** | ⛔⛔⛔ **THE MARKER TAKES SEVERITY COLOUR.** | **Severity colour is ISSUES-ONLY** (D003). A load-bearing inference is a thing to **read first**, not a fault; a document worth verifying is a thing to **check**, not a failure. `NC-D198-06`. |
| **E-D198-6** | ⛔⛔⛔ **A11Y TRADED FOR TIDINESS.** `VERIFY` is deleted and the row's `tabindex`/`onkeydown` go with it. **An action that only exists on hover is unreachable.** | The guard requires **every** by-document row to be **keyboard-activatable** (7/7) **and** the panel-level *"Open \<document\> →"* to survive. `NC-D198-09` · `NC-D198-10`. |
| **E-D198-7** | ⚠️ **A LATER CSS RULE QUIETLY OVERRIDES THE MARKER.** A theme block, an injected sheet, a "quick fix" at the bottom of the file. | **The guard reads the MERGED declarations — later rules win, exactly as the cascade resolves them.** *A guard that reads only the first rule it finds is reading the author's intention, not the browser's.* |
| **E-D199-1** | ⛔⛔⛔ **THE MISS ITSELF: the trend surface said "Confidence."** | **D183b bound the label AND the number; only the number got a guard.** `NC-D199-01` reverts the trend label → **RED**. |
| **E-D199-2** | ⛔⛔⛔ **THE GUARD CANNOT SEE A DORMANT SURFACE.** `#hist-trend` is empty until History is opened; `#exportCurrency` until the export dialog is. ***"It wasn't on screen"* is exactly how D199 happened.** | **The guard PAINTS the dormant surfaces from their own render paths, reads them, and restores every byte** (D182 — both panes are hidden, so nothing reaches the user either way). `NC-D199-11`: hide the trend host → the guard goes **BLIND → RED**, never green. |
| **E-D199-3** | ⚠️⚠️ **THE PROSE THAT MUST SURVIVE.** *"Confidence **is** understanding maturity — not project health"* (the doctrine) · *"the **false confidence** case"* (D052, a named condition) · *"why is **my confidence** where it is?"* (the user speaking). | **All three are must-not-fire controls.** The role test exempts a **possessive/qualifier** before the word, and *"is"* is a hit **only when a BAND follows it**. **A guard that reddens on the doctrine will get the doctrine deleted to make it green** (D166 §3). |
| **E-D199-4** | ⛔⛔ **A SECOND MISS, FOUND BY THE SWEEP.** `PN_SLOTS.pnPayoff` still taught *"THE 0–100 INDEX … **DEMOTED, NOT DELETED**: calibrate it and it earns its hero slot back"* — **D173d, the decision D183b SUPERSEDED**, live in the notes layer for the whole build. `_assertNoZeroToHundredIndexAnywhere()` read the DOM, the render paths, the snapshot, the cascade and the **TOUR** — **and not `PN_SLOTS`.** | **The note is rewritten and the guard now reads `PN_SLOTS` too.** ⛔ **And the notes layer no longer PRINTS the index in any framing** — *a note that keeps printing it keeps it alive* (DL-107), and a scanner cannot tell *"teaching it"* from *"recording its death"* by looking at the digits. **Guard defect #38.** |
