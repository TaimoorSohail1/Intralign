# 03_architecture / legacy_layer_engineering

> ## 🛑 DO NOT BUILD FROM THESE FILES.
> This directory holds the **deprecated layer model** (Judgement / Governance / Communication / Context layers) that the **cognitive spine replaced** under DL-043. The subdirectory names (`judgement_layer/`, `governance_layer/`, `runtime_architecture/`, `components/`) are **historical** — they are **NOT** the canonical architecture. Build to the **cognitive spine** (`Perceive → Retain → Infer → Evaluate → Advise → Disclose`) per `03_architecture/specifications/OSLO_COGNITIVE_RESPONSIBILITY_ARCHITECTURE_SPECIFICATION_V1.md` and the Wave contracts. Each file here carries a DL-043 re-home banner mapping it to its owning responsibility (e.g. `judgement_layer → Evaluate`, `governance_layer → Authority [inactive in R1]`). Use these only as *secondary implementation-detail backing*, never as a primary source. *(KIA-3 escalation banner, 2026-06-05.)*

Secondary (DL-043) layer-engineering material, re-homed under owning responsibilities. Not a primary architecture source.

_Index added in the 2026-06-04 repository reorganization (REPOSITORY_REORGANIZATION_PROPOSAL_V1). Files relocated here by category; referenced elsewhere by bare filename._
